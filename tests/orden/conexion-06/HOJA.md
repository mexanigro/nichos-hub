# HOJA · CONEXION-06 · paleta y eyebrow en el constructor del hub (dos huecos más: 18/36 → 20/36); retiro de CONEXION-05 · 2026-09-23

Escrita por el revisor desde la línea base de CONEXION-05 (aprobada 2026-09-23, T a41f93a · H 0c60811). Estado esperado al abrir A: T a41f93a = origin, H 0c60811 = origin.
Fuentes: `H:`/`T:` = archivos en HEAD; `revisor` = lecturas del 2026-09-23 con salida citada; `hueco` = `T tools/verdad/hueco.mjs --json` en HEAD; `CH` = bloque-04/CONTRATOS-HUECOS.md.
Formato: cada afirmación es una línea `- [ID] (repo) frase · fuente: …`. El nombre del test es la frase exacta entre `(repo) ` y ` · fuente:`. `(T)` sólo en T; `(H)` sólo en H; `(T+H)` en los dos.
Circuito: PLAN.md § Cómo se trabaja, incisos i) a n). Decisiones vigentes: D-1..D-5, D-9..D-11, D-13, D-14, D-16..D-68. Decisiones para esta hoja (revisor, 2026-09-23, tomadas salvo que Liam diga lo contrario): D-69 la paleta se deriva en el hub: `T src/lib/palette.ts` y `T src/lib/oklab.ts` (puros, 320 líneas, sin dependencias) se copian byte a byte a `H src/lib/palette.ts` y `H src/lib/oklab.ts`; la copia de H es la que usa la casilla y el validador (H se despliega solo, no puede importar de T); un guard de H exige los mismos bytes que T **en reposo** (patrón D-61: con una orden viva en cualquiera de los dos repos, difiere con diagnóstico) · D-70 la casilla de paleta (`src/components/config-editors/paleta-editor.tsx`) pide `source`, `origin` y `reason`, toma el modo de `branding.mode` (la casilla de fondo, CONEXION-05) y escribe `branding.colors` y `branding.paletteMeta` **sólo por `derivePalette`**, nunca a mano (CH:11 «nunca a mano»); sólo con `niche === "peluqueria"` · D-71 `validatePalette(config)` nueva en `config-validator.ts`: error si `branding.colors` existe sin `branding.paletteMeta`, si la meta no deriva (fuente sin croma, `reason` corto) o si los colores no coinciden con `derivePalette(meta, mode)` («editada a mano»); los fixtures A y C cumplen (el revisor lo midió: 0 claves distintas en los dos) · D-72 `hero.eyebrow` entra en la pestaña Contenido (sección Hero, junto a titular, subtítulo y CTA), para todos los nichos: la flota también lo rinde (T hero v2–v4 de estética) y el campo es texto · D-73 guard nuevo `T tests/hero-textos.test.ts` (fase `test:unit`) para `hero.eyebrow`: `clampWords` recorta a 4 y hero v6 lo usa con `LIMITS.eyebrow = 4` · D-74 CONEXION-05 se promueve entera (H a, b, d, e = 8; T b, d, e = 6).

Por qué: `paleta` no tiene casilla ni validador declarado (hueco: «sin validador declarado», «sin UI»), aunque es «el primer hueco de toda web» (CH:11) y T ya la deriva con `derivePalette`; nada en H escribe `branding.colors` (revisor: `grep` vacío) y el hub sólo tiene los `theme.*` de la flota. `hero.eyebrow` tiene validador (`validateVariantContracts:660`, aviso > 4 palabras) pero no casilla (la pestaña Contenido edita `titlePrefix`, `titleHighlight`, `titleSuffix`, `subtitle`, `ctaPrimary`, `ctaSecondary`, no `eyebrow`) ni guard. Con esta orden `hueco.mjs` pasa de 18/36 a 20/36.

## A · Las casillas (H)

- [A1] (H) src/components/config-editors/paleta-editor.tsx exporta `aplicarPaleta(config, { source, origin, reason })`, pura, que devuelve un config nuevo con `branding.colors` y `branding.paletteMeta` calculados por `derivePalette` de `src/lib/palette.ts` de H (modo = `branding.mode` o `light`; nicho = `peluqueria`) sin tocar otras claves; aplicada sobre el fixture A sin `branding.colors` ni `branding.paletteMeta`, con el `source`, `origin` y `reason` de su meta, da `branding.colors` igual al de A (deepEqual) y una meta con el mismo `source`, `origin`, `reason` y `niche` y un `derivedAt` ISO; sobre el fixture C (modo `dark`) da `branding.colors` igual al de C; con fuente gris (`#808080`) o `reason` de dos palabras lanza el error de `derivePalette` y no devuelve config · fuente: revisor (`derivePalette` de T sobre la meta de A con modo light y de C con modo dark: 0 claves distintas, 0 que falten, 0 pares WCAG que fallen); T:src/lib/palette.ts:75–81 (se niega con C < 0,03 y `reason` < 3 palabras); CH:11; D-69, D-70.
- [A2] (H) `renderToString(<PaletaEditor …>)` con `niche="peluqueria"` y el `branding` de A muestra el color fuente (`#5d7a57`), el select `origin` con sus cinco valores (`logo`, `local`, `instagram`, `eleccion`, `material`), el `reason` de A, el modo que tomará de `branding.mode` (`light`) y una muestra de los colores actuales (al menos `accent` `#5d7a57` y `surface` `#f5f8f4`); con `niche="barberia"` devuelve cadena vacía; y src/components/client-config-tab.tsx monta `PaletaEditor` pasando `niche` y escribe en el mismo `config` que guarda por `/api/config/${clientId}` · fuente: T:src/lib/palette.ts:19 (`PaletteOrigin`); revisor (colores de A); patrón de las casillas de CONEXION-02..05; D-34.
- [A3] (H) src/lib/palette.ts y src/lib/oklab.ts de H existen y son byte a byte iguales a los de T, y tests/paleta-paridad.test.ts de H lo exige en reposo (con una orden viva en cualquiera de los dos repos imprime «paridad de palette/ diferida: orden viva <id>» y pasa, como el guard de tools/, D-61); con `src/lib/oklab.ts` de H cambiado en un byte y sin órdenes vivas (probado con `HIGIENE_ROOTS` sobre dos temporales) el guard falla nombrando el archivo · fuente: revisor (T:src/lib/palette.ts 211 líneas + oklab.ts 109, sin imports fuera de `./oklab.ts`); H:tests/verdad-02-c.test.ts (patrón de paridad en reposo, VERDAD-09 B1); D-69.
- [A4] (H) `validatePalette` exportada de src/lib/config-validator.ts da **error** cuando `branding.colors` existe sin `branding.paletteMeta`, cuando la meta no deriva (fuente sin croma o `reason` corto) y cuando `branding.colors` no coincide con `derivePalette(meta, branding.mode)` («editada a mano», nombra la primera clave distinta); los fixtures A y C pasan sin errores; A con `branding.colors.text` igual a `branding.colors.surface` da un error con path `branding.colors.text` · fuente: CH:11 («escritos por derivePalette, nunca a mano»); hueco («paleta · validador: sin validador declarado»); revisor (A y C derivan exactos); D-71.
- [A5] (H) la pestaña Contenido edita `hero.eyebrow`: src/components/client-content-tab.tsx tiene en la sección Hero un campo `{ path: "hero.eyebrow", … }` antes de `hero.titlePrefix`, para todos los nichos, y `validateVariantContracts` sigue avisando con más de 4 palabras · fuente: H:src/components/client-content-tab.tsx:44–52 (seis campos del hero, sin eyebrow); H:src/lib/config-validator.ts:660–661; D-72.
- [A6] (H) `scripts/b4-tenant.ts show --id test-b4-peluqueria-a` (con Firestore, sólo lectura) devuelve un `config/{id}` cuyo `branding.colors` es igual al que `aplicarPaleta` construye desde la meta del fixture A y cuyo `hero.eyebrow` es el del fixture A · fuente: CONEXION-03..05 (A4: mismo patrón; el tenant A ya tiene colores y eyebrow del fixture, CONEXION-05-B); D-16 (`show` no escribe); inciso n).

## B · Contrato, guard y línea base (T)

- [B1] (T) verdad/contratos.json y CH: la fila `paleta` gana `ui` = `{ ruta: "/clients/[clientId]", componente: "src/components/config-editors/paleta-editor.tsx", campo: "colors" }` y `validador` = `{ archivo: "src/lib/config-validator.ts", funcion: "validatePalette" }`; la fila `hero.eyebrow` gana `ui` = `{ ruta: "/clients/[clientId]", componente: "src/components/client-content-tab.tsx", campo: "hero.eyebrow" }` y `guard` = `{ archivo: "tests/hero-textos.test.ts", clave: "eyebrow" }`; en CH el párrafo «Hueco «paleta»» (:11) y la fila de `hero.eyebrow` (:36) ganan la nota «casilla de paleta (CONEXION-06)» / «campo de Contenido (CONEXION-06)»; las otras 34 filas del .json byte a byte como en a41f93a · fuente: T:verdad/contratos.json (hoy `paleta`: validador null, ui null; `hero.eyebrow`: ui null, guard null); CH:11, :36; D-70..D-73.
- [B2] (T) tests/hero-textos.test.ts existe, está en `test:unit` de package.json, nombra literalmente «eyebrow», y afirma que `clampWords` de src/lib/words.ts recorta un texto de 6 palabras a las 4 primeras y deja intacto uno de 4, y que src/components/landing/hero/hero-v6.tsx declara `eyebrow: 4` en `LIMITS` y llama `clampWords(hero.eyebrow || brand.tagline, LIMITS.eyebrow, "eyebrow")`; y `hueco.mjs --json` da las cinco casillas en «sí» y `hecho: true` en `paleta` y `hero.eyebrow`, y el total es «20/36 huecos hechos» · fuente: T:src/lib/words.ts:2–5; T:src/components/landing/hero/hero-v6.tsx:32, :178; hueco (hoy 18/36; «hero.eyebrow · guard: sin guard»); D-73.

## D · Retiro de CONEXION-05 (D-74)

- [D1] (T+H) conexion-05 figura en tests/orden/APROBADAS.md con fecha 2026-09-23, T a41f93a y H 0c60811, y rojo-verde --todas en HEAD imprime «conexion-05 · retirada (aprobada 2026-09-23)» y no «orden conexion-05 ·» · fuente: aprobación de Liam 2026-09-23.
- [D2] (T+H) npm test corre las copias tests/conexion-05-a.test.ts, -b, -d, -e en H (8 tests) y tests/conexion-05-b.test.ts, -d, -e en T (6 tests), que importan ./orden/conexion-05/_comun.ts, sin recorte y sin volver a correr copias anteriores; en T van a `test:unit`; tests/orden/conexion-05/ no cambia desde su último rojo (4941cd2 en T, 569f128 en H) · fuente: CONEXION-05 (aprobada); D-74.

## E · Registro

- [E1] (T+H) CLAUDE.md § Arquitectura mínima de H nombra `paleta-editor`, `derivePalette`, la copia `src/lib/palette.ts` y el campo `hero.eyebrow` de Contenido; CLAUDE.md § Puertas de T y de H tiene un párrafo CONEXION-06 que nombra «20/36» y «nunca a mano»; el bloque CONTRATO-DECLARADO es idéntico al del commit aprobado de CONEXION-05 (T a41f93a / H 0c60811) · fuente: E1 anteriores.
- Sin test (Nichos): PLAN.md § Estado fila 4 «CONEXION-06-A (2026-09-23): hoja + 11 afirmaciones (H 9 tests: A1–A6, D1, D2, E1; T 5: B1, B2, D1, D2, E1), tests rojos (T <sha>, H <sha>); B pendiente del «andá»»; CH: párrafo de paleta, fila de eyebrow y § Constructor («28/36 con UI» → «30/36»); `4.3.md` § CONEXION-06 · A.

## Interfaz de prueba (contrato entre los tests de A y el código de B)

Como en CONEXION-05: `node --experimental-strip-types --test`; la utilidad se llama `_comun.ts` y copia (no importa) el cargador de `.tsx`/`@/x`/sin extensión de `tests/orden/conexion-05/_comun.ts`; los imports de `tools/` sólo con `import()` dinámico dentro del test; ninguna afirmación cuenta órdenes ni utilidades (inciso m); el editor se importa con extensión y no puede necesitar `next/*` al cargar. A1: `existsSync` del archivo y «exporta aplicarPaleta» antes de usarla; los fixtures de T se leen por ruta fija; el deepEqual es de `branding.colors` completo y de cuatro campos de la meta (no de `derivedAt`, que es la hora). A2: `renderToString` con las props que la pestaña le pasa; comparación por `includes` de valores literales; con barbería `=== ""`. A3: lectura de bytes de las dos copias por ruta fija (la de T se lee, no se importa) y `HIGIENE_ROOTS` sobre dos temporales para el caso que falla; el diagnóstico se lee del TAP. A4: el validador se importa con extensión. A5: lectura estática del arreglo de campos. A6: `show` real (JSON entero, `.env.local` presente; si falta, falla nombrándolo); rojo por «no exporta aplicarPaleta» antes de `show`. B1 lee el .json (fila por id) y el .md (por texto). B2 lee package.json y los fuentes, importa `clampWords` con extensión (`import()` dinámico) y corre `hueco.mjs --json` real. D1 y D2 como en las hojas anteriores. E1 lee disco y compara el bloque por `git show`.
Ningún test escribe en T ni en H fuera de `tests/orden/conexion-06/`; ninguno toca Storage ni Firestore (el tenant A ya tiene lo que A6 lee: no hace falta reescribirlo); `_comun.ts`, `d`, `e` idénticos T/H por `cmp`; `a` sólo en H; `b` sólo en T; no hay `c`.

## Notas para las sesiones A y B

A: al commit rojo, los catorce tests (11 afirmaciones; H 9, T 5; D1, D2 y E1 en los dos) fallan por su primera aserción: A1, A2, A6 por «no existe paleta-editor.tsx»; A3 por «no existe src/lib/palette.ts en H»; A4 por «validatePalette no está exportada»; A5 por «la sección Hero no tiene hero.eyebrow»; B1 por `ui: null`; B2 por «no existe tests/hero-textos.test.ts»; D1, D2, E1 como siempre. Nombre del test = frase de la hoja (A5).
B: la copia de `palette.ts` y `oklab.ts` es **byte a byte** (se copia el archivo, no se reescribe; si `palette.ts` de T importa `./oklab.ts` con extensión, la copia de H también, y `tsc` de H tiene que aceptarlo); `paleta-editor.tsx` en `src/components/config-editors/` como las demás casillas, montada en la pestaña Config junto a la de fondo (el modo está ahí); botón «Derivar» que llama `aplicarPaleta` y muestra el error de `derivePalette` si se niega; `validatePalette` se suma a la validación que corre al guardar, como las otras. `hero.eyebrow` va en Contenido para todos los nichos (D-72). El guard `tests/hero-textos.test.ts` va en `test:unit`. Los fixtures no cambian; no hace falta `recrear` con Firestore (B lo corre sin Firestore una vez como no regresión). La línea de APROBADAS.md de conexion-05 la escribe B (D1).

## Fuera de esta hoja

`branding.heroToBackdrop` como casilla (D-64); `hero.mask` y `hero.alto` (derivados); las diez filas con casilla sin guard o sin material (`brand.logo`, `brand.logoDark`, `hero.titular`, `hero.subtitle`, `hero.cta`, `staff.photoUrl`, `contact.phone`, `features.themeToggle`, `services.catalogo`…: CONEXION-07); la paleta de la flota (`theme.*`) no cambia; gama al subir (5.1); AUDITORIA-01.
