# HOJA · ARREGLOS-01 · las tres cosas que la corrida en limpio de E2E-01 dejó a la vista, antes de empezar con el diseño; retiro de E2E-01 · 2026-09-25

Escrita por la **sesión A** (inciso p: el segundo pensamiento sólo escribe en el chat). Línea base: E2E-01 aprobada por Liam 2026-09-25, T c4faa5d · H 0c6d3e0. Estado esperado al abrir A: T c4faa5d = origin, H 0c6d3e0 = origin, los dos limpios.
Fuentes: `H:`/`T:` = archivos en HEAD; `medido` = lectura de A del 2026-09-25 con su salida citada; `revisor` = lo que midió la corrida en limpio de E2E-01; `Liam` = decisiones del 2026-09-25.
Formato: cada afirmación es una línea `- [ID] (repo) frase · fuente: …`. El nombre del test es la frase exacta entre `(repo) ` y ` · fuente:`. `(T)` sólo en T; `(H)` sólo en H; `(T+H)` en los dos.
Circuito: PLAN.md § Cómo se trabaja, incisos i) a p) (y **o-bis**, que esta orden escribe). Decisiones vigentes: D-1..D-5, D-9..D-11, D-13, D-14, D-16..D-104. Decisiones para esta hoja:

- **D-105 (Liam, 2026-09-25) E2E-01 aprobada.** T c4faa5d · H 0c6d3e0. Esta orden la retira en `tests/orden/APROBADAS.md` y promueve sus copias a `npm test`; las que salen a la red (Vercel, Firestore, las dos webs) se recortan como en D-89 y D-95, y el recorte se declara.
- **D-106 (Liam, 2026-09-25) antes del diseño se arreglan tres cosas.** Las tres salieron de la corrida en limpio de E2E-01 y ninguna es de diseño: la medición inestable de `e2e.mjs`, el `??` sobre un hueco `""` de `sections.services.images` en T, y LOGO-01 en H (la casilla de logo y la ficha abierta con el slug). Lo que no está en esta hoja no se toca.
- **D-107 (A, medido) la inestabilidad de `e2e.mjs` es de la captura, no de las webs.** El revisor corrió `tests/orden/e2e-01/c.test.ts` **tres veces** sobre las mismas dos webs: la primera dio «pagina-servicios 1280: 1166 px» en la web A; las otras dos, 0 px en las doce zonas. Las webs no cambiaron entre corridas. Medido en el árbol (`T:tools/verdad/e2e.mjs:159–174`, `asentar`): espera `networkidle`, apaga animaciones y transiciones **por CSS**, hace scroll de toda la página, pausa los vídeos, espera `document.fonts.ready` y **duerme 700 ms**. No espera a que las imágenes de la zona estén **cargadas y decodificadas** (`complete && naturalWidth > 0`, `decode()`), no fuerza el `loading="lazy"` a `eager`, no toca las animaciones de la **Web Animations API** (`element.animate()`, que una regla `animation:none` no apaga) y no comprueba que la caja del elemento haya **dejado de moverse**. Un `setTimeout` fijo no es una condición: es una apuesta. Y no alcanza con **congelar**: `e2e.mjs` compara **dos páginas**, no dos capturas de la misma, así que una animación pausada en un instante cualquiera sale igual a sí misma y distinta de la del otro lado. Lo que hace falta es **terminarla en un punto determinista** (`cancel()` en una animación infinita, `finish()` en una finita; `finish()` sobre una infinita lanza). El rojo de esta orden no puede depender del azar, así que se mide sobre una **página local** que trae esas fuentes, nunca contra producción, y se exige que dos cargas distintas de esa página den la misma captura.
- **D-108 (A) la medición se declara estable, o no se declara.** `e2e.mjs` captura cada zona `--repeticiones <n>` veces (**2** por defecto) y el informe lo dice por zona (`repeticiones`, `estable`). Una zona cuyas capturas repetidas no son idénticas **no** cuenta como 0 px: sale `estable: false` y el exit pasa a 2, como una zona faltante. Lo que no cambia: un `pixels > 0` con la zona estable sigue siendo **una diferencia medida**, no un error de la herramienta, y sale 0 (`e2e.mjs:263`). La decisión de salida vive en una función pura exportada, `codigoDeSalida(informe)`, para que el guard la corra en las dos direcciones sin levantar un build ni salir a la red.
- **D-109 (A, medido) el hueco `""` rompe once lectores, no uno.** Desde E2E-01 `H normalizeImageArray` **conserva** los huecos `""` en `sections.services.images`, donde el índice aparea con `services[i]`; y `T src/config/site.ts:443–457` ya los producía por su cuenta al filtrar por `visibleServices` (`allImages[idx] ?? ""`). El revisor vio tres lectores; **son dieciséis**. Medido con `renderToString`, el config por defecto (nicho `barberia`, 5 servicios) y `images[0] = images[1] = ""`, contando las `<img>` **sin atributo `src`** —React descarta `src=""` al serializar, y el navegador resuelve una `<img>` sin `src` pidiendo la página entera— y los avisos «An empty string ("") was passed to the src attribute» que React manda por `console.error`:
  - **Rompen 11**: `hero/estetica/hero-v4.tsx` (2), `services/estetica/services-v2.tsx` (3), `-v3` (2), `-v4` (2), `-v5` (1), `services/services-v2.tsx` (2), `-v3` (1), `-v4` (2), `-v5` (2), `landing/Services.tsx` (2), `services/ServicesPage.tsx` (2). Todos indexan el pool y lo pasan a `src` sin mirar si hay foto (`sectionConfig.images[index % sectionConfig.images.length]`, o el `??` de hero-v4:48–50, que no tapa un `""`).
  - **Sanos 5**: `aura/aura-services.tsx`, `services/services-v6.tsx` (`cards = ordered.filter((s) => !!imageOf(s))`: sin foto la tarjeta no se monta), `services-card-stack-tabs.tsx`, `services-treatment-card-grid.tsx` (`{image ? <img …> : <fallback/>}`) y `services/services-page-v6.tsx`.
  - **Sin el hueco los dieciséis pintan todas sus `<img>` con `src` y no avisan nada**: la diferencia es el hueco y nada más.
  Cinco de los once están **bajo el candado de la flota** (`T tools/candado.mjs:51`, `^src/components/landing/[^/]+/(estetica|aura)/`): `hero-v4` y los cuatro `services/estetica/services-v*`. B necesita `HIGIENE_PERMITIR_FLOTA=1`, que **da Liam**. Los otros seis (`services/services-v2..v5`, `Services.tsx`, `ServicesPage.tsx`) no están bajo candado pero **sirven a los seis nichos en producción**: el arreglo es un guard de verdad sobre el valor del pool, no un cambio de composición, y su blast radius lo mide la regresión de los seis (regla 2 de T).
- **D-110 (Liam, 2026-09-25) la casilla de logo sube como el resto del material, para todos los nichos.** `/api/upload-logo/[clientId]` pasa a `subirMaterial` (rol `branding`, nombre fijo `logo.<ext>` / `logo-dark.<ext>` como D-81, token = sha256 del contenido, idempotente, misma entrada → misma url que `scripts/b4-material.ts`). Hoy (`H:src/app/api/upload-logo/[clientId]/route.ts:62–68, :76, :79`) guarda en `clients/<id>/logo-light-bg.png` y `logo-dark-bg.png` con token `randomUUID()` y la extensión `.png` **fija**, aunque el archivo sea `.webp`. **Los clientes que ya tienen logo no cambian**: no hay migración ni reescritura de `config/{id}`; la subida de una variante escribe sólo su clave y rellena la otra únicamente cuando está vacía (`route.ts:112`, `:121`), y esa decisión se extrae a una función pura para poder probarla.
- **D-111 (Liam, 2026-09-25) la ficha abre con el slug.** `H:src/app/api/clients/[clientId]/route.ts:13` hace `db.collection("hub_clients").doc(clientId).get()`: con el `clientId` (el slug de la web) en vez del id del documento devuelve 404 `{ error }`, y `H:src/app/clients/[clientId]/page.tsx:187` lee `data?.client.clientId` — `data` es el objeto de error, `data.client` es `undefined`, y la página **revienta** antes de llegar a su `if (!data) return null` de `:343`. La resolución pasa a una función con dependencias inyectables (id de documento primero, campo `clientId` después) y la página, con una respuesta sin `client`, muestra un error que nombra el id.
- **D-112 (A, medido) la etiqueta «(LOGO-01)» sólo para una diferencia de convención.** `H:tests/orden/e2e-01/b.test.ts:129` emite el diagnóstico cuando el valor no coincide «por valor» aunque coincida por contenido. La url de Storage lleva el `clientId`, y el `clientId` de la web **siempre** es distinto del de la plantilla: por eso hoy sale también en `hero.video` y en `sections.services.images`, que siguen exactamente la misma convención. La copia promovida compara **normalizando el `clientId`** en la url: sólo etiqueta cuando cambia la forma de la ruta (`media/<rol>/` contra `logo-light-bg.png`) o la clase de token (sha256 contra UUID).

Por qué: E2E-01 dio 0 píxeles en las doce zonas, y su corrida en limpio dejó tres cosas que no son de diseño y que envenenan lo que viene. Una medición que da 1166 px una vez de cada tres no sirve para juzgar nada; un `??` que no tapa el hueco que el propio hub conserva rompe la web de un cliente de estética el día que le falte la foto del primer servicio; y una casilla que sube el logo por otra puerta deja el material del cliente fuera de la convención que el resto del hub ya cumple. Se arreglan antes de empezar el diseño, no después.

## A · La medición de `e2e.mjs` es estable (T)

- [A1] (T) tools/verdad/e2e.mjs exporta `asentar(page)` y, sobre una página local que trae las cuatro fuentes de azar de D-107 —una imagen que llega tarde, una imagen `loading="lazy"` bajo el pliegue, una animación de la Web Animations API y una transición CSS—, tres capturas seguidas del mismo elemento tras `asentar` —y la de una segunda carga de la misma página— son byte a byte idénticas, y sin `asentar` no lo son · fuente: revisor (tres corridas de `tests/orden/e2e-01/c.test.ts`: 1166 px en `pagina-servicios 1280` de la web A la primera, 0 px las otras dos); T:tools/verdad/e2e.mjs:159–174; D-107.
- [A2] (T) tools/verdad/e2e.mjs captura cada zona `--repeticiones <n>` veces (2 por defecto), cada entrada de `zonas` del informe lleva `repeticiones` y `estable`, y la función exportada `codigoDeSalida(informe)` da 2 cuando una zona no es estable aunque su `pixels` sea 0, 2 cuando falta una zona, y 0 cuando todas las zonas son estables · fuente: T:tools/verdad/e2e.mjs:260–263 (el exit de hoy: 2 sólo por faltantes); D-108.

## B · Ningún lector de `sections.services.images` pinta una foto que no está (T)

- [B1] (T) con `sections.services.images[0]` y `[1]` en `""` —el hueco que el hub conserva desde E2E-01— ningún componente de T que pinte esa lista emite una `<img>` sin `src` ni el aviso de React «An empty string ("") was passed to the src attribute», con una url en su lugar todos la pintan, y en `src/` no hay más lectores de la lista que los que este test monta y `src/config/site.ts` · fuente: medido (`renderToString` con el config por defecto: 11 de 16 lectores emiten `<img>` sin `src` con el hueco y ninguno sin él); H:src/app/api/config/[clientId]/route.ts (`normalizeImageArray` con huecos, E2E-01); T:src/config/site.ts:443–457; D-109.

## C · La casilla de logo sube como el resto del material (H)

- [C1] (H) src/lib/media-upload.ts exporta `nombreLogo` con los nombres fijos `logo.<ext>` y `logo-dark.<ext>` (la extensión real del archivo) y /api/upload-logo/[clientId] sube por `subirMaterial` con rol `branding`: para los mismos bytes la url es la misma que la de scripts/b4-material.ts, y la ruta ya no usa `randomUUID` ni `clients/<id>/logo-light-bg.png` · fuente: H:src/lib/media-upload.ts:58–83 (`tokenDeContenido`, `urlDeStorage`, el path por rol); H:scripts/b4-material.ts:42 (`brand.logo` y `brand.logoDark` con rol `branding`); H:src/app/api/upload-logo/[clientId]/route.ts:62–68, :76, :79; D-110, D-20, D-81.
- [C2] (H) la casilla de logo no mira el nicho —`client-config-tab.tsx` no le pasa `niche` a `LogoUploadField` y la ruta del logo no lo nombra— y un cliente que ya tiene logo no lo pierde: `actualizacionDeLogos`, exportada por src/lib/media-upload.ts, escribe sólo la clave de la variante subida y rellena la otra únicamente cuando está vacía · fuente: H:src/app/api/upload-logo/[clientId]/route.ts:106–125 (la decisión de hoy, en línea); H:src/components/client-config-tab.tsx:721; D-110.

## D · La ficha abre con el slug y no revienta (H)

- [D1] (H) src/lib/hub-clients.ts exporta `buscarClienteHub`, que con el id del documento de `hub_clients` y con el campo `clientId` (el slug) devuelve el mismo cliente y con un id que no existe devuelve `undefined`; y /api/clients/[clientId] la usa y responde 404 con `error` cuando no hay cliente · fuente: H:src/app/api/clients/[clientId]/route.ts:11–17; H:src/lib/media-upload.ts:45–53 (el patrón de dependencias inyectables, `BucketMinimo`); D-111.
- [D2] (H) src/app/clients/[clientId]/page.tsx no lee ninguna propiedad de `client` sin `?.` y muestra «Cliente no encontrado» con el id cuando la respuesta no trae `client`, en vez de reventar · fuente: H:src/app/clients/[clientId]/page.tsx:187 (`data?.client.clientId`), :343 (`if (!data) return null`, al que no se llega); D-111.

## E · Retiro y promoción de E2E-01 (D-105)

- [E1] (T+H) e2e-01 figura en tests/orden/APROBADAS.md con fecha 2026-09-25, T c4faa5d y H 0c6d3e0, y rojo-verde --todas en HEAD imprime «e2e-01 · retirada (aprobada 2026-09-25)» y no «orden e2e-01 ·» · fuente: aprobación de Liam 2026-09-25.
- [E2] (T+H) npm test corre las copias de e2e-01 que su hoja promueve, que importan ./orden/e2e-01/_comun.ts, sin volver a correr copias anteriores (la copia `d` excluye las órdenes vivas con `excluidas()`, sin nombrarlas); en T van a `test:unit` salvo que abran Chromium; y tests/orden/e2e-01/ no cambia desde su último rojo (cb540c5 en T, 4b6c373 en H) · fuente: E2E-01 (aprobada); D-105.
- [E3] (H) la copia promovida tests/e2e-01-b.test.ts corre sin salir a la red y su diagnóstico etiqueta «(LOGO-01)» `brand.logo` y `brand.logoDark`, que cambian de ruta y de clase de token, y no `hero.video` ni `sections.services.images`, donde lo único que cambia en la url es el `clientId` · fuente: H:tests/orden/e2e-01/b.test.ts:129, :145; D-112.

## F · Registro

- [F1] (T+H) CLAUDE.md § Puertas de T y de H tiene un párrafo ARREGLOS-01 que nombra «estable», «repeticiones», «hueco» y «LOGO-01»; el bloque CONTRATO-DECLARADO es idéntico al del commit aprobado de E2E-01 (T c4faa5d / H 0c6d3e0) · fuente: E1 anteriores.
- Sin test (Nichos, sin git): PLAN.md § Estado fila 4 «ARREGLOS-01-A (2026-09-25): hoja + 11 afirmaciones (T 6 tests: A1, A2, B1, E1, E2, F1; H 8: C1, C2, D1, D2, E1, E2, E3, F1), tests rojos (T <sha>, H <sha>); B pendiente del «andá»»; PLAN.md § Cómo se trabaja gana el inciso **o-bis**; `bloque-04/DISENO-REGLAS.md` gana la regla GLOBAL de los cuatro idiomas como originales; PLAN.md § bloque 5 paso 5.0 cambia «traducción asistida» por esa regla; PLAN.md anota como pendientes «crear desde plantilla A o C» (sólo peluquería) y «carga por lote». Los cuatro los escribe **A**, en este turno (Liam los aprobó el 2026-09-25).

## Interfaz de prueba (contrato entre los tests de A y el código de B)

Como en E2E-01: `node --experimental-strip-types --test`; utilidad `_comun.ts` (**copia**, no import, de `tests/orden/e2e-01/_comun.ts`, recortada a lo que esta orden usa y ampliada con lo suyo); imports de `tools/` y de `src/` **sólo** con `import()` dinámico dentro del test; ninguna afirmación cuenta órdenes (inciso m); cada afirmación en el repo cuyo árbol la pone en rojo (inciso n): A1, A2, B1 en T; C1, C2, D1, D2, E3 en H. Comparación de objetos **por valor**, con las claves ordenadas. Ningún test escribe fuera de `tests/orden/arreglos-01/`; **ninguno escribe en Firestore, en Storage ni en Vercel, y ninguno sale a la red**: A1 levanta su propia página en `127.0.0.1` y C1 sube a un bucket en memoria. Toda carpeta temporal lleva el prefijo «arreglos-01-» y se borra en `finally`. `_comun.ts`, `d` y `e` idénticos T/H por `cmp`; `f` (A) y `g` (B) sólo en T; `a` (C), `b` (D) y `c` (E3) sólo en H.

**Lo que B escribe, por afirmación:**

| afirmación | archivo | interfaz |
|---|---|---|
| A1 | `T tools/verdad/e2e.mjs` | `export async function asentar(page)` — la misma que hoy, ampliada con lo que D-107 nombra |
| A2 | `T tools/verdad/e2e.mjs` | `export function codigoDeSalida(informe)`; CLI `--repeticiones <n>`; `zonas[i].repeticiones` y `zonas[i].estable` |
| B1 | los **once** lectores de D-109 en `T src/components/` | sin export nuevo: un valor vacío del pool no llega a `src` (cinco bajo candado de flota: `HIGIENE_PERMITIR_FLOTA=1`) |
| C1 | `H src/lib/media-upload.ts` + `H src/app/api/upload-logo/[clientId]/route.ts` | `export function nombreLogo(variante: "light" \| "dark", archivo: string): string` → `logo.<ext>` / `logo-dark.<ext>` |
| C2 | `H src/lib/media-upload.ts` | `export function actualizacionDeLogos({ lightUrl, darkUrl, logoActual, logoDarkActual })` → `{ result, updates }` |
| D1 | `H src/lib/hub-clients.ts` (nuevo) + la ruta | `export async function buscarClienteHub(id, deps?)` → `{ id, data } \| undefined`; `deps.coleccion` con `doc()` y `where().limit().get()`, por defecto `db.collection("hub_clients")` |
| D2 | `H src/app/clients/[clientId]/page.tsx` | sin export nuevo: `?.` en toda la cadena y una rama de error que nombra el id |

**El recorte de las copias promovidas de E2E-01 (D-105, patrón D-89/D-95).** En T: `c` **sólo con C1** (C2 levanta un `vite build` y pide las dos webs desplegadas), `d`, `e` y `g` enteras. En H: `a` **sólo con la mitad de árbol de A1** (el registro declara las dos webs con sus campos; el documento de Firestore y la API de Vercel salen), `b` **sin Firestore y sin los GET a Storage** —el comparador se ejercita en local, con el fixture contra una copia suya con el `clientId` cambiado en cada url y con el logo en la ruta y el token viejos— y `d` y `e` enteras. `tests/orden/e2e-01/` no se toca.

## Notas para las sesiones A y B

A: al commit rojo los once tests fallan por su primera aserción — A1 y A2 por «tools/verdad/e2e.mjs no exporta …» (hoy no exporta nada: medido, `Object.keys(await import(…))` = `[]`); B1 por las `<img>` sin `src` de los once lectores; C1 por «media-upload.ts no exporta nombreLogo»; C2 por «no exporta actualizacionDeLogos»; D1 por «no existe src/lib/hub-clients.ts»; D2 por el `data?.client.clientId` de `page.tsx:187`; E1, E2, E3 y F1 como siempre. Nombre del test = frase de la hoja.

B: **los arreglos de código de producción van en commits propios, separados de tests, copias y registro.** Lo que necesita de Liam: `HIGIENE_PERMITIR_FLOTA=1` para los cinco lectores bajo candado de D-109 (`hero/estetica/hero-v4.tsx` y `services/estetica/services-v{2,3,4,5}.tsx`) — y sólo para eso; ningún otro archivo de la flota entra en esta orden. Orden sugerido: (1) A1/A2 en `e2e.mjs`; (2) B1 en los once lectores, midiendo el blast radius de los seis nichos (regla 2 de T: seis de los once no están bajo candado pero sirven a la flota en producción); (3) C1/C2 y D1/D2 en H; (4) el retiro, las copias recortadas y el registro. Lo no nombrado en esta hoja no se toca: ni fixtures, ni las dos webs, ni Firestore, ni Vercel, ni la ficha más allá de D-111. `hueco.mjs` sigue en 29/36: esta orden no mueve el contrato. La línea de APROBADAS.md de e2e-01 la escribe B (E1).

## Fuera de esta hoja

DISEÑO-01 (`features.themeToggle`, D-82). FLOTA-01 (D-88). Las otras puertas por las que entra un logo: `/api/onboarding/client-info` y el wizard pago (`src/app/onboarding/info/paid-wizard-client.tsx`, que sube con `uploadSerializedFiles` y un token), y el SVG de `/api/onboarding/route.ts:192` — esta orden arregla **la casilla del hub**, no el wizard del cliente. El `PATCH` y el `DELETE` de `/api/clients/[clientId]` y las rutas hijas siguen resolviendo por id de documento: D-111 pide que la **ficha abra**; si B ve que conviene pasarlas todas por `buscarClienteHub`, lo propone y lo decide Liam. Las webs `demo-test-e2e-peluqueria-{a,c}` de E2E-01: ningún test de esta orden las toca.
