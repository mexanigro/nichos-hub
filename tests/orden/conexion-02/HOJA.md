# HOJA · CONEXION-02 · la casilla del hero en el constructor del hub (primer hueco hecho) · 2026-09-22

Escrita por el revisor desde la línea base de CONEXION-01 (aprobada 2026-09-21, T fdf0ca2 · H 3132b22) y VERDAD-07 (aprobada 2026-09-22, T 5d81390 · H da83814). Estado esperado al abrir A: T 5d81390 = origin, H da83814 = origin.
Fuentes: `H:`/`T:` = archivos en HEAD; `revisor` = lecturas del 2026-09-22 con salida citada; `hueco` = `T tools/verdad/hueco.mjs --json` en HEAD.
Formato: cada afirmación es una línea `- [ID] (repo) frase · fuente: …`. El nombre del test es la frase exacta entre `(repo) ` y ` · fuente:`. `(T)` sólo en T; `(H)` sólo en H; `(T+H)` en los dos.
Circuito: PLAN.md § Cómo se trabaja, incisos i), j), k), l). Decisiones vigentes: D-1..D-5, D-9..D-11, D-13, D-14, D-16..D-32. Decisiones de Liam para esta hoja (2026-09-22): D-33 **convive**: `hero.video` (mp4, webm, poster, medium, portrait) es el campo de peluquería; los otros nichos siguen con `hero.videoUrl`, sin mapeo · D-34 sin Playwright en el hub (la ficha está detrás de Google owner; D-17): la casilla se prueba por render en servidor, por su lógica pura y por `hueco.mjs` · D-35 la casilla aparece sólo cuando `niche === "peluqueria"` · D-36 nombres fijos del contrato (`hero.mp4`, `hero.webm`, `hero-poster.avif`, `hero-1280.mp4`, `hero-1280.webm`, `hero-v.mp4`, `hero-v.webm`, `hero-v-poster.avif`) al rol `hero`: misma entrada, misma url que `b4-material.ts` · D-37 VERDAD-07 se promueve entera (11 T / 11 H).

Por qué: el constructor existe (ficha `/clients/[clientId]`, pestaña Config, ~20 editores) y `contratos.json` ya apunta 12 de 36 huecos a editores; las tres filas del hero (`hero.video`, `hero.video.portrait`, `hero.video.poster`) tienen contrato, validador y material en Storage y les falta la casilla; a dos les falta además que el guard nombre la clave («tests/ajustes-01.test.ts no nombra «hero.video»» / «no nombra «poster»»). Con esta orden `hueco.mjs` pasa de 2/36 a 5/36 y la promesa «el hub fabrica la pieza» se prueba a nivel de datos: lo que la casilla escribe es, byte a byte, lo que hoy tiene el tenant A.

## A · La casilla (H)

- [A1] (H) src/components/config-editors/hero-video-editor.tsx exporta `HeroVideoEditor` y dos funciones puras: `aplicarHeroVideo(config, campo, url)` escribe sólo la clave `hero.video.<campo>` (campos: `mp4`, `webm`, `poster`, `medium.mp4`, `medium.webm`, `portrait.mp4`, `portrait.webm`, `portrait.poster`; una url vacía borra la clave; ninguna otra clave de `config` cambia) y `nombreHero(campo)` devuelve el nombre fijo de D-36; aplicar las ocho urls del fixture A (T dev-fixtures/peluqueria-paleta-a.json `hero.video`) sobre un config vacío da exactamente ese `hero.video` (deepEqual) · fuente: revisor (fixture A: `hero.video` con esas ocho claves, todas urls de Storage bajo `clients/test-b4-peluqueria-a/media/hero/<nombre fijo>`); D-36.
- [A2] (H) `renderToString(<HeroVideoEditor clientId="x" niche="peluqueria" value={hero.video de A} onChange={…} />)` contiene ocho campos, cada uno con su etiqueta (Horizontal mp4/webm, Medio 1280 mp4/webm, Retrato mp4/webm/póster, Póster) y su url actual; con `niche="barberia"` devuelve cadena vacía (D-35); y src/components/client-config-tab.tsx monta `HeroVideoEditor` pasando `niche` y escribe el resultado en el mismo `config` que guarda por `/api/config/${clientId}` · fuente: H:src/components/client-config-tab.tsx:363–369 (`niche: string`), :391 y :468 (`fetch(/api/config/${clientId})`); H:src/app/clients/[clientId]/page.tsx:674–675 (`clientId`, `niche={client.niche}`); D-34.
- [A3] (H) la casilla sube por `/api/upload/${clientId}` con el campo `rol` = `hero` y el archivo renombrado a `nombreHero(campo)` (la función pura `formularioHero(archivo, campo)` devuelve un FormData con `rol` y `file` con ese nombre); acepta `video/mp4` y `video/webm` para los campos de vídeo e `image/avif` (y jpeg/png/webp) para los pósters; el path que `subirMaterial` produce para cada campo (`clients/<id>/media/hero/<nombreHero>`) es igual al path decodificado de la url de ese campo en el fixture A (ocho de ocho) · fuente: H:src/lib/media-upload.ts (CONEXION-01 A1: path `clients/<id>/media/<rol>/<nombre saneado>`); H:src/app/api/upload/[clientId]/route.ts (rol; vídeo ≤ 6 MB); revisor (paths de A decodificados: hero.mp4, hero.webm, hero-poster.avif, hero-1280.mp4, hero-1280.webm, hero-v.mp4, hero-v.webm, hero-v-poster.avif).

## B · Contrato, validador y guard

- [B1] (T) verdad/contratos.json declara `ui` en `hero.video`, `hero.video.portrait` y `hero.video.poster`: `{ ruta: "/clients/[clientId]", componente: "src/components/config-editors/hero-video-editor.tsx", campo: "hero.video" | "portrait" | "poster" }`, y bloque-04/CONTRATOS-HUECOS.md dice lo mismo en las tres filas (columna UI: «casilla del hero (CONEXION-02)»); las otras 33 filas no cambian · fuente: T:verdad/contratos.json (hoy `ui: null` en las tres); VERDAD-05 (gemelo .md ↔ .json).
- [B2] (H) `validateVariantContracts` de src/lib/config-validator.ts, además de sus avisos actuales, da **error** (no aviso) cuando un valor de `hero.video.*` está presente y no empieza por `https://` («producción no sirve rutas locales», nombra la clave); el fixture A y el C pasan sin errores; un config con `hero.video.mp4 = "/dev-fixtures/media/paleta-a/hero.mp4"` es rechazado · fuente: H:src/lib/config-validator.ts:671–676 (sólo `warn`); VERDAD-05 (material: nunca `/dev-fixtures/media`).
- [B3] (T) tests/ajustes-01.test.ts nombra literalmente «hero.video» y «poster» (los guards de las filas 1 y 3, con la clave que `contratos.json` les asigna) sin cambiar lo que afirman; `hueco.mjs --json` da las cinco casillas en «sí» y `hecho: true` en `hero.video`, `hero.video.portrait` y `hero.video.poster`, y el total es «5/36 huecos hechos» (los otros 33 no cambian de estado) · fuente: hueco (hoy: «tests/ajustes-01.test.ts no nombra «hero.video»», «no nombra «poster»»; webkit-ios ya nombra «hero-v»); T:verdad/contratos.json (guard.clave); D-11 (guards nombrados: ajustes-01).

## C · Punta a punta (T)

- [C1] (T) `recrear.mjs --paleta a --sin-firestore --paginas home --vistas 375 --puerto <libre>` y `--paleta c` no producen ninguna brecha «validador H rechaza» ni «material que producción no sirve», y las brechas totales no superan la línea base (A ≤ 1 en home 375, C ≤ 2); `b4-tenant.ts show --id test-b4-peluqueria-a` (con Firestore) devuelve en `hero.video` el mismo objeto que A1 construye desde el fixture · fuente: CONEXION-01-B (línea base: A 6 diff / C 1 sin contrato + 6 diff, 0 material; con --sin-firestore home 375: A 1); D-16 (sólo lectura del tenant `-a`; `show` no escribe).

## D · Retiro de VERDAD-07 (D-37)

- Sin test (D-38, 2026-09-22): la línea «- verdad-07 · aprobada 2026-09-22 · T 5d81390 · H da83814» de tests/orden/APROBADAS.md la escribe la sesión A en su commit rojo (en T y en H), porque A4 de verdad-07 (congelada) exige que todas las órdenes de HEAD estén retiradas y rompe el push de cualquier orden nueva mientras verdad-07 siga viva; con la línea, `--todas` la salta. Por eso no hay D1: ya no puede estar en rojo. `--orden verdad-07` explícito fallará en A4 desde ahora (declarado; misma clase que las copias `e` que fijan el bloque).
- [D2] (T+H) npm test corre las copias tests/verdad-07-a.test.ts, -b, -c, -d, -e en T y en H (11 tests cada uno), que importan ./orden/verdad-07/_util.ts, sin recorte; pasan 11/11; tests/orden/verdad-07/ no cambia desde su último rojo (b051c89 en T, 296a755 en H) · fuente: VERDAD-06 D2 (patrón).

## E · Registro

- [E1] (T+H) CLAUDE.md § Arquitectura mínima de H nombra `hero-video-editor`, «convive» y `hero.videoUrl`; CLAUDE.md § Puertas de T y de H tiene un párrafo CONEXION-02 que nombra «5/36»; el bloque CONTRATO-DECLARADO es idéntico al del commit aprobado de VERDAD-07 (T 5d81390 / H da83814) · fuente: E1 anteriores.
- Sin test (Nichos): PLAN.md § Estado fila 4 «CONEXION-02-A (2026-09-22): hoja + 9 afirmaciones (H 6 tests: A1–A3, B2, D2, E1; T 5: B1, B3, C1, D2, E1), tests rojos (T <sha>, H <sha>); verdad-07 retirada por A (D-38); B pendiente del «andá»»; PLAN.md § Cómo se trabaja, inciso m) (Liam, 2026-09-22): «**Una afirmación no cuenta órdenes ni fija el estado de otras órdenes.** Cita las que nombra por id; lo demás cambia con cada hoja y el test quedaría congelado con un número que deja de ser cierto.»; `bloque-04/CONTRATOS-HUECOS.md` § Constructor (cabecera pedida el 2026-09-21, si no está) y las tres filas; `4.3.md` § CONEXION-02 · A.

## Interfaz de prueba (contrato entre los tests de A y el código de B)

Tests con `node --experimental-strip-types --test`; los de H importan el componente con extensión (`../../../src/components/config-editors/hero-video-editor.tsx`) y `react-dom/server` (ya en H); el componente no puede importar nada que necesite Next en tiempo de carga (nada de `next/*`, `use client` sí). `aplicarHeroVideo` devuelve un config nuevo (no muta). A2: el render se compara por `includes` de las ocho urls y de las etiquetas; el «cadena vacía» de barbería es `renderToString` === "". A3: `formularioHero` se prueba con un `File` de Node (`new File([bytes], "x.mp4", { type })`); los paths se comparan decodificando `/o/<path>?` de las urls del fixture A leído desde T por ruta fija (`C:/Users/liama/Desktop/Nichos/Barber-shop-template-main`, como hueco). B2 importa el validador con extensión y comprueba `errors`/`warnings` por clave. B3 corre `hueco.mjs --json` real y `hueco.mjs --id hero.video` (exit 0). C1 corre recrear real (`--sin-firestore`, prefijo temporal «conexion-02-», ≤ 10 min) y `b4-tenant.ts show` real (sólo lectura; necesita `.env.local`: si falta, el test falla nombrándolo, no se salta). D1 como en verdad-07 (repro de HEAD en temporal + `--todas --repo`). E1 lee disco y compara el bloque por `git show`.
Ningún test escribe en T ni en H fuera de `tests/orden/conexion-02/`; ninguno escribe en Storage ni en Firestore (A3 no sube: sólo construye el FormData y calcula paths); A y B en T y H; los tests de H con `SOY`; `_util.ts` compartido idéntico por `cmp`.

## Notas para las sesiones A y B

A: al commit rojo, los nueve tests (H 6, T 5; D2 y E1 en los dos) fallan por su primera aserción (A1/A2/A3: el módulo no existe → el test debe fallar por `existsSync` del archivo antes de importar, no por error de carga; B1: `ui: null`; B2: `warn` en vez de `error`; B3: hueco 2/36; C1: `hero.video` del tenant existe pero A1 no → mismo `existsSync`; D2/E1 como siempre). D-38: A escribe además la línea de verdad-07 en tests/orden/APROBADAS.md (T y H, fuera de la carpeta de la orden). Nombre del test = frase de la hoja (A5).
B: la casilla vive en `src/components/config-editors/` como los demás editores, se monta en la pestaña Config junto a los editores del hero (`HeroObjectsEditor`, `HeroSlotPicker`) y usa `ImageUploadField` sólo si acepta vídeo sin romper a la flota (hoy `accept="image/*"`): si no, un campo propio. `layout-variants-editor.tsx` (v3 «usa hero.videoUrl») no cambia (D-33). El guard ajustes-01 se toca sólo para nombrar las claves (mensajes), no la afirmación. Nada en Storage ni Firestore (recrear con `--sin-firestore`; `show` sólo lee).

## Fuera de esta hoja

Las 21 filas restantes sin casilla (CONEXION-03 en adelante, por sección: servicios, galería, branding, paleta); mapeo `hero.videoUrl` → `hero.video` para la flota (rechazado por D-33); Playwright en el hub (D-34); los diffs de 36 px y los 1300 px (AUDITORIA-01); gama al subir (5.1).
