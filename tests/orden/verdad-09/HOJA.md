# HOJA · VERDAD-09 · el permiso abierto no pone la suite en rojo; la paridad de tools/ no bloquea una corrección de rojo; retiro de VERDAD-08 · 2026-09-22

Escrita por el revisor desde lo medido en VERDAD-08-A/A2/B (2026-09-22). Estado esperado al abrir A: T 139a2f8 = origin, H 5737d30 = origin. VERDAD-08 aprobada (2026-09-22, T 139a2f8 · H 5737d30).
Fuentes: `H:`/`T:` = archivos en HEAD; `revisor` = lecturas del 2026-09-22 con salida citada; `A`/`A2`/`B` = entregas de VERDAD-08.
Formato: cada afirmación es una línea `- [ID] (repo) frase · fuente: …`. El nombre del test es la frase exacta entre `(repo) ` y ` · fuente:`. `(T)` sólo en T; `(H)` sólo en H; `(T+H)` en los dos.
Circuito: PLAN.md § Cómo se trabaja, incisos i) a n). Decisiones vigentes: D-1..D-5, D-9..D-11, D-13, D-14, D-16..D-59. Decisiones para esta hoja (revisor, 2026-09-22, tomadas salvo que Liam diga lo contrario): D-60 ninguna copia promovida corre el candado contra el repo real: todas lo corren sobre un repo temporal (`HIGIENE_ROOT`/`HIGIENE_ROOTS`), así que `.git/permitir-tests` abierto en el repo real no cambia el resultado de `npm test`; la secuencia de A vuelve a ser «abrir, escribir, commitear, pushear, cerrar» · D-61 la paridad de `tools/` entre T y H (guard promovido `verdad-02-c`) se exige sólo en reposo: cuando alguno de los dos repos tiene una orden viva (HOJA.md en `tests/orden/<id>/` sin línea en APROBADAS.md), el guard imprime un diagnóstico «paridad de tools/ diferida: orden viva <id>» y pasa; con todas las órdenes retiradas, sigue exigiendo bytes iguales; el revisor sigue comprobando la paridad en cada corrida en limpio antes del «aprobado» · D-62 D-59 (un `--no-verify` en el rojo de A2) queda cerrado como excepción única: con D-61 no vuelve a hacer falta · D-63 VERDAD-08 se promueve entera (T a, b, c, d, e, f = 11 tests; H a, b, c, e, f = 9).

Por qué (medido): (1) con `.git/permitir-tests` = 1, el pre-commit cae 214/216 en H porque `tests/verdad-02-b.test.ts` lanza `tools/candado.mjs` del repo real (H:tests/orden/verdad-02/_util.ts:115–117: sin `HIGIENE_ROOT`) y espera veto; A tuvo que cerrar el permiso antes de commitear y se quedó sin poder restaurar T; A2 repitió la secuencia. (2) `tests/verdad-02-c.test.ts:170–180` exige `tools/_transcript.mjs` y `tools/cierre.mjs` idénticos byte a byte contra el hermano por ruta fija; cuando B pusheó el verde en H y T necesitaba un rojo nuevo (A2), ningún commit de T pasaba el pre-commit y el único camino fue un `--no-verify` autorizado (D-59). La paridad es un invariante de reposo, no de cada commit intermedio de un circuito que avanza repo por repo.

## A · Permiso abierto sin suite roja (D-60)

- [A1] (T+H) ninguna copia promovida de `tests/*.test.ts` lanza `tools/candado.mjs` contra el repo real: cada llamada (`candado(`, `candadoJson(`, o `spawnSync` con `candado.mjs`) va con `HIGIENE_ROOT` o `HIGIENE_ROOTS` en su entorno apuntando a un repo temporal; `tests/verdad-02-b.test.ts` reproduce en un temporal (`git init` + rojo con HOJA.md) los mismos cinco casos (Edit/Write/MultiEdit sobre `tests/orden/<id>/…` con HOJA en HEAD → veto; `HIGIENE_PERMITIR_TESTS=0` no abre; la variable no abre; el archivo del temporal abre; `APROBADAS.md` libre) y `tests/orden/verdad-02/` no cambia (`git diff` vacío desde su rojo) · fuente: H:tests/orden/verdad-02/_util.ts:115–117 (`candado()` sin raíz); H:tests/verdad-02-b.test.ts:16–23 (contra el repo real); revisor (verdad-03-b, 06-b, 07-b ya usan temporales); A (pre-commit 214/216 con permiso abierto); D-60. · ajuste de A (2026-09-22): la fuente citaba sólo `verdad-02-b`; medido con el test escrito, hoy tropiezan TRES copias promovidas — `tests/verdad-02-b.test.ts` (9 llamadas, sin raíz), `tests/verdad-03-a.test.ts` (`candado(tool, resolve(ROOT, "tests/orden/APROBADAS.md"))`) y `tests/verdad-04-b.test.ts` (`tools/candado.mjs` por stdin crudo con el cwd del repo real). La frase no cambia: las tres caen dentro de «cada llamada»; B las apunta a un temporal.
- [A2] (T+H) en un clon temporal del repo (prefijo «verdad-09-», `git clone` + junction a `node_modules`, `npm run prepare`) con `<clon>/.git/permitir-tests` = `1`, `node --experimental-strip-types --test tests/verdad-02-b.test.ts tests/verdad-03-b.test.ts tests/verdad-06-b.test.ts tests/verdad-07-b.test.ts` pasa entero (exit 0) y `tools/arranque.mjs` del clon imprime «PERMISO ABIERTO»; sin el archivo, pasa igual · fuente: A (con el permiso abierto: 214/216); VERDAD-07 B2 (arranque avisa); D-60.

## B · Paridad de tools/ sólo en reposo (D-61)

- [B1] (T+H) el guard promovido de paridad (`tests/verdad-02-c.test.ts`, test «tools/_transcript.mjs y tools/cierre.mjs son idénticos byte a byte en T y en H …») pasa con un diagnóstico «paridad de tools/ diferida: orden viva <id>» cuando el repo propio o el hermano tienen alguna carpeta `tests/orden/<id>/` con HOJA.md y sin línea «- <id> · aprobada» en su `tests/orden/APROBADAS.md`; y sigue fallando cuando no hay órdenes vivas y los bytes difieren; probado con `HIGIENE_ROOTS` sobre dos repos temporales: (a) sin órdenes vivas y `tools/cierre.mjs` distinto → falla nombrando el archivo; (b) igual pero con `tests/orden/viva-01/HOJA.md` sin aprobar en el hermano → pasa con el diagnóstico; (c) sin órdenes vivas y bytes iguales → pasa sin diagnóstico · fuente: H:tests/verdad-02-c.test.ts:170–180; A2 (244/245 en T con H adelante; D-59); D-61.
- [B2] (T+H) `tools/verdad/rojo-verde.mjs --todas` imprime al final «paridad de tools/: <igual | difiere: …> · órdenes vivas: <lista | ninguna>», sin cambiar su exit por eso (informativo: el revisor lo lee en el pre-push de cada entrega) · fuente: D-61 (la paridad se sigue viendo en cada push).

## C · Retiro de VERDAD-08 (D-63)

- [C1] (T+H) verdad-08 figura en tests/orden/APROBADAS.md con fecha 2026-09-22, T 139a2f8 y H 5737d30, y rojo-verde --todas en HEAD imprime «verdad-08 · retirada (aprobada 2026-09-22)» y no «orden verdad-08 ·» · fuente: aprobación de Liam 2026-09-22.
- [C2] (T+H) npm test corre las copias tests/verdad-08-a.test.ts, -b, -c, -d, -e, -f en T (11 tests) y -a, -b, -c, -e, -f en H (9 tests), que importan ./orden/verdad-08/_util.ts, sin recorte y sin volver a correr copias anteriores; la copia `a` adapta A2 al inciso m (no exige un número exacto de `_util.ts` congelados); en T van a `test:unit` (ninguna importa `playwright`); tests/orden/verdad-08/ no cambia desde su último rojo (519a892 en T, bd6e61f en H) · fuente: VERDAD-08 (aprobada); CONEXION-04-B2 (copias `d` sin recursión); D-63. · ajuste de A (2026-09-22): la frase no pedía nada sobre A2 de verdad-08, que exige `congeladas.length === 10` (`tests/orden/verdad-08/a.test.ts:64`): cada orden nueva con `_util.ts` la pone en rojo, así que promovida tal cual tumbaría `npm test` en la orden siguiente. Es el mismo caso del inciso m que D-38 arregló en verdad-07 («la copia `a` adapta A4»).

## D · Registro

- [D1] (T+H) CLAUDE.md § Puertas tiene un párrafo VERDAD-09 que nombra «permiso abierto», «repo temporal», «paridad» y «orden viva», y el bloque CONTRATO-DECLARADO es idéntico al del commit aprobado de VERDAD-08 (T 139a2f8 / H 5737d30) · fuente: D1/E1/F1 anteriores.
- Sin test (Nichos): PLAN.md § Estado fila 4 «VERDAD-09-A (2026-09-22): hoja + 7 afirmaciones (T 7 tests, H 7), tests rojos (T <sha>, H <sha>); B pendiente del «andá»»; PLAN.md § Cómo se trabaja: la secuencia de A vuelve a «abrir el permiso, escribir, commitear, pushear, cerrar el permiso» (D-60) y D-59 se anota como excepción cerrada (D-62); `4.3.md` § VERDAD-09 · A.

## Interfaz de prueba (contrato entre los tests de A y el código de B)

Tests con `node --experimental-strip-types --test`; los imports de `tools/` sólo con `import()` dinámico dentro del test (VERDAD-08 § Interfaz). A1: lectura estática de `tests/*.test.ts` (cada línea que nombre `candado.mjs`, `candado(` o `candadoJson(` debe tener `HIGIENE_ROOT`/`HIGIENE_ROOTS` en la misma llamada o en un `env` que el test construya desde un `repoTemporal`/`mkdtemp`; se comprueba por texto: el archivo que invoca al candado importa o define un repo temporal y no pasa `ROOT` real como raíz) y `git diff <rojo verdad-02> HEAD -- tests/orden/verdad-02/` vacío. A2: clon temporal como en verdad-06 A1 (junction a `node_modules`, `finally`), archivo `permitir-tests` escrito en el clon, corrida de las cuatro copias `b` con `NODE_TEST_CONTEXT` borrado; después `arranque.mjs` del clon con `HIGIENE_ROOTS=<clon>` (o el mecanismo que arranque acepte) → «PERMISO ABIERTO». B1: dos repos temporales con `tools/` copiado del real y `HIGIENE_ROOTS`; el test corre `tests/verdad-02-c.test.ts` del repo real con esos roots (`--test-name-pattern` sobre el test de paridad) tres veces; el diagnóstico se lee del TAP (`# paridad de tools/ diferida: …`). B2: `rojo-verde --todas --repo <tmp>` sobre una repro con y sin orden viva; se lee la última línea. C1 y C2 como en las hojas anteriores. D1 lee disco y compara el bloque por `git show`.
Ningún test escribe en T ni en H fuera de `tests/orden/verdad-09/`; ninguno toca Storage ni Firestore; `_comun.ts`, `a`, `b`, `c`, `d` idénticos T/H por `cmp` (todas T+H). · ajuste de A (2026-09-22): el archivo de utilidades de ESTA orden se llama `_comun.ts` y no `_util.ts`. Medido: con `tests/orden/verdad-09/_util.ts` en el árbol, A2 de verdad-08 (viva, congelada, `congeladas.length === 10`) falla en HEAD y el pre-push corta el rojo de esta orden; retirarla desde A para desbloquear pondría C1 en verde en el commit rojo, que es lo que el circuito prohíbe. El nombre distinto deja intactas las diez congeladas, no toca APROBADAS.md y mantiene los siete tests en rojo; C2 exige que B adapte la copia `a` para que el problema no vuelva.

## Notas para las sesiones A y B

A: al commit rojo, los siete tests fallan por su primera aserción (A1: `verdad-02-b` invoca el candado sin raíz temporal; A2: las cuatro copias `b` en el clon con el permiso abierto salen 1, no 0; B1: cae el caso (a); B2: sin la línea; C1: sin línea; C2: copias ausentes; D1: sin párrafo). Nombre del test = frase de la hoja (A5). · ajuste de A (2026-09-22): la nota decía que B1 caería en el caso (b); medido, el guard de hoy ignora `HIGIENE_ROOTS` (compara por la ruta fija de `tools/_git.mjs`) y en el caso (a) sale 0 en vez de fallar, así que la primera aserción que cae es la de (a). Las frases de A1 y B1 no cambian. A puede abrir el permiso y dejarlo abierto hasta después del push (pero hoy todavía no: la suite sigue roja con permiso abierto hasta que B termine; para esta orden, «abrir, escribir, cerrar, commitear» por última vez).
B: `tests/verdad-02-b.test.ts` es copia editable: se reescribe con `repoTemporal` (patrón verdad-03-b); `tests/orden/verdad-02/_util.ts` no se toca (congelado); `tests/verdad-02-c.test.ts` gana la lectura de órdenes vivas en los dos repos (APROBADAS.md de cada uno); `rojo-verde.mjs` sólo suma la línea informativa. Nada de esto cambia el candado ni el cierre.

## Fuera de esta hoja

El verde en clon neutro; CONEXION-05 (fondo y branding); gama al subir (5.1); AUDITORIA-01.
