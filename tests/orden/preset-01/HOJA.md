# HOJA · PRESET-01 · el preset de peluquería sin datos de un negocio concreto; retiro de CONEXION-08 · 2026-09-23

Escrita por el revisor desde la línea base de CONEXION-08 (aprobada por Liam 2026-09-23, T 621bfe5 · H 73d0070). Estado esperado al abrir A: T 621bfe5 = origin, H 73d0070 = origin.
Fuentes: `H:`/`T:` = archivos en HEAD; `revisor` = lecturas del 2026-09-23 con salida citada; `hueco` = `T tools/verdad/hueco.mjs --json` en HEAD.
Formato: cada afirmación es una línea `- [ID] (repo) frase · fuente: …`. El nombre del test es la frase exacta entre `(repo) ` y ` · fuente:`. `(T)` sólo en T; `(H)` sólo en H; `(T+H)` en los dos.
Circuito: PLAN.md § Cómo se trabaja, incisos i) a n). Decisiones vigentes: D-1..D-5, D-9..D-11, D-13, D-14, D-16..D-84. Decisiones para esta hoja (revisor, 2026-09-23, tomadas salvo que Liam diga lo contrario):

- **D-85 alcance.** Liam, 2026-09-23: «sí, preset antes» (antes de CONEXION-09), sobre el hallazgo de la corrida en limpio de CONEXION-08. Regla (D-79 extendida al preset): nada de lo que el nicho peluquería sirve por defecto identifica un negocio, un lugar exacto o una persona alcanzable desde afuera. Entra: teléfono, email, calle y número, código postal, razón social y nombre de marca de un negocio concreto, cuentas de Instagram (del negocio y de cada empleada), la ciudad del preset y la atribución «reseña de Google» en reseñas que nadie escribió en Google. Queda: nombres de pila de empleadas y reseñas (genéricos), fotos, catálogo, horarios y políticas. Las **plantillas A y C conservan su nombre y su ciudad** (son su diseño: A «הדר · עיצוב שיער» en Herzliya Pituach, C «נועה» en Florentin).
- **D-86 valores genéricos.** Teléfono `+972 3-000-0000` (D-80). Email `hello@example.com` (dominio reservado por RFC 2606: no es de nadie). Sin calle, número ni código postal: el preset no trae ciudad (`hero.titleSuffix`, `brand.tagline`, `brand.description`, `brand.aiPersona`, `contact.address.district` / `cityStateZip`, `business.address` y cualquier otro texto que nombre Ramat Gan quedan sin ciudad o vacíos); las plantillas A y C ganan `contact.address` con **sólo** la ciudad o el barrio de su hero, sin dígitos. Razón social y `brand.name` del preset: genéricos del oficio (p. ej. «סטודיו לשיער» / «Hair Studio»), nunca «Studio Noa». Instagram: el preset no trae ninguna url `instagram.com/…` ni `sections.instagram.handle`; las empleadas del preset, de la copia de H y de los fixtures A y C pierden su `social`. Reseñas: el `title` deja de decir Google en los cuatro idiomas (p. ej. «לקוחה» / «Client»), en el preset y en el fixture C.
- **D-87 la sección de Instagram sin cuenta.** Con el preset genérico la sección sigue mostrando sus fotos y no pinta ningún enlace a una cuenta (ni `@` vacío ni `instagram.com/` pelado). Si el componente que monta peluquería lo hiciera, B lo corrige en el componente y lo declara; es el único código de producción de la orden.
- **D-88 fuera de la orden.** Los presets de los otros siete nichos tienen el mismo patrón (revisor: emails de dominios con nombre de negocio en los ocho `*.he.ts`, cuentas de Instagram de personas en barberia, estetica, nails y tattoo, «גוגל» en las reseñas de cafeteria y remodelaciones) y sirven a la flota en producción: van en una orden propia (FLOTA-01) que decide Liam. Los tests que usan `03-612-4477` como **entrada** de una conversión (`tests/contacto-logo.test.ts:22`, `tests/verdad-05-b.test.ts:75`) y el comentario de `src/lib/whatsapp.ts:3` no son contenido servido y no cambian.
- **D-89** CONEXION-08 se promueve: T `a` (A1 entero; A2 sin el GET a Storage), `b`, `d`, `e` = 7 tests; H `c` (sólo C1: C2 lee Firestore), `d`, `e` = 4 tests. Toda copia promovida que compare los fixtures con una línea base (al menos `conexion-05-b` y la nueva `conexion-08-a`; B enumera las demás) excluye también las claves de PRESET-01, hoja por hoja, como hizo 621bfe5.

Por qué: la corrida en limpio de CONEXION-08 midió que el preset de peluquería de T (cuatro idiomas) es el de un negocio concreto: «Studio Noa», razón social, `רחוב ביאליק 24, רמת גן, 5245204`, `03-612-4477`, `hello@studionoa.co.il`, `@studionoa.hair`, tres empleadas con Instagram propio (`noa.color`, `maya.curls`, `dana.updo`) y tres reseñas firmadas como «ביקורת גוגל». El alta del hub (`H src/lib/provisioning.ts`) pisa `brand.name`, `tagline`, `description`, `phone`, `email` y `contact.address.street` (el `mergeDeep` de T:src/config/site.ts:209 sólo salta `null`/`undefined`, así que un `""` pisa), pero **no** pisa `district`, `cityStateZip`, `business.legalName` / `address`, `contact.social.instagram`, `hero.titleSuffix`, `aiPersona`, las reseñas ni `sections.instagram`, y **copia** en `config/{id}` el `staff` de `H src/lib/client-config/niche-presets.he.json`, que lleva las tres cuentas de Instagram. Un cliente real de peluquería saldría con la ciudad, la dirección parcial, la cuenta de Instagram y las reseñas «de Google» de otro negocio; y las plantillas A y C ya muestran hoy `@studionoa.hair` y las tres cuentas (sus fixtures no traen `sections.instagram.handle` y copian el `staff` con `social`).

## A · El preset de peluquería (T)

- [A1] (T) tests/preset-generico.test.ts existe, está en `test:unit` de package.json y nombra literalmente «phone», «email», «instagram» y «Google»; afirma, sobre los cuatro presets `src/config/presets/peluqueria.{he,en,ru,ar}.ts` (cargados, no leídos como texto): que `contact.phone` es `+972 3-000-0000`; que todo email que aparece en el preset termina en `@example.com`; que ningún string del preset contiene `instagram.com/` y que `sections.instagram.handle` está vacío o ausente; que ningún `testimonials[].title` contiene «גוגל», «Google», «Гугл» ni «غوغل» (sin distinguir mayúsculas); y que ningún string del preset contiene «Studio Noa», «studionoa», «נועה לשיער», «نوعا للشعر», «5245204», «ביאליק», «Bialik», «Бялик», «بياليك», «רמת גן», «Ramat Gan», «Рамат-Ган» ni «رمات غان» · fuente: revisor (grep de los cuatro presets: `phone: "03-612-4477"`, `email: "hello@studionoa.co.il"`, `instagram.com/studionoa.hair`, `noa.color`, `maya.curls`, `dana.updo`, `title: "ביקורת גוגל"`, Ramat Gan en `legalName`, `address`, `tagline`, `description`, `aiPersona`, `titleSuffix` y dos títulos de sección); D-85, D-86.
- [A2] (T) dev-fixtures/peluqueria-paleta-a.json y -c.json no tienen ninguna url `instagram.com/` en `staff[]`, ningún `testimonials[].title` con «גוגל» o «Google», y tienen `contact.address` con sólo la ciudad o el barrio de su hero (A contiene «הרצליה פיתוח», C contiene «פלורנטין») y ningún dígito; nada más cambia en los fixtures (`git diff 621bfe5 HEAD -- dev-fixtures/*.json`, comparado hoja por hoja, sólo toca `staff[].social`, `testimonials[].title` y `contact.address`) · fuente: revisor (A y C: `staff` = נועה לוי / מאיה כהן / דנה מזרחי con `social.instagram`; C: 3 reseñas con `title: "ביקורת גוגל"`; A: 8 reseñas sin título; `contact` = `{ phone }` en los dos; hero de A «בהרצליה פיתוח», de C «בפלורנטין»); D-85, D-86.

## B · La copia del hub y el tenant de prueba (H)

- [B1] (H) src/lib/client-config/niche-presets.he.json tiene en `peluqueria.staff` los mismos `id`, `name` y `schedule` que `staff` de `peluqueria.he.ts` de T (el hermano, leído del disco) y ningún miembro con `social`, y su `_provenance` nombra «PRESET-01» · fuente: H:src/lib/client-config/niche-presets.he.json (revisor: tres miembros con `social.instagram`; `_provenance` «No editar a mano: regenerar…» desde T@65507e0); H:src/lib/provisioning.ts:71 (el alta copia ese `staff` a `config/{id}`); D-86.
- [B2] (H) scripts/b4-tenant.ts no contiene «03-612-4477» ni «ביאליק», y `buildProvisionDocs` recibe `+972 3-000-0000` como teléfono por defecto (cuando el fixture no trae `contact.phone`) y en `create` · fuente: H:scripts/b4-tenant.ts:66, :88, :90 (revisor); D-80, D-86.
- [B3] (H) `scripts/b4-tenant.ts show --id test-b4-peluqueria-a` (con Firestore, sólo lectura) devuelve un `config/{id}` sin ninguna url `instagram.com/` en `staff` y con `contact.address` igual al del fixture A · fuente: D-16 (tenants `test-b4-peluqueria-*`); inciso n (la prueba «casilla = tenant» vive en H).

## D · Retiro de CONEXION-08 (D-89)

- [D1] (T+H) conexion-08 figura en tests/orden/APROBADAS.md con fecha 2026-09-23, T 621bfe5 y H 73d0070, y rojo-verde --todas en HEAD imprime «conexion-08 · retirada (aprobada 2026-09-23)» y no «orden conexion-08 ·» · fuente: aprobación de Liam 2026-09-23.
- [D2] (T+H) npm test corre las copias tests/conexion-08-a.test.ts, -b, -d, -e en T (7 tests; la `a` sin el GET a Storage) y tests/conexion-08-c.test.ts, -d, -e en H (4 tests; la `c` sólo con C1), que importan ./orden/conexion-08/_comun.ts, sin volver a correr copias anteriores (la copia `d` excluye las órdenes vivas con `excluidas()`, sin nombrarlas); en T van a `test:unit` salvo que importen `playwright`; tests/orden/conexion-08/ no cambia desde su último rojo (8e913a5 en T, 6bcad35 en H) · fuente: CONEXION-08 (aprobada); D-89.

## E · Registro

- [E1] (T+H) CLAUDE.md § Puertas de T y de H tiene un párrafo PRESET-01 que nombra «genérico», «example.com» y «FLOTA-01»; el bloque CONTRATO-DECLARADO es idéntico al del commit aprobado de CONEXION-08 (T 621bfe5 / H 73d0070) · fuente: E1 anteriores.
- Sin test (Nichos): PLAN.md § Estado fila 4 «PRESET-01-A (2026-09-23): hoja + 8 afirmaciones (T 5 tests: A1, A2, D1, D2, E1; H 6: B1, B2, B3, D1, D2, E1), tests rojos (T <sha>, H <sha>); B pendiente del «andá»»; `4.3.md` § PRESET-01 · A; FLOTA-01 anotada como pendiente de decisión de Liam (D-88).

## Interfaz de prueba (contrato entre los tests de A y el código de B)

Como en CONEXION-08: `node --experimental-strip-types --test`; utilidad `_comun.ts` (copia, no import, del cargador de `tests/orden/conexion-08/_comun.ts`); imports de `tools/` y de `src/` sólo con `import()` dinámico dentro del test; ninguna afirmación cuenta órdenes (inciso m); cada afirmación en el repo cuyo árbol la pone en rojo (inciso n): A1, A2 en T; B1, B2, B3 en H. A1: primero `existsSync` del guard (`tests/preset-generico.test.ts` no existe) → rojo por eso; después corre el guard (`node --test` anidado con `NODE_TEST_CONTEXT: undefined`; el guard lo corre la suite de T con `tsx`, así que el anidado usa el mismo runner) y exige exit 0 y los nombres de sus tests en el TAP; y mide él mismo, por su cuenta, los cuatro presets (import dinámico, recorrido de todos los strings). A2 lee los fixtures y compara con `git show 621bfe5:…` hoja por hoja. B1 lee el JSON y el preset hebreo del hermano (ruta por `ROOTS` de `tools/_git.mjs`); B3 corre `b4-tenant.ts show` real (sólo lectura). D1 y D2 como en las hojas anteriores. E1 lee disco y compara el bloque por `git show`.
Ningún test escribe en T ni en H fuera de `tests/orden/preset-01/`; ninguno escribe en Storage ni en Firestore; `_comun.ts`, `d`, `e` idénticos T/H por `cmp`; `a` sólo en T, `b` sólo en H.

## Notas para las sesiones A y B

A: al commit rojo, los tests (8 afirmaciones; T 5, H 6) fallan por su primera aserción: A1 por «no existe tests/preset-generico.test.ts»; A2 por la primera url `instagram.com/` del `staff` de A; B1 por el primer `social` de `peluqueria.staff`; B2 por «03-612-4477» en `b4-tenant.ts`; B3 por el `social` del tenant A; D1, D2, E1 como siempre. Nombre del test = frase de la hoja (A5).
B: en T, los cuatro presets de peluquería (D-86; los cuatro idiomas dicen lo mismo), los dos fixtures (D-86) y el guard nuevo en `test:unit`; en H, `niche-presets.he.json` (sólo `peluqueria.staff` y `_provenance`; los otros nichos no se tocan, D-88) y `b4-tenant.ts`. Los tenants `test-b4-peluqueria-a` y `-c` se reescriben con `recrear --paleta a|c` **con Firestore** (0 brechas de validador y de material, como en CONEXION-08). `hueco.mjs` sigue en 29/36 (ninguna fila cambia). La sección de Instagram de A y C sin cuenta (D-87): B declara qué muestra y, si pinta un enlace o un `@` vacío, lo corrige en el componente. La línea de APROBADAS.md de conexion-08 la escribe B (D1). Las copias promovidas que comparan fixtures con una línea base excluyen las claves de PRESET-01 (D-89); B las enumera en su informe.


### Ajustes de A (2026-09-23)

- **A2 · `testimonials[].title` incluye los de `translations.<lang>`.** La frase dice «ningún `testimonials[].title` con «גוגל» o «Google»»; medido, el fixture C tiene **12**: tres en la raíz (`ביקורת גוגל`) y nueve en sus traducciones (`translations.en` «Google review», `translations.ru` «Отзыв Google», `translations.ar` «تقييم Google»). Leerlo sólo en la raíz dejaría el fixture C diciendo «Google» en tres idiomas, contra D-86 («el `title` deja de decir Google en los cuatro idiomas … en el preset y en el fixture C»). El test recorre los dos sitios, y la lista de «nada más cambia» admite por eso `translations.<lang>.testimonials[].title` además de `staff[].social`, `testimonials[].title` y `contact.address`. La frase de la hoja no cambia.
- **D2 · la fase de la copia `a` se mide por Chromium, no por el literal.** La frase dice «en T van a `test:unit` salvo que importen `playwright`». La copia promovida `tests/conexion-08-a.test.ts` **no** contiene el literal `from "playwright"`: abre Chromium por `medirPng` de `./orden/conexion-08/_comun.ts`, que lo importa con `import()` dinámico (CONEXION-08 § Interfaz: «por lo que va a `test:browser` como `tests/verdad-05-c.test.ts`»). Leer sólo el literal mandaría esa copia a la fase concurrente, que es lo que D-57 evita. El test manda a `test:browser` toda copia que nombre `medirPng` o el literal, y a `test:unit` el resto, exigiéndole además que no contenga el literal.
- **D2 · sin cláusula «sin recorte».** A diferencia de las hojas anteriores, D-89 recorta las copias a propósito (la `a` de T pierde el GET a Storage, la `c` de H se queda sólo con C1), así que el test no compara el número de `test(` con el original congelado: exige los totales que fija la hoja (T 7, H 4) y que cada copia siga importando su `_comun.ts`. La frase de la hoja ya lo decía así; se anota para que no se lea como un olvido.

## Fuera de esta hoja

FLOTA-01 (D-88): los presets de barberia, estetica, tattoo, nails, cafeteria, remodelaciones y employment, que sirven a clientes en producción: decisión de Liam. CONEXION-09 (`branding.heroToBackdrop` calculado en el hub, D-83) va después de esta orden. DISEÑO-01 (`features.themeToggle`, D-82). AUDITORIA-01 (flake de navegador: en la corrida en limpio de CONEXION-08, `modo-paleta` cayó una vez en la suite completa por `page.goto` timeout con `waitUntil: "networkidle"` y pasó 4/4 aislado y 22/22 en la segunda corrida de la fase).
