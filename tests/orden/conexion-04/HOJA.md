# HOJA · CONEXION-04 · galería en el constructor del hub (cuatro huecos más: 10/36 → 14/36) · 2026-09-22

Escrita por el revisor desde la línea base de CONEXION-03 (aprobada 2026-09-22, T 4c44c0c · H 47d2908). Estado esperado al abrir A: T 4c44c0c = origin, H 47d2908 = origin.
Fuentes: `H:`/`T:` = archivos en HEAD; `revisor` = lecturas del 2026-09-22 con salida citada; `hueco` = `T tools/verdad/hueco.mjs --json` en HEAD; `CH` = bloque-04/CONTRATOS-HUECOS.md.
Formato: cada afirmación es una línea `- [ID] (repo) frase · fuente: …`. El nombre del test es la frase exacta entre `(repo) ` y ` · fuente:`. `(T)` sólo en T; `(H)` sólo en H; `(T+H)` en los dos.
Circuito: PLAN.md § Cómo se trabaja, incisos i) a n). Decisiones vigentes: D-1..D-5, D-9..D-11, D-13, D-14, D-16..D-46. Decisiones para esta hoja (revisor, 2026-09-22, tomadas salvo que Liam diga lo contrario): D-47 la galería no tiene editor en el hub (el `portfolio-editor` es otra cosa: proyectos antes/después): se crea `src/components/config-editors/gallery-editor.tsx`, sólo con `niche === "peluqueria"` (patrón D-35/D-46) · D-48 `gallery.presion` (interacción derivada) y `pagina.galeria` (ruta derivada) quedan fuera, como D-45: no tienen casilla ni validador por definición · D-49 la casilla mantiene `gallery[]` de la raíz (respaldo sin tipo que el validador y T conservan) igual a `sections.gallery.items[].src`, en el mismo orden · D-50 nombres fijos de las fotos: `galeria-<i+1>.<ext>` (jpg, webp, avif, png), rol `gallery`: misma entrada, misma url que `b4-material.ts` · D-51 `selection` es una lista de ids de `items` en el orden de `items` (marca por pieza), con 4–6 marcadas (lo que el validador ya exige) · D-52 el `alt` de una pieza vive en cuatro idiomas: `items[i].alt` (hebreo, base) y `translations.<en|ru|ar>.sections.gallery.alts[id]`.

Por qué: cuatro filas de galería tienen contrato, validador, material y guard pero no casilla: `gallery.items`, `gallery.items.alt`, `gallery.selection`, `gallery.surface` (a `surface` le falta además que el guard nombre «textura»). `gallery.variant` ya está hecha desde VERDAD-05 (selector de variantes). El contrato (CH § página `/galeria`, GALERIA-04/05) fija la forma: `items[] = { id, src, type ∈ {color, rizos, liso, recogidos, novia, cortes}, alt, serviceId }`, `selection` 4–6 ids en el orden del mapa, `surface = textura`, `gallery[]` de la raíz como respaldo, y el alt en cuatro idiomas. Con esta orden `hueco.mjs` pasa de 10/36 a 14/36.

## A · La casilla (H)

- [A1] (H) src/components/config-editors/gallery-editor.tsx exporta funciones puras que devuelven un config nuevo sin tocar otras claves: `aplicarPieza(config, i, campo, valor)` con campo `id`, `src`, `type`, `alt` (hebreo) o `serviceId` sobre `sections.gallery.items[i]` (crea la pieza si no existe; `src` también reescribe `gallery[i]` de la raíz, D-49; `serviceId` vacío borra la clave); `aplicarAltIdioma(config, id, lang, texto)` con lang `en` | `ru` | `ar` sobre `translations.<lang>.sections.gallery.alts[id]` (texto vacío borra la clave); `aplicarSeleccion(config, id, activo)` sobre `sections.gallery.selection` (ids en el orden de `items`; lista vacía borra la clave); `aplicarSurfaceGaleria(config, valor)` sobre `sections.gallery.surface`; y `nombreFotoGaleria(i, nombreArchivo)` = `galeria-<i+1>.<ext>` (D-50). Aplicadas sobre el fixture A sin `sections.gallery.{items,selection,surface}`, sin `gallery[]` y sin `translations.*.sections.gallery.alts`, con los valores de A, devuelven `sections.gallery`, `gallery` y `translations.{en,ru,ar}.sections.gallery.alts` iguales a los de A (deepEqual) · fuente: revisor (A: 6 piezas `{id, src, type, alt}` (cinco con `serviceId`: `g-rizos` no lo tiene — **ajuste de A**, 2026-09-22) con ids `g-<tipo>`, los seis tipos una vez, `src` en Storage bajo `clients/test-b4-peluqueria-a/media/gallery/galeria-<n>.jpg`; `selection` = los seis ids en orden; `surface` «textura»; `gallery[]` raíz igual a `items[].src`; `translations.{en,ru,ar}.sections.gallery.alts` con seis claves cada uno); D-49..D-52.
- [A2] (H) `renderToString(<GalleryEditor …>)` con `niche="peluqueria"` y la galería de A muestra, para la pieza abierta (la primera), los campos `id`, `type` (select con los seis tipos de GALLERY_TYPES), foto (url actual de `items[0].src`), `alt` en cuatro idiomas (etiquetas he, en, ru, ar con los cuatro textos de A) y `serviceId` (select con los ids del catálogo de A); por pieza, la marca de `selection`; y a nivel de sección el campo `surface` con valor `textura`; con `niche="barberia"` devuelve cadena vacía (D-47); y src/components/client-config-tab.tsx monta `GalleryEditor` pasando `niche` y escribe en el mismo `config` que guarda por `/api/config/${clientId}` · fuente: H:src/lib/config-validator.ts:743 (`GALLERY_TYPES`); H:src/components/client-config-tab.tsx (`/api/config/${clientId}`; pasa `niche` a los editores de peluquería desde CONEXION-02/03); D-34 (render en servidor); D-52.
- [A3] (H) la foto de una pieza sube por `/api/upload/${clientId}` con `rol` = `gallery` y el archivo renombrado a `nombreFotoGaleria(i, nombre)` (`formularioFotoGaleria(archivo, i)` devuelve el FormData; lanza si el tipo no es image/jpeg, png, webp o avif); el path que `subirMaterial` produce para cada una de las seis fotos de A (`clients/<id>/media/gallery/galeria-<n>.jpg`) es igual al path decodificado de `items[i].src` en el fixture A (seis de seis) · fuente: CONEXION-01 A1 (path `clients/<id>/media/<rol>/<nombre saneado>`); revisor (nombres de A: galeria-1.jpg … galeria-6.jpg).
- [A4] (H) `scripts/b4-tenant.ts show --id test-b4-peluqueria-a` (con Firestore, sólo lectura) devuelve un `config/{id}` cuyos `sections.gallery`, `gallery` y `translations.{en,ru,ar}.sections.gallery.alts` son iguales a los que A1 construye desde el fixture A · fuente: CONEXION-03 (A4: mismo patrón, tenant A reescrito con `recrear --paleta a` con Firestore el 2026-09-22; esta hoja no cambia los fixtures, así que no hace falta reescribirlo); D-16 (`show` no escribe); inciso n).

## B · Contrato, validador y guard

- [B1] (T) verdad/contratos.json y CH: `ui` en `gallery.items`, `gallery.items.alt`, `gallery.selection` y `gallery.surface` = `{ ruta: "/clients/[clientId]", componente: "src/components/config-editors/gallery-editor.tsx", campo: "items" | "alt" | "selection" | "surface" }`; en CH las filas de `sections.gallery.items[]`, `items[].alt`, `gallery.selection` y `sections.gallery.surface` ganan la nota «casilla de galería (CONEXION-04)»; las otras 32 filas del .json byte a byte como en 4c44c0c · fuente: T:verdad/contratos.json (hoy `ui: null` en las cuatro); CH:99, :104, :114, :115 (la fila de `items[].alt` está en :104, no en :97 — **ajuste de A**, 2026-09-22).
- [B2] (H) `validateReplanteoHuecos` da **error** (no aviso) por cada `sections.gallery.items[i].src` no vacío que no empiece por `https://` (path `sections.gallery.items[i].src`, «producción no sirve rutas locales»), y por cada `gallery[i]` de la raíz distinto de `sections.gallery.items[i].src` cuando existen los dos (path `gallery[i]`, «respaldo desincronizado», D-49); los fixtures A y C pasan sin errores; un config con `items[0].src` local es rechazado; un config con `gallery[0]` distinto de `items[0].src` es rechazado · fuente: H:src/lib/config-validator.ts:762–784 (items: id/src/type/serviceId/alt; nada sobre `https://` ni sobre el respaldo); D-49.
- [B3] (T) tests/galeria-03.test.ts nombra literalmente «textura» (el guard de `gallery.surface`, con la clave que `contratos.json` le asigna) sin cambiar lo que afirma; `hueco.mjs --json` da las cinco casillas en «sí» y `hecho: true` en `gallery.items`, `gallery.items.alt`, `gallery.selection` y `gallery.surface`, y el total es «14/36 huecos hechos»; `gallery.presion` y `pagina.galeria` siguen sin hacer (D-48) · fuente: hueco (hoy 10/36; «tests/galeria-03.test.ts no nombra «textura»»); T:verdad/contratos.json (guard.clave «textura»); D-11 (guard nombrado: galeria-03).

## D · Retiro de CONEXION-03

- [D1] (T+H) conexion-03 figura en tests/orden/APROBADAS.md con fecha 2026-09-22, T 4c44c0c y H 47d2908, y rojo-verde --todas en HEAD imprime «conexion-03 · retirada (aprobada 2026-09-22)» y no «orden conexion-03 ·» · fuente: aprobación de Liam 2026-09-22; VERDAD-03 (retiro).
- [D2] (T+H) npm test corre las copias tests/conexion-03-a.test.ts, -b, -d, -e en H (7 tests) y tests/conexion-03-b.test.ts, -c, -d, -e en T (5 tests), que importan ./orden/conexion-03/_util.ts, sin recorte (la `c` de T conserva su C1 entera: recrear sin Firestore); pasan 7/7 y 5/5; tests/orden/conexion-03/ no cambia desde su último rojo (5754669 en T, 8b3eb95 en H) · fuente: CONEXION-03 (aprobada); D-19.

## E · Registro

- [E1] (T+H) CLAUDE.md § Arquitectura mínima de H nombra `gallery-editor` y las cuatro casillas (piezas con tipo y alt ×4, selección, superficie); CLAUDE.md § Puertas de T y de H tiene un párrafo CONEXION-04 que nombra «14/36»; el bloque CONTRATO-DECLARADO es idéntico al del commit aprobado de CONEXION-03 (T 4c44c0c / H 47d2908) · fuente: E1 anteriores.
- Sin test (Nichos): PLAN.md § Estado fila 4 «CONEXION-04-A (2026-09-22): hoja + 10 afirmaciones (H 8 tests: A1–A4, B2, D1, D2, E1; T 5: B1, B3, D1, D2, E1), tests rojos (T <sha>, H <sha>); B pendiente del «andá»»; CH: las cuatro filas y § Constructor («20/36 con UI» → «24/36»); `4.3.md` § CONEXION-04 · A.

## Interfaz de prueba (contrato entre los tests de A y el código de B)

Como en CONEXION-03: `node --experimental-strip-types --test`; el cargador de `.tsx`, `@/x` e imports sin extensión se copia de `tests/orden/conexion-03/_util.ts` (no se importa); el editor se importa con extensión y no puede necesitar `next/*` al cargar. A1: `existsSync` del archivo y «exporta <función>» antes de usar cada función (rojo por «no existe» / «no exporta»); deepEqual contra A leído de T por ruta fija. A2: `renderToString` con las props que la pestaña le pasa (`config`, `clientId`, `niche`, `setConfig` no-op); comparación por `includes` de claves literales (`type`, «alt», los cuatro textos, los seis tipos, la url de la foto 1, `selection`, `surface`, «textura») como texto o `data-campo`; con barbería `=== ""`. A3: `File` de Node; paths decodificando `/o/<path>?` de las seis urls. A4: `show` real (JSON entero, `.env.local` presente; si falta, falla nombrándolo); deepEqual con lo construido en A1; rojo por «no exporta» antes de `show`. B1 lee el .json y el .md (fila por campo, no por número de línea). B2 importa el validador con extensión. B3 corre `hueco.mjs --json` real. D1 como en las hojas anteriores (repro de HEAD en temporal + `--todas --repo`); D2 lista de `npm test` + `git diff`. E1 lee disco y compara el bloque por `git show`.
Ningún test escribe en T ni en H fuera de `tests/orden/conexion-04/`; ninguno escribe en Storage ni en Firestore; `_util.ts`, `d`, `e` idénticos T/H por `cmp`; `a` sólo en H; `b` en los dos por `REPO`; no hay `c` (recrear no cambia con esta hoja: lo corren B y el revisor como verificación, no como afirmación, incisos l y n).

## Notas para las sesiones A y B

A: al commit rojo, todos los tests fallan por su primera aserción: A1–A4 por «no existe gallery-editor.tsx»; B1 por `ui: null`; B2 por `items[0].src` local sin error; B3 por «no nombra «textura»» / 10/36; D1 por la línea ausente; D2 por copias ausentes; E1 por el párrafo. Nombre del test = frase de la hoja (A5). Ninguna afirmación de T mira a H ni al revés (inciso n); A4 vive en H.
B: la casilla vive en `src/components/config-editors/gallery-editor.tsx`, se monta en la pestaña Config junto a las de hero y servicios, sólo con `niche === "peluqueria"`; escribe con las funciones puras de A1; la foto sube con `ImageUploadField` si sirve (rol `gallery`, nombre fijo). `gallery[]` de la raíz se mantiene en sincronía desde la casilla (D-49) y el validador lo vigila (B2). La línea de APROBADAS.md de conexion-03 la escribe B (D1). Recrear sin y con Firestore no hace falta (los fixtures no cambian); B cita `recrear --paleta a --sin-firestore` una vez como verificación de no regresión (A ≤ 1, 0 validador, 0 material).

## Fuera de esta hoja

`gallery.presion` y `pagina.galeria` (D-48); fondo y branding (`branding.texture`, `localPhoto`, `localPhotoMobile`, `heroToBackdrop`, `branding.mode`), `paleta`, `hero.eyebrow`, `hero.mask`, `hero.alto` (CONEXION-05 en adelante); gama al subir (5.1); AUDITORIA-01; Playwright en el hub (D-34).
