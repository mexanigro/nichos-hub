# HOJA · VERDAD-05 · «aprobado = hueco real en el hub»: contratos.json, hueco.mjs (cinco lugares), recrear.mjs (diff cero), tenant de prueba por fixture, retiro de verdad-04 · 2026-09-21

Escrita por el segundo pensamiento (sólo lectura) desde el código retirado en T 2016255 (leído con `git show`), la orden de Liam del 2026-09-20 (`orden` = C:/Users/liama/Desktop/VERDAD-01 · el método antes que el.txt) y el disco de hoy (T f202481, H 7d42172). Ninguna afirmación nace de la ejecutora.
Fuentes: `2016255:<ruta>:<líneas>` = `git show 2016255:<ruta>` en T; `H:` / `T:` = archivos en HEAD; `CONTRATOS` = `bloque-04/CONTRATOS-HUECOS.md`; `PLAN` = PLAN.md; `revisor` = lecturas del 2026-09-21 con salida citada en el chat.
Formato: cada afirmación es una línea `- [ID] (repo) frase · fuente: …`. El nombre del test es la frase exacta entre `(repo) ` y ` · fuente:`. `(T)` = test sólo en T; `(H)` = sólo en H; `(T+H)` = en los dos.
Circuito: PLAN.md:30 (inciso i) e inciso j). Decisiones vigentes: D-1..D-5 (VERDAD-02), D-9..D-11 (VERDAD-03), D-13, D-14 (VERDAD-04). Decisiones de Liam para esta hoja (2026-09-21): D-16 `recrear` puede escribir los tenants test-b4-peluqueria-a y -c en la base `default` de barbertemplate-madre (misma autorización que test-b4-peluqueria del 2026-09-17), con `archive` al cierre del bloque 4; los tests nunca escriben · D-17 «UI del hub» = ruta montada + componente que nombra la clave (estática); cada orden de CONEXION-01 trae su prueba real de UI · D-18 «material servido por producción» = URL https de Storage o archivo bajo T/public/; /dev-fixtures/media no cuenta, aunque la línea base quede baja · D-19 el e2e de recrear va dentro de los tests de orden y se recorta al promover. El push del rojo va por el hook (rojo pendiente de B).

## A · contratos.json: el gemelo legible por máquina de CONTRATOS-HUECOS.md

- [A1] (T) verdad/contratos.json existe con `{ "$comment", "huecos": [36 filas] }`, cada fila con id, seccion, ruta, tipo, clave, contrato.campo, validador (null o {archivo, funcion}), ui (null o {ruta, componente, campo?}), material.vive ∈ config|locale|public|storage y guard (null o {archivo, clave?}); los 36 ids son exactamente los de 2016255 (paleta, branding.mode, hero.video, hero.video.portrait, hero.video.poster, hero.eyebrow, hero.titular, hero.subtitle, hero.cta, hero.mask, hero.alto, contact.phone, testimonials.rating, staff.photoUrl, services.catalogo, services.priceMax, services.mode, services.images, services.featured, services.surface, pagina.servicios, gallery.items, gallery.items.alt, gallery.selection, gallery.variant, gallery.surface, gallery.presion, pagina.galeria, branding.texture, branding.localPhoto, branding.localPhotoMobile, branding.heroToBackdrop, brand.logo, brand.logoDark, navbar.variant, features.themeToggle); ningún id repetido; y cada `contrato.campo` aparece literal en CONTRATOS-HUECOS.md · fuente: 2016255:verdad/contratos.json (36 filas, campos id…guard; revisor); orden:24 (1.4: «cada hueco de CONTRATOS-HUECOS.md como fila legible por máquina»); CONTRATOS:187–197 (Reglas en código).

## B · hueco.mjs: los cinco lugares, de verdad y no por nombre

- [B1] (T) node tools/verdad/hueco.mjs [--id <id>] [--json] comprueba por fila los cinco lugares: (1) contrato = `contrato.campo` literal en CONTRATOS-HUECOS.md; (2) validador = el archivo de H existe, exporta la función (`export function|const <nombre>`) y su texto nombra la clave; (3) UI = `H/src/app/<ui.ruta>/page.tsx` existe y `H/<ui.componente>` existe y nombra `ui.campo` o la clave; (4) material = el valor de `ruta` está en el fixture A y, según `material.vive`: config|locale presente, storage = URL https, public = archivo bajo T/public/, y cualquier `/dev-fixtures/` es «producción no sirve dev-fixtures/media»; (5) guard = el archivo de T existe, está en el script `test` de package.json y nombra `guard.clave` o la clave; una fila está HECHA sólo con los cinco; exit 0 si todas las filas pedidas están hechas, 2 si no, con un detalle por cada «no» que nombra el lugar; HIGIENE_ROOTS=<T>;<H> sustituye las raíces sólo para pruebas · fuente: 2016255:tools/verdad/hueco.mjs:3–11 y :27–75; orden:17 (R-V3) y :24; H:tools/_git.mjs (ROOTS por HIGIENE_ROOTS desde VERDAD-03).
- [B2] (T) Sobre los repos reales en HEAD, hueco.mjs sale 2, imprime 36 filas y termina con «N/36 huecos hechos»; N se registra en la entrega como línea base, no como objetivo; la fila paleta tiene contrato, material y guard en «sí» y UI en «no» («sin UI en el hub (CONEXION-01)»); ninguna fila con `/dev-fixtures/media` en el fixture A tiene material en «sí» · fuente: revisor (2/36 con el código retirado, 2026-09-20: los «no» son «sin UI en el hub», «producción no sirve dev-fixtures/media», «sin guard», «sin validador declarado»); T:dev-fixtures/peluqueria-paleta-a.json (37 rutas `/dev-fixtures/media/`).
- [B3] (T) hueco.mjs --id <id inexistente> sale 2 con «sin filas»; --id paleta imprime una sola fila; --json devuelve un array con {id, seccion, checks: {contrato, validador, ui, material, guard} cada uno {ok, detalle}, hecho} · fuente: 2016255:tools/verdad/hueco.mjs:84–97.

## C · recrear.mjs: la prueba raíz de V5 («la web se borra en una hora y se recrea en minutos, en el mismo lugar»)

- [C1] (T) node tools/verdad/recrear.mjs --paleta a|c [--out <dir>] [--sin-firestore] [--solo-diff] [--paginas home,servicios,galeria] [--vistas 375,1280] [--puerto <n>]: (a) valida el fixture con validateConfig, validateVariantContracts y validateReplanteoHuecos de H (cada error es una brecha «validador H rechaza» con su hueco por valor) y lista como brecha «sin contrato» cada campo del fixture (hojas hasta profundidad 3, infraestructura excluida) sin fila en contratos.json; (b) escribe el tenant test-b4-peluqueria-<paleta> con `H scripts/b4-tenant.ts create --id <id> --fixture <json>` salvo --sin-firestore, que lo declara; (c) levanta T con server.ts en el puerto pedido, primero con VITE_TENANT_FIXTURE y después sólo con VITE_CLIENT_ID, y captura página entera de las páginas y vistas pedidas con animaciones apagadas y vídeo oculto; (d) compara píxel a píxel cada par fixture/clientId: tamaño distinto o píxeles ≠ 0 es brecha «diff ≠ 0»; toda petición a /dev-fixtures/media/ y toda petición fallida con VITE_CLIENT_ID es brecha con su hueco; escribe recrear-<paleta>.json y las capturas en <out>; exit 2 con brechas, 0 sin brechas; nunca mata procesos ajenos: si el puerto está ocupado es brecha «puerto ocupado» · fuente: 2016255:tools/verdad/recrear.mjs:3–17, :34–63, :116–172 (y :65, `kill3000` con `taskkill`, que esta hoja prohíbe); orden:11 (V5) y :25 (1.5); H:src/lib/config-validator.ts:41,655,721.
- [C2] (T) server.ts toma el puerto de la variable PORT (entero; sin ella, 3000), y una corrida de recrear.mjs con --puerto distinto de 3000 deja intacto un proceso que ya escucha en 3000 · fuente: T:server.ts:3845 (`const PORT = 3000`) y :3877; 2016255:tools/verdad/recrear.mjs:65 (mataba todo lo que escuchara en :3000).
- [C3] (H) scripts/b4-tenant.ts acepta create|archive|show --id <id> [--fixture <ruta>]: con --fixture, `create` escribe config/{id} desde ese JSON (más hub_clients demo y clients, sin token de cobro ni nextChargeAt); sin --id sigue siendo test-b4-peluqueria; un --id que no empieza por «test-b4-peluqueria» se rechaza con exit 2 antes de tocar Firestore; `show --id test-b4-peluqueria` imprime los tres documentos · fuente: H:scripts/b4-tenant.ts:1–8 y :17 (CLIENT_ID fijo; autorización de Liam 2026-09-17); 9c00a94 (diff de 37 líneas, --id/--fixture, retirado en REVERSA-01); D-16.

## D · Retiro de verdad-04

- [D1] (T+H) verdad-04 figura en tests/orden/APROBADAS.md con fecha 2026-09-21, T f202481 y H 7d42172, y la salida de rojo-verde --todas en HEAD contiene «verdad-04 · retirada (aprobada 2026-09-21)» y no contiene «orden verdad-04 ·» · fuente: chat 2026-09-21 («apruebo por mi lado también»); VERDAD-03 A1/A2.
- [D2] (T+H) npm test corre tests/verdad-04-a.test.ts, -b, -c, -d y -e (copias editables de los 9 tests, que importan ./orden/verdad-04/_util.ts; en la copia c, C1 comprueba sólo el fuente de las copias de verdad-02 y C2 sólo el repo temporal de las tres fallas, sin correr `--orden verdad-02`) y pasan 9/9; tests/orden/verdad-04/ no cambia desde su último rojo · fuente: VERDAD-04 D2 (patrón de promoción y aserción «desde su último rojo»); H:tests/orden/verdad-04/c.test.ts:25 y :86 (≈ 2 min repetidos dentro de npm test); D-10.

## E · Registro

- [E1] (T+H) CLAUDE.md § Puertas tiene un párrafo VERDAD-05 que nombra «contratos.json», «hueco.mjs», «recrear.mjs», «brecha» y «línea base»; y el bloque CONTRATO-DECLARADO no cambia · fuente: PLAN.md:23 (e: registro); H:CLAUDE.md § Puertas.
- Sin test (Nichos, sin repo): PLAN.md § Estado fila 4: «VERDAD-04 aprobada por Liam 2026-09-21 (T f202481, H 7d42172)» y las filas de VERDAD-05 (rojo y verde) con la línea base N/36; PLAN.md § Cómo se trabaja inciso k): «Aprobado = hueco real: una pieza del nicho está hecha cuando `hueco.mjs --id` da verde en los cinco lugares y `recrear.mjs` da cero brechas para A y C; hasta entonces está en construcción, aunque se vea bien.»; CONTRATOS-HUECOS.md gana una línea en la cabecera que nombra `T verdad/contratos.json` como su gemelo; `4.3.md` § VERDAD-05.

## Interfaz de prueba (contrato entre los tests de A y el código de B)
`hueco.mjs`: exporta `comprobarFila(fila, ctx)`, `contexto()` y `leerContratos()`; raíces por `tools/_git.mjs` (T y H por sufijo; `HIGIENE_ROOTS` las sustituye); CONTRATOS-HUECOS.md en `C:/Users/liama/Desktop/Nichos/bloque-04/` (`BLOQUE_DIR` de `_git.mjs`; `HIGIENE_BLOQUE=<dir>` lo sustituye sólo para pruebas). `recrear.mjs`: exporta `hojas()`, `sinContrato()`, `huecoDe()`, `diffPng()`; `--out` por defecto `C:/Users/liama/Desktop/Nichos/bloque-04/verdad/capturas/<sha del árbol de HEAD>/`; `--puerto` pasa `PORT` a server.ts; `--paginas` y `--vistas` acotan (c) y (d); con `--sin-firestore` el servidor con VITE_CLIENT_ID lee lo que haya en Firestore para ese id (o cae al preset) y lo declara en `recrear-<paleta>.json.bootstrap`. `b4-tenant.ts`: `--id` sólo con prefijo `test-b4-peluqueria`; `--fixture` = JSON de config completo. Copias de verdad-04 en `tests/verdad-04-<letra>.test.ts`; T las añade a la lista `test`.

## Fuera de esta hoja
Capturas con hash e informe por orden, retención, `impacto.mjs` (VERDAD-06); la UI del hub por hueco y la subida del material de A y C a Storage (CONEXION-01, una orden por grupo de huecos, cada una con su prueba real de UI); AUDITORIA-01.

## Notas para las sesiones A y B
A: B1/B3 se prueban con `HIGIENE_ROOTS` y `HIGIENE_BLOQUE` sobre un par temporal T/H que tenga una fila con los cinco lugares reales (→ HECHO) y una fila por cada «no» (contrato ausente, función no exportada, page.tsx ausente, valor bajo /dev-fixtures/, guard fuera de npm test); B2 corre el hueco real (rápido, sin servidores). C1 se prueba en dos partes: las funciones puras (`hojas`, `sinContrato`, `huecoDe`, `diffPng` con dos PNG conocidos) y un e2e contra la T real con `--sin-firestore --paginas home --vistas 375 --puerto <libre>`: exit 2 esperado (el material de A vive en dev-fixtures), `recrear-a.json` escrito con brechas «material que producción no sirve» y su hueco, y un `diff home 375` calculado; C2 abre un socket propio en :3000 antes del e2e y comprueba que sigue vivo después. C3 sólo con `show` (lectura) y con un `--id` rechazado; **ningún test escribe en Firestore**. Los tests que levantan servidores no corren en paralelo (inciso j). Carpetas temporales con prefijo «verdad-05-» y `finally`. Al commit rojo, todos los tests de A fallan (los archivos de A1/B/C no existen). B: `.gitignore` de T ignora `tests/*` pero no `verdad/`; comprobar `git check-ignore verdad/contratos.json` antes del commit; `server.ts` es infraestructura compartida de la flota: el cambio de C2 es una línea y se declara con `git diff`; en la copia promovida de C1 (VERDAD-06) el e2e se recorta.
