# HOJA · VERDAD-07 · el rojo se reconstruye en un clon neutro (hook), permiso de A apagable de verdad, `git config` de lectura en el clasificador, retiro de VERDAD-06 · 2026-09-21

Escrita por la sesión A desde los hallazgos de la B de VERDAD-06 y del revisor del 2026-09-21 (VERDAD-06 aprobada por Liam el 2026-09-21: T 0a9dc03, H 7ef7a8a). Ninguna afirmación nace de la ejecutora.
Fuentes: `H:` / `T:` = archivos en HEAD; `revisor` = lecturas del 2026-09-21 con salida citada en el chat; `A` = mediciones de esta sesión (entorno mínimo bajo `node --test`, `git config --show-origin core.autocrlf` en H, transcript real de esta sesión); `HOJA-0N` = `tests/orden/verdad-0N/HOJA.md`; `orden` = C:/Users/liama/Desktop/VERDAD-01 · el método antes que el.txt.
Formato: cada afirmación es una línea `- [ID] (repo) frase · fuente: …`. El nombre del test es la frase exacta entre `(repo) ` y ` · fuente:`. `(T)` sólo en T; `(H)` sólo en H; `(T+H)` en los dos.
Circuito: PLAN.md:30 (inciso i), j), k) y l). Decisiones vigentes: D-1..D-5, D-9..D-11, D-13, D-14, D-16..D-28. Decisiones del revisor para esta hoja, tomadas por Liam (2026-09-21): D-29 el rojo (y el «rojo pendiente de B») se corre en un clon neutro del commit rojo; el verde sigue en el repo (techo declarado) · D-30 el permiso de A es el archivo `.git/permitir-tests` con contenido `1`; la variable de entorno deja de abrir · D-31 `git config` sin valor ni subopción de escritura es lectura · D-32 verdad-06 se promueve entera (10 en T, 9 en H).

Por qué (hallazgos de VERDAD-06, 2026-09-21): (1) C2 estuvo «nunca en rojo» porque el worktree con el que rojo-verde reconstruye el commit rojo comparte la config local del repo, que B había cambiado; la regla «el rojo es del árbol» (PLAN.md inciso l) hoy es texto, y Liam pidió que sea hook. (2) `HIGIENE_PERMITIR_TESTS=1` entra por settings.local.json y sobrevive en toda sesión que estaba abierta cuando se dio: el revisor comprobó que su propia sesión lo tiene aunque el archivo ya no; el candado no lo frenaría. (3) `git config --show-origin core.autocrlf` (sin valor) cuenta como escritura de git en `tools/_transcript.mjs`, y el cierre bloqueó al revisor por una lectura.

## A · Rojo en clon neutro (D-29)

- [A1] (T+H) `rojo-verde --orden <id>` corre los tests del árbol rojo en un clon neutro y no en un worktree: `git clone --no-checkout <repo> <tmp>/<id>-neutro-…` + `checkout <rojo>`, con entorno mínimo (`PATH`, `SystemRoot`, `TEMP`/`TMP`, `HOME`=<tmp>; sin ninguna variable `HIGIENE_*`, `GIT_*` ni `NODE_*`), `GIT_CONFIG_NOSYSTEM=1` y `GIT_CONFIG_GLOBAL`=<tmp>/vacio (ni config de sistema, ni global, ni la local del repo), sin `npm run prepare`, con `node_modules` enlazado por junction desde el repo; la tabla dice «rojo en <sha> (clon neutro)»; el clon se borra al final y «<id> · carpetas temporales borradas: …» lo lista si quedó · fuente: T:tools/verdad/rojo-verde.mjs (worktree actual); VERDAD-06-B STOP (worktree comparte `.git/config`); revisor: `git worktree add` hereda `core.autocrlf=false` del repo.
- [A2] (T+H) sobre un repo temporal (`git init` + rojo con HOJA.md y un test que pasa sólo si `core.autocrlf` local es `false` + verde que fija eso en package.json `prepare` y lo aplica a `.git/config`), `rojo-verde --orden` sale 2 con «nunca estuvo en rojo» ANTES de D-29 (worktree hereda la config) y sale 0 DESPUÉS sólo si el test del rojo cae también por algo del árbol; el fixture con la aserción del árbol (`.gitattributes` empieza por `* -text`) sale 0 y el fixture sin ella sale 2 · fuente: VERDAD-06 C2 (dos versiones del mismo test: 5c6db0d y 16c0f1f).
- [A3] (T+H) con rojo == HEAD («rojo pendiente de B»), los tests corren en el clon neutro de HEAD, no en el repo: un test que pasa sólo si `process.env.HIGIENE_MARCA === "1"` cuenta como rojo aunque el que llama tenga `HIGIENE_MARCA=1`; y ningún archivo ignorado del repo (`.git/info/exclude` + un `marca.txt` ignorado) existe en el clon · fuente: rojo-verde.mjs:139–143 («una sola corrida, en el repo»); hallazgo (2).
- [A4] (T+H) `.githooks/pre-push` sigue corriendo `rojo-verde --todas` y `tests/contrato-hooks.test.ts` pasa sin cambios; el tiempo de `--todas` en HEAD con las seis órdenes retiradas y verdad-07 viva se imprime al final («tiempo: N s») · fuente: CLAUDE.md CONTRATO-DECLARADO; VERDAD-04 (acumula fallas).

## B · Permiso apagable (D-30)

- [B1] (T+H) candado.mjs abre `tests/orden/<id>/` (Edit/Write/MultiEdit y shells) sólo si existe `<raíz>/.git/permitir-tests` con contenido exactamente `1` (tras `trim`); con `HIGIENE_PERMITIR_TESTS=1` en el entorno y sin el archivo → veto; con el archivo y sin la variable → pasa; el archivo con `0` o vacío → veto; el veto dice «permiso: <raíz>/.git/permitir-tests ausente» · fuente: H:tools/candado.mjs (env.HIGIENE_PERMITIR_TESTS !== "1"); hallazgo (2).
- [B2] (T+H) `tools/arranque.mjs` imprime «PERMISO ABIERTO · <repo>: .git/permitir-tests existe (sólo la sesión A)» por cada repo donde exista, y nada si no existe; `tools/cierre.mjs` lo imprime como aviso (no bloquea por esto) · fuente: HIGIENE-02 (arranque y cierre revisan T y H).
- [B3] (T+H) CLAUDE.md § Puertas dice que el permiso es el archivo y que la variable ya no abre; y `tests/orden/APROBADAS.md` sigue libre sin permiso · fuente: VERDAD-02/03 (candado).

## C · Clasificador (D-31)

- [C1] (T+H) `escrituraShell` de tools/_transcript.mjs trata como lectura `git config --show-origin core.autocrlf`, `git config core.autocrlf`, `git config --get x`, `git config --show-origin --get x`, `git config --list`, `git config -l`; y como escritura `git config core.autocrlf false`, `git config --unset x`, `git config --add x y`, `git config --replace-all x y`, `git config --edit`, `git config --remove-section x`, `git config --rename-section a b`; y `tools/cierre.mjs` con un transcript real que sólo tiene `git config --show-origin core.autocrlf` clasifica «sólo lectura» · fuente: _transcript.mjs:126 (`verbo === "config" && !/(--get\S*|--list|-l)/`); D-4 (transcripts reales como fixtures); hallazgo (3).

## D · Retiro de verdad-06 (D-32)

- [D1] (T+H) verdad-06 figura en tests/orden/APROBADAS.md con fecha 2026-09-21, T 0a9dc03 y H 7ef7a8a, y rojo-verde --todas en HEAD imprime «verdad-06 · retirada (aprobada 2026-09-21)» y no «orden verdad-06 ·» · fuente: aprobación de Liam 2026-09-21.
- [D2] (T+H) npm test corre las copias tests/verdad-06-a.test.ts, -b, -c, -d, -e en T (10 tests) y -b, -c, -d, -e en H (9 tests), que importan ./orden/verdad-06/_util.ts, sin recorte (D-32); pasan 10/10 y 9/9; tests/orden/verdad-06/ no cambia desde su último rojo (16c0f1f en T, 088b32e en H) · fuente: VERDAD-06 D2 (patrón).

## E · Registro

- [E1] (T+H) CLAUDE.md § Puertas tiene un párrafo VERDAD-07 que nombra «clon neutro», «permitir-tests», «git config» y «el rojo es del árbol», y el bloque CONTRATO-DECLARADO es idéntico al del commit aprobado de VERDAD-06 (T 0a9dc03 / H 7ef7a8a) · fuente: E1 anteriores.
- Sin test (Nichos, sin repo): PLAN.md § Estado fila 4 «VERDAD-07-A (2026-09-21): hoja + 11 afirmaciones, tests rojos (T <sha>, H <sha>); B pendiente del «andá»»; inciso l) gana al final «Lo hace cumplir rojo-verde desde VERDAD-07: el rojo se corre en un clon neutro»; `4.3.md` § VERDAD-07 · A.

## Interfaz de prueba (contrato entre los tests de A y el código de B)
A1–A3 por caja negra (`node tools/verdad/rojo-verde.mjs --orden <id> --repo <tmp>`) sobre repos temporales con prefijo «verdad-07-» en os.tmpdir() y nombre real (Nichos-hub / Barber-shop-template-main), con un `node_modules/verdad-07-marca/` propio en el repo temporal (carpeta normal, ignorada por su .gitignore) que el clon tiene que ver por el junction; los fixtures sólo importan módulos de node, así que no hace falta el `node_modules` real. El test del fixture escribe una línea JSON en `<base>/registro.jsonl` (ruta absoluta fuera del repo temporal, fija en su fuente) en cada corrida: `raiz` (import.meta.dirname/../../..), `cwd`, `env` (process.env entero), `gitEsCarpeta` (`.git` es carpeta: clon; en un worktree es un archivo), `worktrees` (`git -C <repo temporal> worktree list --porcelain`), `worktreesDir` (existe `<repo temporal>/.git/worktrees`), `configLocal` (`git config --local --list` del árbol donde corre), `enlace` (`node_modules` es junction y tiene `verdad-07-marca/`), `verde` (existe `verde.txt`), `marca` (existe `marca.txt`). La corrida del rojo es la entrada con `verde: false` (A1) o la única (A3).
A1 exige de esa entrada: `raiz` bajo `<os.tmpdir()>/<id>-neutro-` («<tmp>» = os.tmpdir() del que llama; cualquier subcarpeta vale); `gitEsCarpeta` true; `worktrees` con una sola línea `worktree ` y `worktreesDir` false («no existe <tmp>/.git/worktrees», `git worktree list` → sólo el principal); `env` con `PATH`, `SystemRoot`, `HOME` y `TEMP` o `TMP`, con `HOME` dentro de os.tmpdir(), `GIT_CONFIG_NOSYSTEM` = «1», `GIT_CONFIG_GLOBAL` dentro de os.tmpdir() y terminado en «vacio», y ninguna clave que empiece por `HIGIENE_`, `GIT_` (salvo esas dos) o `NODE_` salvo `NODE_TEST_CONTEXT`, que el propio runner de node pone en sus hijos (A midió: bajo `node --test` con entorno {PATH, SystemRoot, TEMP, TMP, HOME, GIT_CONFIG_*} el hijo ve además NODE_TEST_CONTEXT y las que Windows añade sola: HOMEDRIVE, HOMEPATH, LOGONSERVER, SYSTEMDRIVE, USERDOMAIN, USERNAME, USERPROFILE, WINDIR; todas se toleran); `configLocal` sin `core.hookspath` ni `core.autocrlf` (sin `npm run prepare`: el package.json del fixture lleva un `prepare` que los fijaría); `enlace` true. Además la tabla lleva `rojo en <sha7> (clon neutro)` y al terminar no queda en os.tmpdir() ninguna carpeta `<id>-neutro-…` (si stderr dice «<id> · carpetas temporales borradas: …», también vale: lo importante es que no quede).
A2 «antes/después de D-29» no se simula: el test sólo exige el comportamiento nuevo (fixture con `* -text` → 0 y tabla con «(clon neutro)»; sin él → 2 con «nunca estuvo en rojo»). El test del fixture lee `git config --get core.autocrlf` (git de la máquina, sin -c, dentro del árbol donde corre) y pasa si vale `false` o está sin fijar (el valor por defecto de git, que es lo que un clon neutro ve); el fixture «con árbol» exige además que `.gitattributes` empiece por `* -text`. El verde de cada fixture añade package.json con `prepare: git config core.autocrlf false` (y `.gitattributes` en el «con árbol») y `git config core.autocrlf false` queda en `.git/config` del repo temporal. A3: el fixture pasa sólo si `process.env.HIGIENE_MARCA === "1"`; el test lo comprueba primero en el repo temporal con la variable (control: pasa) y después llama a rojo-verde con `HIGIENE_MARCA=1` en su entorno; `.git/info/exclude` lleva `marca.txt` y el archivo existe en el repo temporal. A4: `--todas` se mide sobre una reproducción de HEAD en un repo temporal (tests/orden/APROBADAS.md y tests/orden/<id>/ de todas las órdenes de HEAD menos verdad-07, que se correría a sí misma sin fin); «verdad-07 viva» = tests/orden/verdad-07/HOJA.md existe en disco y APROBADAS.md no la lista; «tiempo: N s» lo imprime el test como diagnóstico TAP («# tiempo: N s»), no rojo-verde (su stdout sigue siendo determinista, VERDAD-02 A6).
B1 por `candadoJson` (VERDAD-06 `_util`) con `HIGIENE_ROOT` sobre un repo temporal con vieja-01 (rojo en main) y nueva-99 (sin rojo): `.git/permitir-tests` se escribe en el `.git` del repo temporal; «contenido exactamente `1` (tras trim)» = «1\n» abre, «0\n» y «» no; el veto sin archivo dice `permiso: <raíz>/.git/permitir-tests ausente` (el test acepta `/` o `\` como separador y exige la raíz temporal en la línea); con «0» o vacío basta el veto «CANDADO · …». B2 corriendo `arranque.mjs` y `cierre.mjs` con `HIGIENE_ROOTS`/`HIGIENE_PLAN` sobre un par temporal limpio (cierre con un transcript de sólo lectura): «<repo>» en la línea `PERMISO ABIERTO · <repo>: .git/permitir-tests existe (sólo la sesión A)` es la etiqueta (T/H) o la ruta de la raíz; el test exige la línea entera con una de las dos; sin archivo, ninguna línea con «PERMISO ABIERTO»; cierre la imprime en stderr y sale 0 (par limpio). B3 lee `seccionPuertas()`: nombra `.git/permitir-tests` y, en una misma línea, `HIGIENE_PERMITIR_TESTS` con «no abre» (cubre «ya no abre» / «deja de abrir» → se pide «no abre» literal); APROBADAS.md: Write y `sed -i` sobre `tests/orden/APROBADAS.md` sin archivo ni variable → 0.
C1 importando `escrituraShell` de tools/_transcript.mjs (import directo desde el test, con cwd = raíz del repo y raíces [ROOT, una hermana inexistente]): lectura = `""`, escritura = el segmento; y, para el cierre, con un transcript real de esta sesión (línea 131 del JSONL de la sesión A, 2026-09-21T21:18:15Z: un solo `tool_use` Bash `git config --show-origin core.autocrlf` con cwd en H; D-4) con las raíces reales reubicadas al par temporal como en verdad-02/03, sobre un par con un archivo sucio en T (para que haya faltas y el cierre llegue a clasificar): exit 0 y «CIERRE AVISO … sólo leyó». D1 como en verdad-06 (repro de HEAD en temporal sin verdad-07 + `--todas --repo`), con verdad-06 aprobada T 0a9dc03 · H 7ef7a8a. D2 «sin recorte» = cada copia tiene tantos `test(` como su original en tests/orden/verdad-06/; las copias se corren con `node --experimental-strip-types --test` (T 10, H 9). E1: el bloque CONTRATO-DECLARADO se compara línea a línea (sin CR) con el de T 0a9dc03 / H 7ef7a8a por `git show`.
Ningún test escribe en T ni en H fuera de `tests/orden/verdad-07/`; `finally` y prefijo «verdad-07-» en todo temporal; A1/A2/A3/A4 en un mismo archivo, en secuencia (inciso j); rojo-verde se lanza con `correrLargo` (10 min) porque clona.

## Notas para las sesiones A y B
A: al commit rojo, los once tests fallan por su primera aserción (A1/A2: sin «(clon neutro)» en la tabla; A3: «nunca estuvo en rojo» porque hoy corre en el repo con la variable; A4: verdad-06 sin retirar → cinco «retirada», no seis; B1: la variable abre; B2: sin «PERMISO ABIERTO»; B3: sin «permitir-tests»; C1: `git config --show-origin core.autocrlf` clasificado escritura; D1: sin línea en APROBADAS.md; D2: sin copias; E1: sin párrafo).
B: el `.git/permitir-tests` lo crea y borra Liam con los scripts nuevos que el revisor le da tras la B (`Set-Content .git\permitir-tests 1` / `Remove-Item`); el `rojo-verde` nuevo debe seguir borrando restos y acumulando fallas (VERDAD-04); el verde en el repo se declara como techo (los tests que necesitan `.env.local` o al hermano al lado siguen corriendo en la máquina real). En el clon neutro el runner es `node --experimental-strip-types --test --test-reporter=tap` con cwd = clon; los tests de una orden ven `ROOT` = clon (import.meta.dirname/../../..) y al `node_modules` real por el junction.

## Fuera de esta hoja
El candado sobre `tests/orden/` del hermano (una sesión en H no vigila T); el verde en clon neutro; renormalizar blobs; CONEXION-02.
