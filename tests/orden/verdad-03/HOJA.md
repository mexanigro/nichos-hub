# HOJA · VERDAD-03 · retiro de órdenes aprobadas, rojo pendiente de B, corrección del rojo, clasificador sin falsos positivos, limpieza y promoción, contrato-hooks completo · 2026-09-20

Escrita por el segundo pensamiento (sólo lectura) desde el registro y desde su corrida en limpio de VERDAD-02 (aprobada por Liam el 2026-09-20: T 0a13c2e, H 82e33f5). Ninguna afirmación nace de la ejecutora.
Fuentes: `H:` / `T:` = archivos en 82e33f5 / 0a13c2e; `HOJA-02` = `tests/orden/verdad-02/HOJA.md`; `chat 2026-09-20` = frases literales de Liam de hoy; `revisor` = hallazgos de la corrida en limpio de VERDAD-02, con salida citada en el chat.
Formato: cada afirmación es una línea `- [ID] (repo) frase · fuente: …`. El nombre del test es la frase exacta entre `(repo) ` y ` · fuente:`. `T+H` = el mismo test en los dos repos.
Circuito: PLAN.md:30 (inciso i). Decisiones vigentes de VERDAD-02: D-1 `HIGIENE_PERMITIR_TESTS=1` · D-2 rojo-verde en pre-push · D-4 fixtures = transcripts reales, sólo líneas con tool_use · D-5 dos sesiones por orden más la corrida en limpio del revisor. Decisiones de Liam para esta hoja (2026-09-20): D-9 variables sin valor en rutas se emulan como bash (vacío) · D-10 se promueven los 18 tests de verdad-02 a npm test · D-11 B modifica `tests/contrato-hooks.test.ts` bajo el test rojo de A · D-12 el push del rojo de esta orden va con `--no-verify` por única vez, con lo que el hook habría corrido citado a mano.

## A · Retiro de órdenes aprobadas y rojo pendiente de B

- [A1] (T+H) tests/orden/APROBADAS.md lista las órdenes aprobadas por Liam, una por línea «- <id> · aprobada <fecha> · T <sha7> · H <sha7>»; rojo-verde --todas no corre las listadas y por cada una imprime una sola línea «<id> · retirada (aprobada <fecha>)» en vez de su tabla; --orden <id> explícito las sigue verificando; y candado.mjs deja escribir APROBADAS.md sin la variable · fuente: H:tools/verdad/rojo-verde.mjs:110 («--todas» = toda carpeta de tests/orden/); H:tools/candado.mjs:33–37 (la regla mira «tests/orden/<id>/»); revisor hallazgo 5 (≈ 60 s por repo y por push, para siempre); chat 2026-09-20 («Aprobado»).
- [A2] (T+H) verdad-02 figura en APROBADAS.md con fecha 2026-09-20, T 0a13c2e y H 82e33f5, y la salida de rojo-verde --todas en HEAD contiene «verdad-02 · retirada» y no contiene «orden verdad-02 ·» · fuente: chat 2026-09-20 («Aprobado»); revisor (tabla de 18 filas en cada push).
- [A3] (T+H) Una orden cuyo commit rojo es HEAD está «rojo pendiente de B»: rojo-verde --todas la acepta con exit 0 e imprime «<id> · rojo pendiente de B» si todos sus tests fallan en HEAD y sus nombres coinciden con la hoja (A5 de VERDAD-02); si alguno pasa, exit 2 con «<nombre>: nunca estuvo en rojo»; una orden cuyo rojo no es HEAD se verifica entera como hasta ahora · fuente: HOJA-02:13 (A3 exige verde en HEAD) + HOJA-02:17 (A7, pre-push) → un commit rojo no se puede pushear (defecto de la hoja VERDAD-02, del revisor; D-12); PLAN.md:30 (el circuito empieza por el rojo).

## B · Corrección del rojo (camino de A después del commit rojo)

- [B1] (T+H) Rojo = el ÚLTIMO commit de main que añade tests/orden/<id>/HOJA.md; cuando hubo más de uno, la tabla de rojo-verde termina con una línea «rojos anteriores: <sha7> …» (los reverts quedan a la vista); con un solo rojo la salida no cambia · fuente: H:tools/verdad/rojo-verde.mjs:4 y :81 («PRIMER»); HOJA-02:11 (A1); revisor hallazgo 6 (un segundo commit de A dejaba la orden imposible de cerrar por A4).
- [B2] (T+H) candado.mjs corta tests/orden/<id>/… también cuando HOJA.md de esa orden estuvo alguna vez en la historia de main aunque hoy no esté en HEAD (revert incluido), salvo HIGIENE_PERMITIR_TESTS=1; una orden nunca commiteada sigue libre; HIGIENE_ROOT=<ruta> sustituye la raíz sólo para pruebas · fuente: H:tools/candado.mjs:29 (`hojaEnHead`: sólo HEAD); HOJA-02:22 (B2, `zz-sin-commit`); revisor hallazgo 6.
- Sin test (Nichos): PLAN.md inciso i) gana: «Corrección de A tras el rojo: revert del commit rojo + nuevo commit rojo con HIGIENE_PERMITIR_TESTS=1 dado por Liam; rojo-verde cita los rojos anteriores.»

## C · Clasificador (cierre por transcript) sin falsos positivos ni huecos conocidos

- [C1] (T+H) Las variables $X, ${X} y %X% dentro de rutas y destinos se resuelven primero con las asignaciones «X=valor» del mismo comando, después con el entorno del hook, y si no existen valen vacío como en bash; cuenta como escritura sólo lo que resuelve dentro de T/H. Casos, con cwd de la sesión en H: S=/tmp/x; mkdir -p "$S" → lectura · S=src; mkdir -p "$S" → escritura · node x.mjs > "$S/a.txt" con S=/tmp/x → lectura · rm -rf "$D" con D=/tmp/y → lectura · node x.mjs > "$NOEXISTE/a.txt" → lectura · mkdir -p "$NOEXISTE" → lectura · echo x > "$S/a.txt" con S=. → escritura · fuente: H:tools/_transcript.mjs:29 y :51–55 (una cadena con «$» no es ruta, se vuelve «Q» y «Q» resuelve dentro del cwd); revisor hallazgo 1 (7 comandos del revisor marcados, todos con «"$VAR/…"»); D-9.
- [C2] (T+H) Una comilla sin cerrar no oculta lo que sigue: quitadas las cadenas balanceadas, una comilla suelta se descarta y el resto del comando se analiza como fuera de comillas. Casos con cwd en H: cat <<EOF … don't … EOF; git commit -m 'x' → escritura · cat <<EOF … don't … EOF; git status → lectura · echo "it's" > /dev/null; git status → lectura; y el fixture de escritura de VERDAD-02 (tests/orden/verdad-02/fixtures/transcript-escritura.jsonl, reubicado a repos temporales) sigue clasificando como escribió con la misma primera escritura, Write tools/_transcript.mjs · fuente: H:tools/_transcript.mjs:12–13 (techo declarado por la ejecutora: la comilla suelta empareja con la siguiente y esconde lo del medio); revisor hallazgo 2; HOJA-02 C2; T:CLAUDE.md:69 («las dos direcciones»).
- [C3] (T+H) El transcript real del revisor de hoy (sesión a616bd1a-e396-45e0-a96c-67d58fe5222b, proyecto H; sólo líneas con tool_use; ≥ 51 tool_use, 0 Edit/Write/MultiEdit/NotebookEdit, 0 Agent/Task/Workflow), reubicado a repos temporales, clasifica como sólo lectura, y con T sucio cierre.mjs sale 0 con aviso. Fixture: tests/orden/verdad-03/fixtures/transcript-revisor.jsonl · fuente: revisor hallazgo 1 (hoy clasifica «escribió», 7 escrituras); chat 2026-09-20 (Liam: el clasificador «vuelve a bloquear al revisor»).

## D · Tests de orden: limpieza y promoción

- [D1] (T+H) rojo-verde mide el directorio temporal del sistema antes y después de correr los tests de una orden en HEAD; si quedan carpetas nuevas cuyo nombre empieza por «<id>-», sale 2 con «<id>: carpetas temporales sin borrar: …»; sin restos, nada cambia · fuente: revisor hallazgo 3 (232 carpetas `verdad-02-*` en %TEMP%; H:tests/orden/verdad-02/a.test.ts:31,50,61,75: `borrar()` sólo al final del camino feliz, 0 `finally`/`after` en los cuatro archivos).
- [D2] (T+H) npm test corre tests/verdad-02-a.test.ts, tests/verdad-02-b.test.ts, tests/verdad-02-c.test.ts y tests/verdad-02-e.test.ts (copias editables de los 18 tests de la orden, que importan ../orden/verdad-02/_util.ts; A1 adaptado a «último commit que añade» y a los rojos anteriores) y pasan; tests/orden/verdad-02/ no cambia · fuente: revisor hallazgo 5 (retirada = deja de correr en --todas; lo que deba seguir vigilando se promueve a suite normal, editable, fuera de tests/orden); H:package.json:11 (glob `tests/*.test.ts`); T:package.json:22 (lista explícita); T:.gitignore:38–40; D-10.

## E · contrato-hooks compara lo que ejecutan los hooks de git

- [E1] (T+H) El bloque CONTRATO-DECLARADO tiene las claves «githook.pre-commit» y «githook.pre-push» con los scripts node que cada hook ejecuta, en orden y con sus argumentos (pre-push: «destino-no-despliega.mjs rojo-verde.mjs --todas»); tests/contrato-hooks.test.ts las compara contra .githooks/ en las dos direcciones; la línea de comentario «# pre-push :: …» de VERDAD-02 desaparece; y en una copia del repo donde pre-push pierde la línea de rojo-verde, contrato-hooks sale rojo · fuente: H:tests/contrato-hooks.test.ts:17 (salta las líneas «#»), :32 (githooks = nombres de archivo), :39 (deepEqual); H:.githooks/pre-push:9,11; revisor hallazgo 4; D-11.

## F · Registro

- [F1] (T+H) CLAUDE.md § Puertas tiene un párrafo VERDAD-03 que nombra APROBADAS.md, «rojo pendiente de B», «último commit que añade», la corrección por revert, las variables de entorno en rutas y la promoción a npm test; y el párrafo VERDAD-02 ya no dice «el primero que añade» · fuente: H:CLAUDE.md:52; PLAN.md:23 (e: registro).
- Sin test (Nichos, sin repo): PLAN.md § Estado fila 4 con la aprobación de VERDAD-02 y las filas de VERDAD-03 (rojo y verde); inciso i) ampliado (§ B); `4.3.md` § VERDAD-03 con D-12 y las tres salidas citadas. Tarea de B sin test: borrar las carpetas `verdad-02-*` de %TEMP% (fuera de los repos).

## Interfaz de prueba (contrato entre los tests de A y el código de B)
`tests/orden/APROBADAS.md`: líneas «- <id> · aprobada AAAA-MM-DD · T <sha7> · H <sha7>»; rojo-verde la lee en HEAD. Rojo = `git log --diff-filter=A main -- tests/orden/<id>/HOJA.md`, el más reciente; anteriores = el resto, en la línea «rojos anteriores: …» al final de la tabla; rojo == HEAD → «<id> · rojo pendiente de B». Carpetas temporales de los tests de una orden: prefijo «<id>-» en `os.tmpdir()`; rojo-verde compara el listado antes/después de la corrida en HEAD. candado: `HIGIENE_ROOT=<ruta>` sustituye ROOT; historia = `git log main --diff-filter=A -- tests/orden/<id>/HOJA.md` no vacío. cierre/_transcript: `HIGIENE_ROOTS`, `HIGIENE_PLAN`, `HIGIENE_TRANSCRIPT` como en VERDAD-02; C1/C2 con transcripts en línea y cwd en el H temporal. contrato-hooks: claves «githook.<hook>» = basenames de los `node tools/…mjs` del hook en orden, con argumentos.

## Fuera de esta hoja
Retención de capturas e informe con hash (primera hoja de diseño), hueco.mjs, recrear.mjs, contratos.json, impacto.mjs; la auditoría de lo ya hecho (AUDITORIA-01) va después de esta orden, bajo el mismo circuito.

## Notas para las sesiones A y B
A: carpeta temporal de cada test borrada en `finally`; C3 se extrae del transcript real, sólo líneas con tool_use, y se reubica con la misma técnica que VERDAD-02; C2 reutiliza por ruta el fixture de escritura de VERDAD-02. B: verdad-02 sigue activa hasta que B la retire (A2), y B1 contradice el test A1 de verdad-02: APROBADAS.md, B1 y las copias de D2 (con su A1 adaptado) van en el mismo commit, y el pre-push corre el rojo-verde nuevo del árbol de trabajo. Al commit rojo, los 12 tests de A fallan.
