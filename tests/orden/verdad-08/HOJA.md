# HOJA · VERDAD-08 · restos por proceso, candado sin falsos positivos y sobre el hermano, `cp` como lectura, navegador en serie; retiro de CONEXION-04 · 2026-09-22

Escrita por el revisor desde lo medido en CONEXION-02..04 (2026-09-21/22). Estado esperado al abrir A: T 1398730 = origin, H 4673f8c = origin. CONEXION-04 aprobada (2026-09-22, T 4a22d7b · H f8bd9bb; corrección B2 T 1398730 · H 4673f8c).
Fuentes: `H:`/`T:` = archivos en HEAD; `revisor` = lecturas del 2026-09-22 con salida citada (candado por stdin desde un script fuera del repo; clasificador importado); `B` = entregas de CONEXION-03-B, 04-B y 04-B2.
Formato: cada afirmación es una línea `- [ID] (repo) frase · fuente: …`. El nombre del test es la frase exacta entre `(repo) ` y ` · fuente:`. `(T)` sólo en T; `(H)` sólo en H; `(T+H)` en los dos.
Circuito: PLAN.md § Cómo se trabaja, incisos i) a n). Decisiones vigentes: D-1..D-5, D-9..D-11, D-13, D-14, D-16..D-52. Decisiones para esta hoja (revisor, 2026-09-22, tomadas salvo que Liam diga lo contrario): D-53 `rojo-verde` da a cada corrida de tests un directorio temporal propio (`TEMP`/`TMP`/`TMPDIR` = `<tmpdir>/<id>-neutro-<azar>/tmp` para el rojo y `<tmpdir>/<id>-verde-<azar>/tmp` para el verde; `os.tmpdir()` los obedece, así que los `_util.ts` congelados no cambian) y los restos son lo que queda en ese directorio, no un barrido de `<tmpdir>` por prefijo · D-54 el candado vigila `tests/orden/<id>/` de los dos repos (`ROOTS = [propio, hermano]` de `tools/_git.mjs`), para Edit/Write/MultiEdit y shells · D-55 en `segmentos()` una cadena entre comillas es un solo token aunque contenga `|`, `;`, `&&` o comillas escapadas · D-56 `cp` escribe sólo en su último argumento; `mv` escribe en los dos; `rm`, `mkdir`, `touch`, `tee` escriben en todos (candado y clasificador, misma regla) · D-57 la suite de T corre en dos fases: unitaria con `--test-concurrency=2` y navegador (los archivos que importan `playwright`) con `--test-concurrency=1`; un guard impide que un archivo con navegador quede en la fase concurrente · D-58 CONEXION-04 se promueve entera (H a, b, d, e = 8; T b, d, e = 5).

Por qué (medido): (1) la suite de H cayó 214/216 dos veces porque `rojo-verde` cuenta como restos las carpetas `<id>-…` que otro proceso de la misma suite está usando (`readdirSync(tmpdir())` antes/después, prefijo global; H:tools/verdad/rojo-verde.mjs:97–100); B2 cortó la recursión de las copias, pero el barrido sigue siendo global. (2) El candado sobre shells vetó lecturas del revisor: `grep -n -E "a|b" <orden>/HOJA.md` y `cp <orden>/HOJA.md C:/tmp/x` salen 2 (revisor, seis casos por stdin). (3) El clasificador del cierre marcó como escritura `cp <archivo de T> <carpeta fuera>` (bloqueó al revisor dos días). (4) El candado sólo mira `ROOT` (H:tools/candado.mjs:37): una sesión abierta en H puede escribir `tests/orden/<id>/` de T por ruta absoluta sin veto (B, VERDAD-06). (5) La suite de T corre diez archivos con Playwright bajo `--test-concurrency=2`: cuatro reintentos de pre-commit en dos días por timeouts distintos cada vez (`galeria-05`, `services-v6`, `ajustes-01`, `hero-viewport`, `lang-01`, `modo-paleta`).

## A · Restos por proceso (D-53)

- [A1] (T+H) `rojo-verde --orden <id>` corre los tests del rojo y del verde con `TEMP`, `TMP` y `TMPDIR` apuntando a un directorio temporal propio de esa corrida (`<tmpdir>/<id>-neutro-<azar>/tmp` y `<tmpdir>/<id>-verde-<azar>/tmp`, creados vacíos), y los «restos» son las entradas que quedan en ese directorio al terminar (se listan, se borran y se declaran «<id> · carpetas temporales borradas: …»; las del verde son falla D1); nunca lee `<tmpdir>` entero: sobre un repo temporal con una orden cuyo test verde crea y no borra `<tmp>/<id>-marca-…`, sale 2 nombrando esa carpeta, y las dos carpetas `<id>-ajena-…` que ese mismo test crea en `<tmpdir>` real DURANTE la corrida (otro proceso en paralelo) siguen existiendo al terminar, sin listarse ni borrarse · fuente: H:tools/verdad/rojo-verde.mjs:97–100 (`readdirSync(tmpdir())` antes/después, `startsWith(\`${id}-\`)`); B2 (salida «conexion-04: carpetas temporales sin borrar: conexion-04-2M2pfK …» con otro proceso vivo); D-53. · ajuste de A (2026-09-22): la frase original creaba las dos `<id>-ajena-…` «antes de la corrida»; medido, así ya sobreviven hoy (entran en el `antes` de `correr`, rojo-verde.mjs:97) y la afirmación nacía verde. Se crean DURANTE la corrida, que es el caso que B2 midió (otro proceso de la misma suite) y el único que distingue el barrido global de `<tmpdir>` del directorio propio de la corrida.
- [A2] (T+H) `os.tmpdir()` dentro de un test corrido por `rojo-verde` devuelve el directorio propio de esa corrida (el fixture escribe `os.tmpdir()` a un archivo y el test lo lee: está bajo `<id>-neutro-` o `<id>-verde-`), y los diez `_util.ts` congelados de `tests/orden/*/` siguen usando `os.tmpdir()` sin cambios (`git diff` vacío) · fuente: Node `os.tmpdir()` (Windows: `TEMP`/`TMP`; POSIX: `TMPDIR`); revisor (10 `_util.ts` con `tmpdir()`); D-53.

## B · Candado (D-54, D-55, D-56)

- [B1] (T+H) `vetoShell` de tools/candado.mjs parte el comando con `segmentos()` de tools/_transcript.mjs y no crudo, así que una cadena entre comillas dobles o simples es un solo token aunque contenga `|`, `;`, `&&`, `||` o comillas escapadas (`\"`): con `vieja-01` en rojo, ni `grep -n -E "a|b" tests/orden/vieja-01/HOJA.md` ni `grep -n -E "existsSync|niche=\"barberia\"|rol" "<raíz>/tests/orden/vieja-01/a.test.ts"` dan veto, `cat tests/orden/vieja-01/HOJA.md | grep b` y `echo "a;b"; ls tests/orden/vieja-01` tampoco, y `segmentos()` los parte en 1, 1, 2 y 2 segmentos · fuente: H:tools/candado.mjs:154 (`expandir(command, env).split(/&&|\|\||[;|\n]/)`: parte crudo, sin pasar por `segmentos`); H:tools/_transcript.mjs:83–91 (comillas → `Q` o ruta: ya lo cumple); revisor (veto real de `grep … "a|b" <orden>/HOJA.md`); D-55. · ajuste de A (2026-09-22): la frase original afirmaba la partición sólo de `segmentos()`, que hoy ya da 1, 1, 2 y 2 (medido) y nacía verde; el hueco que el revisor midió está en el candado, que parte el comando crudo y por eso vetó sus dos `grep`.
- [B2] (T+H) el candado sobre shells deja pasar `grep -n -E "a|b" tests/orden/<id>/HOJA.md`, `grep -n -E "x|y" "<raíz>/tests/orden/<id>/a.test.ts"` y `cp tests/orden/<id>/HOJA.md C:/tmp/x` (el origen de `cp` es lectura, D-56), y sigue vetando `cp C:/tmp/x tests/orden/<id>/HOJA.md`, `mv tests/orden/<id>/HOJA.md C:/tmp/x`, `echo x >> tests/orden/<id>/HOJA.md`, `sed -i s/a/b/ tests/orden/<id>/c.test.ts` y `node -e "…writeFileSync('tests/orden/<id>/c.test.ts',…)"` · fuente: revisor (seis casos por stdin, 2026-09-22: los dos `grep` y el `cp` de lectura salen 2 hoy); VERDAD-06 B2 (casos que siguen); D-56.
- [B3] (T+H) el candado vigila `tests/orden/<id>/` del repo propio y del hermano (`ROOTS` de tools/_git.mjs, con «hermano no encontrado» → sólo el propio): desde un repo temporal H' con un hermano temporal T' (rutas inyectadas con `HIGIENE_ROOTS=<H'>;<T'>`), un Write o un Bash que nombre `<T'>/tests/orden/vieja-01/HOJA.md` (rojo en la historia de T') → veto «CANDADO · orden «vieja-01» (hermano) …»; `<T'>/tests/orden/nueva-99/` (sin rojo) → pasa; `.git/permitir-tests` del hermano abre el hermano y el del propio no · fuente: H:tools/candado.mjs:37 (`ROOT` único; «fuera del repo no opina»); H:tools/_git.mjs (`ROOTS`); B (VERDAD-06 B, punto 5); D-54.

## C · Clasificador del cierre (D-56)

- [C1] (T+H) `escrituraShell` de tools/_transcript.mjs trata como lectura `cp <archivo de la raíz> <fuera>`, `cp -r <carpeta de la raíz> <fuera>` y `cp <fuera> <fuera>`, y como escritura `cp <fuera> <archivo de la raíz>`, `mv <archivo de la raíz> <fuera>`, `mv <fuera> <archivo de la raíz>`, `rm <archivo de la raíz>`, `touch <archivo de la raíz>` y `tee <archivo de la raíz>`; y `tools/cierre.mjs` con un transcript real que sólo tiene `cp C:/Users/liama/Desktop/Nichos/Barber-shop-template-main/tests/helpers/png.ts tests/helpers/` (cwd fuera de los repos) clasifica «sólo lectura» · fuente: H:tools/_transcript.mjs:41 (`DISCO`: cp con todos sus argumentos); revisor (cierre bloqueado por esa línea, 2026-09-21/22); D-4 (transcripts reales); D-56.

## D · Navegador en serie (D-57)

- [D1] (T) package.json `test` corre dos fases encadenadas con `&&`: `test:unit` (`tsx --test --test-concurrency=2 …` con los archivos que no importan `playwright`) y `test:browser` (`tsx --test --test-concurrency=1 …` con los diez que sí: tests/ajustes-01, galeria-03, galeria-04, galeria-05, hero-viewport, lang-01, modo-paleta, services-v6, verdad-05-c, webkit-ios); la unión de las dos listas no repite ningún archivo y contiene los 65 que `npm test` nombra en 1398730 (ninguno se pierde); y ningún archivo de `test:unit` contiene `from "playwright"` (guard: se comprueba leyendo cada archivo) · fuente: T:package.json en 1398730 (un solo `tsx --test --test-concurrency=2 …` con 65 archivos); revisor (`grep -l playwright tests/*.test.ts` → 10); D-57. · ajuste de A (2026-09-22): la hoja decía «45 archivos»; contados en 1398730 son 65. Y «unión = exactamente la lista actual» no se puede exigir, porque el mismo B añade a la lista las tres copias de conexion-04 (E2) y el guard de fases (D2): lo que se exige es que no se pierda ninguno de los 65 ni se repita.
- [D2] (T) el guard de fases vive en `tests/suite-fases.test.ts` y lo corre `test:unit`, mientras `.githooks/pre-commit` sigue corriendo `npm test` (sin nombrar fases) y `tests/contrato-hooks.test.ts` pasa sin cambios · fuente: T:.githooks/pre-commit; CLAUDE.md CONTRATO-DECLARADO; § Notas para las sesiones A y B (el guard en `tests/suite-fases.test.ts`, en `test:unit`). · ajuste de A (2026-09-22): la frase original sólo afirmaba el hook y `contrato-hooks`, verdes hoy y sin nada que B cambie, así que el test no podía fallar en el árbol rojo; se le antepone el guard de fases, que es lo que esta orden sí trae.

## E · Retiro de CONEXION-04 (D-58)

- [E1] (T+H) conexion-04 figura en tests/orden/APROBADAS.md con fecha 2026-09-22, T 4a22d7b y H f8bd9bb, y rojo-verde --todas en HEAD imprime «conexion-04 · retirada (aprobada 2026-09-22)» y no «orden conexion-04 ·» · fuente: aprobación de Liam 2026-09-22.
- [E2] (T+H) npm test corre las copias tests/conexion-04-a.test.ts, -b, -d, -e en H (8 tests) y tests/conexion-04-b.test.ts, -d, -e en T (5 tests), que importan ./orden/conexion-04/_util.ts, sin recorte (cada copia lleva los mismos `test(` que su original en la carpeta congelada), y la copia `d` no importa `correrLargo`: no vuelve a correr las copias anteriores (B2); tests/orden/conexion-04/ no cambia desde su último rojo (47490a2 en T, 00b4fe0 en H) · fuente: CONEXION-04 (aprobada); CONEXION-04-B2 (copias `d` sin recursión); D-58. · ajuste de A (2026-09-22): la frase original pedía además «pasan 8/8 y 5/5», y correrlas desde esta orden es recursión sin fin — la copia `d` corre `rojo-verde --todas` sobre una reproducción de HEAD que incluye verdad-08 (todavía sin aprobar), que volvería a correr este mismo test. El verde de las copias lo da `npm test` al correrlas (D-13, como la nota de B2 en conexion-03-d); aquí se cuentan sus `test(`: 8 en H y 5 en T.

## F · Registro

- [F1] (T+H) CLAUDE.md § Puertas tiene un párrafo VERDAD-08 que nombra «restos por proceso», «hermano», «`cp`», «navegador en serie» y «14/36», y el bloque CONTRATO-DECLARADO es idéntico al del commit aprobado de CONEXION-04 (T 4a22d7b / H f8bd9bb) · fuente: E1/F1 anteriores.
- Sin test (Nichos): PLAN.md § Estado fila 4 «VERDAD-08-A (2026-09-22): hoja + 11 afirmaciones (T 11 tests, H 9: sin D1, D2), tests rojos (T <sha>, H <sha>); B pendiente del «andá»»; `4.3.md` § VERDAD-08 · A.

## Interfaz de prueba (contrato entre los tests de A y el código de B)

A1/A2 por caja negra (`node tools/verdad/rojo-verde.mjs --orden <id> --repo <tmp>`) sobre repos temporales con prefijo «verdad-08-» y `node_modules/verdad-08-marca/` propio (patrón verdad-07); el fixture escribe `os.tmpdir()`, `process.env.TEMP`, `TMP` y `TMPDIR` a un archivo del repo temporal (ruta absoluta en el fuente, para que el clon neutro anote en el mismo archivo) y, en A1, crea `<os.tmpdir()>/<id>-marca-<azar>` sin borrarlo y `<tmpdir real>/<id>-ajena-1` y `-2` (ruta absoluta: el proceso ajeno) también sin borrarlas; el test comprueba que las dos `ajena` siguen ahí y las borra en `finally`. El fixture de A2 no crea ninguna de las tres. B1 importando `segmentos` de tools/_transcript.mjs y `vetoShell` de tools/candado.mjs con extensión, con `hoja` y `permiso` sustituidos (la orden `vieja-01` no existe en el repo). B2 por `candadoJson` (patrón verdad-06/07) sobre un repo temporal con `vieja-01` (rojo) y `nueva-99`. B3 con `HIGIENE_ROOTS=<H'>;<T'>` (dos repos temporales, el segundo con rojo de `vieja-01` en su historia; el candado lee `HIGIENE_ROOTS` sólo para pruebas, como `_git.mjs`); el veto nombra «(hermano)». C1 importando `escrituraShell`, y para el cierre un transcript real con la línea de `cp` (D-4; se toma del JSONL de la sesión del revisor del 2026-09-22 o se reproduce con las mismas claves). D1 lee package.json y los archivos de tests; D2 lee el hook y corre `tests/contrato-hooks.test.ts`. E1 como en las hojas anteriores (repro de HEAD sin `verdad-08/` + `--todas --repo`); E2 lista de `npm test` + conteo de `test(` + `git diff`, sin correr las copias (recursión, ver el ajuste de E2). F1 lee disco y compara el bloque por `git show`.
Ningún test escribe en T ni en H fuera de `tests/orden/verdad-08/`; ninguno toca Storage ni Firestore; `_util.ts`, `a`, `b`, `c`, `e`, `f` idénticos T/H por `cmp` (todas T+H salvo D1/D2, que van en `d.test.ts` sólo en T).

## Notas para las sesiones A y B

A: al commit rojo, todos los tests fallan por su primera aserción (A1: las dos carpetas ajenas borradas por el barrido global; A2: `os.tmpdir()` = tmpdir real; B1: el candado veta el primer `grep`; B2: los tres pases salen 2; B3: sin veto en el hermano; C1: `cp` clasificado escritura; D1: un solo `tsx --test`; D2: sin `tests/suite-fases.test.ts`; E1: sin línea; E2: copias ausentes; F1: sin párrafo). Nombre del test = frase de la hoja (A5). Cinco frases se ajustaron al escribir los tests (A1, B1, D1, D2, E2): cada una lleva su «ajuste de A» con el porqué al final de su fuente.
B: `rojo-verde` conserva todo lo demás (clon neutro, acumulación de fallas, tabla); el directorio propio del verde se crea en `<tmpdir>` real con `mkdtemp` y se borra en `finally`; el candado parte el comando con `segmentos()` de `./_transcript.mjs` (ya existe y ya trata bien las comillas) en vez de `expandir(...).split(...)`, importa `ROOTS` de `./_git.mjs` (ya existe) y acepta `HIGIENE_ROOTS` como `cierre.mjs`; `cp`/`mv` en `DISCO` se parten en dos reglas; `test:unit`/`test:browser` en T con el guard en un test propio (`tests/suite-fases.test.ts`, en `test:unit`); la línea de APROBADAS.md de conexion-04 la escribe B (E1). Verificación de B además de los tests: `npm test` en T tres veces seguidas verde (a archivo), `npm test` en H tres veces.

## Fuera de esta hoja

Los timeouts internos de cada test de navegador (AUDITORIA-01 si persisten en serie); el verde en clon neutro; CONEXION-05 (fondo y branding); gama al subir (5.1).
