# HOJA · CONEXION-03 · servicios en el constructor del hub (cinco huecos más: 5/36 → 10/36) y la prueba «lo que la casilla escribe = lo que tiene el tenant», en H · 2026-09-22

Escrita por el revisor desde la línea base de CONEXION-02 (aprobada 2026-09-22, T 6410316 · H 5bf6762; cierre T 3c154f6 · H f70cd65). Estado esperado al abrir A: T 3c154f6 = origin, H f70cd65 = origin.
Fuentes: `H:`/`T:` = archivos en HEAD; `revisor` = lecturas del 2026-09-22 con salida citada; `hueco` = `T tools/verdad/hueco.mjs --json` en HEAD; `CH` = bloque-04/CONTRATOS-HUECOS.md.
Formato: cada afirmación es una línea `- [ID] (repo) frase · fuente: …`. El nombre del test es la frase exacta entre `(repo) ` y ` · fuente:`. `(T)` sólo en T; `(H)` sólo en H; `(T+H)` en los dos.
Circuito: PLAN.md § Cómo se trabaja, incisos i) a n). Decisiones vigentes: D-1..D-5, D-9..D-11, D-13, D-14, D-16..D-38. Decisiones para esta hoja (revisor, 2026-09-22, tomadas salvo que Liam diga lo contrario): D-41 `sections.services.featured` es **orden**, no cantidad (fase 2b de CH:64, T services-v6 «featured = ORDEN, se muestran todas»): lista opcional de ids existentes sin repetir, sin el aviso «exactamente 2» del validador · D-42 `sections.services.surface` acepta lo que T y el validador ya aceptan: `base | alt | velo | liso | textura` · D-43 nombres fijos de las fotos de servicio: `servicio-<i+1>.<ext>` (ext del archivo: jpg, webp, avif, png), rol `services`: misma entrada, misma url que `b4-material.ts` · D-44 los fixtures A y C ganan `sections.services.featured` = los dos primeros ids de su catálogo (orden idéntico al actual: sin cambio visual) para que la fila tenga material · D-45 `pagina.servicios` (ruta derivada) queda fuera: no tiene casilla ni validador por definición · D-46 las casillas nuevas de servicios aparecen sólo con `niche === "peluqueria"` (como D-35); la flota sigue igual.

Por qué: cinco filas de servicios tienen contrato, validador y guard (cuatro) pero no casilla: `services.priceMax`, `services.mode`, `services.images`, `services.featured`, `services.surface`; `services.featured` tampoco tiene material (no está en el fixture A) y `services.priceMax` no tiene guard declarado aunque tests/services-v6.test.ts:66 ya lo nombra. El editor de servicios existe (`services-editor.tsx`, modo custom con `config.services[]`: name, price, duration, description) y es donde peluquería se cuelga: le faltan `priceMax`, `mode`, la foto por servicio (`sections.services.images[i]` ↔ `services[i]`, T services-v6.tsx:14), el destacado (orden) y la superficie de la sección. Y CONEXION-02 dejó una deuda formal: «lo que la casilla escribe es lo que tiene el tenant» nunca estuvo en rojo porque vivía en T; aquí se prueba en H (inciso n), para el hero y para servicios.

## A · Las casillas (H)

- [A1] (H) src/components/config-editors/services-editor.tsx exporta cuatro funciones puras que devuelven un config nuevo sin tocar otras claves: `aplicarServicio(config, i, campo, valor)` con campo `priceMax` (número o undefined, que borra) o `mode` («reserva» | «consulta») sobre `services[i]`; `aplicarFotoServicio(config, i, url)` sobre `sections.services.images[i]` (la lista se rellena con "" hasta i; url vacía deja ""); `aplicarDestacado(config, id, activo)` sobre `sections.services.featured` (ids en el orden del catálogo; lista vacía borra la clave); `aplicarSurface(config, valor)` sobre `sections.services.surface`; y `nombreFotoServicio(i, nombreArchivo)` = `servicio-<i+1>.<ext>` (D-43). Aplicadas sobre el fixture A sin `priceMax`, `mode`, `images`, `featured` ni `surface`, con los valores de A (T dev-fixtures/peluqueria-paleta-a.json), devuelven `services` y `sections.services` iguales a los de A (deepEqual) · fuente: revisor (A: 12 servicios con id, name, description, duration, price, priceMax ×11 —`consult` no lo tiene—, mode reserva/consulta y `popular: true` en `highlights`; `sections.services` = variant, surface «velo», title, subtitle, images ×12 de las que **once** son urls de Storage bajo `clients/test-b4-peluqueria-a/media/services/servicio-<n>.jpg` y la de `kids-cut` (`images[10]`) está vacía — ajuste del revisor en A, 2026-09-22: la hoja decía «images ×12 en Storage»); D-43, D-44.
- [A2] (H) `renderToString` del editor de servicios en modo custom con `niche="peluqueria"` muestra los campos `priceMax`, `mode` (select con `reserva` y `consulta`), foto y destacado del servicio abierto —con un solo servicio, el primero de A: su `priceMax`, su `mode` y la url de `sections.services.images[0]`— y, a nivel de sección, el select `surface` con las cinco opciones de D-42; con `niche="barberia"` ninguno de esos cinco campos aparece (D-46) y los campos de hoy (name, price, duration, description) siguen; src/components/client-config-tab.tsx pasa `niche` al editor · fuente: H:src/components/config-editors/services-editor.tsx:97 (`ServicesEditor`), :480 (`CustomServicesEditor`; `expanded` arranca con un solo servicio abierto, de ahí «del servicio abierto»), :615–645 (name, id, price, duration, description del modo custom); H:src/components/client-config-tab.tsx:1851–1856 (ya pasa `niche`); D-34 (render en servidor); ajuste del revisor en A, 2026-09-22 (la hoja decía «por cada servicio» y citaba :398–422, que es el modo preset, y :363–369, que es la firma de `ClientConfigTab`).
- [A3] (H) la foto de un servicio sube por `/api/upload/${clientId}` con `rol` = `services` y el archivo renombrado a `nombreFotoServicio(i, nombre)` (`formularioFotoServicio(archivo, i)` devuelve el FormData; lanza si el tipo no es image/jpeg, png, webp o avif); el path que `subirMaterial` produce para cada una de las once fotos de A (`clients/<id>/media/services/servicio-<n>.jpg`) es igual al path decodificado de `sections.services.images[i]` en el fixture A (once de once: `images[10]` está vacía) · fuente: CONEXION-01 A1 (path `clients/<id>/media/<rol>/<nombre saneado>`); revisor (paths de A: servicio-1.jpg … servicio-12.jpg, sin el 11 —`images[10]` es `""`—; ajuste del revisor en A, 2026-09-22: la hoja decía «doce de doce»).
- [A4] (H) `scripts/b4-tenant.ts show --id test-b4-peluqueria-a` (con Firestore, sólo lectura) devuelve un `config/{id}` cuyo `hero.video` es igual al que `aplicarHeroVideo` construye desde el fixture A (deuda de CONEXION-02 C1, ahora en H), y cuyos `services` y `sections.services.{surface,images,featured}` son iguales a los que A1 construye desde el fixture A · fuente: CONEXION-02 (C1 nunca estuvo en rojo por el hermano; inciso n); D-16 (`show` no escribe; el tenant lo reescribe B con `recrear --paleta a` con Firestore tras D-44); H:src/components/config-editors/hero-video-editor.tsx (`aplicarHeroVideo`).

## B · Contrato, validador, guard y material

- [B1] (T) verdad/contratos.json y CH: `ui` en `services.priceMax`, `services.mode`, `services.images`, `services.featured`, `services.surface` = `{ ruta: "/clients/[clientId]", componente: "src/components/config-editors/services-editor.tsx", campo: "priceMax" | "mode" | "images" | "featured" | "surface" }`; `guard` de `services.priceMax` = `{ archivo: "tests/services-v6.test.ts", clave: "priceMax" }`; `tipo` de `services.surface` = «enum base|alt|velo|liso|textura» (D-42) y de `services.featured` = «orden (lista de ids, fase 2b)» (D-41); en bloque-04/CONTRATOS-HUECOS.md, la fila que empieza por el `contrato.campo` de cada una de las cinco lleva la nota «casilla de servicios (CONEXION-03)» (como las tres del hero llevan «casilla del hero (CONEXION-02)»); las otras 31 filas del .json byte a byte como en 3c154f6 · fuente: T:verdad/contratos.json:106–110 («enum velo|textura»); CH:52 y :71 (`sections.services.images[i]`), :56 y :74 (`services[i].price`, `priceMax`), :57 y :76 (`services[i].mode`), :70 y :150 (`services.featured`, «exactamente 2»), :59, :83 y :149 (`surface`, «base | alt»), :210–214 (tabla § Constructor del hero) — ajuste del revisor en A, 2026-09-22: la hoja decía «filas 59, 70, 83 y 150», que no son las cinco (el `contrato.campo` de `services.surface` es `sections.<id>.surface`, línea 149), así que el test busca por campo y no por número de línea; T:tests/services-v6.test.ts:66 (`priceMax`); hueco («services.priceMax · guard: sin guard»).
- [B2] (H) `validateReplanteoHuecos` acepta `sections.services.featured` de cualquier longitud ≥ 1 con ids existentes y sin repetir, sin el aviso «exactamente 2» (D-41), y sigue dando error por id inexistente o repetido y por lista que no es de strings; `validateVariantContracts` da **error** (no aviso) por cada `sections.services.images[i]` no vacío que no empiece por `https://` (como `hero.video.*` en CONEXION-02); los fixtures A y C (con D-44) pasan sin errores; un config con `images[0] = "/dev-fixtures/media/paleta-a/servicio-1.jpg"` es rechazado con path `sections.services.images[0]` · fuente: H:src/lib/config-validator.ts:737–745 (featured: `length !== 2` → warning), :682–704 (images: sólo avisos), :830–831 (surface: base, alt, velo, liso, textura).
- [B3] (T) dev-fixtures/peluqueria-paleta-a.json y -c.json tienen `sections.services.featured` = los dos primeros ids de su catálogo (A: los dos primeros de `services[]`; C ídem), y nada más cambia en ellos (`git diff 3c154f6 HEAD -- dev-fixtures/` = esas dos líneas); `hueco.mjs --json` da las cinco casillas en «sí» y `hecho: true` en las cinco filas de servicios, y el total es «10/36 huecos hechos»; `pagina.servicios` sigue sin hacer (D-45) · fuente: hueco (hoy 5/36; `services.featured · material: sections.services.featured no está en el fixture A`); D-44.

## C · Punta a punta (T)

- [C1] (T) `recrear.mjs --paleta a --sin-firestore --paginas home --vistas 375 --puerto <libre>` y `--paleta c` no producen ninguna brecha «validador H rechaza» ni «material que producción no sirve», y las brechas totales no superan la línea base (A ≤ 1 en home 375, C ≤ 2): D-44 no cambia la página · fuente: CONEXION-02-B (A 1 diff, C sin contrato + diff; 0 validador, 0 material); T:src/components/landing/services/services-v6.tsx:77 (`orderFeatured`: los dos primeros ids = orden del catálogo).

## D · Retiro de CONEXION-02

- [D1] (T+H) npm test corre las copias tests/conexion-02-a.test.ts, -b, -d, -e en H (6 tests) y tests/conexion-02-b.test.ts, -c, -d, -e en T (5 tests: la copia `c` conserva sólo la parte de recrear de C1, sin `show` ni nada de H, inciso n), que importan ./orden/conexion-02/_util.ts; pasan 6/6 y 5/5; tests/orden/conexion-02/ no cambia desde su último rojo (25d5be7 en T, f1e25f1 en H) · fuente: CONEXION-02 (aprobada; la línea de APROBADAS.md ya está: T 3c154f6 · H f70cd65); D-19 (recorte de e2e al promover).

## E · Registro

- [E1] (T+H) CLAUDE.md § Arquitectura mínima de H nombra las casillas de servicios (`priceMax`, `mode`, foto por servicio, destacados, `surface`) en `services-editor`; CLAUDE.md § Puertas de T y de H tiene un párrafo CONEXION-03 que nombra «10/36» y «featured = orden»; el bloque CONTRATO-DECLARADO es idéntico al del commit aprobado de CONEXION-02 (T 6410316 / H 5bf6762) · fuente: E1 anteriores.
- Sin test (Nichos): PLAN.md § Estado fila 4 «CONEXION-03-A (2026-09-22): hoja + 10 afirmaciones (H 7 tests: A1–A4, B2, D1, E1; T 5: B1, B3, C1, D1, E1), tests rojos (T <sha>, H <sha>); B pendiente del «andá»»; CH: filas 59, 70, 83, 150 y § Constructor («15/36 con UI» → «20/36»); `4.3.md` § CONEXION-03 · A.

## Interfaz de prueba (contrato entre los tests de A y el código de B)

Como en CONEXION-02: `node --experimental-strip-types --test`; los tests de H cargan `.tsx` con el hook `module.register` de `tests/orden/conexion-02/_util.ts` (copiar el mecanismo, no importar `../conexion-02/`: la reproducción de HEAD del D1 congelado no lleva otras carpetas). **Ajuste del revisor en A (2026-09-22):** el editor de servicios **ya existe** y sus imports son `@/lib/…` y relativos sin extensión, así que el hook de esta orden añade un `resolve` que traduce `@/x` → `<ROOT>/src/x` y prueba `.tsx`, `.ts`, `/index.tsx`, `/index.ts` en los relativos; se adapta el arnés y **no** se le pide a B reescribir los imports de producción (comprobado el 2026-09-22: con ese hook cargan `services-editor.tsx` y todo su grafo —image-upload-field, reorder-controls, language-mismatch-warning, client-language-context, client-config/services, image-compress, use-language-mismatch, client-language, dashboard-placeholders—, ninguno necesita `next/*`). Cada campo nuevo se reconoce en el HTML por su clave literal (`priceMax`, `mode`, `featured`/«destacado», `surface`, la url de la foto): B puede darla como texto o como atributo (`data-campo="priceMax"`). `nombreFotoServicio` normaliza la extensión al juego de D-43: `.JPG` y `.jpeg` → `jpg`. A1: cada función pura se prueba con `existsSync` del archivo antes del import dinámico (rojo por «no existe»/«no exporta», no por error de carga) y con deepEqual contra A. A2: `renderToString` del editor con las props que hoy usa la pestaña (`config` con `services` de A, `niche`, `clientId`, `setConfig` no-op); se compara por `includes` de etiquetas y valores; la ausencia con barbería es `!html.includes("priceMax")` etc. A3: `File` de Node; los paths se comparan decodificando `/o/<path>?` de las doce urls de A leídas de T por ruta fija. A4: `show` real con `.env.local` (si falta, falla nombrándolo); el test parsea el JSON entero de `show` (CONEXION-02 lo dejó sin recorte) y compara `hero.video`, `services` y `sections.services.{surface,images,featured}` con lo construido en A1/`aplicarHeroVideo`; rojo por `existsSync` de la función nueva antes de `show`. B1 lee el .json y el .md; B2 importa el validador con extensión; B3 lee los fixtures de T por ruta fija y corre `hueco.mjs --json` real. C1 recrear real (`--sin-firestore`, prefijo «conexion-03-», ≤ 10 min). D1 y E1 como en las hojas anteriores.
Ningún test escribe en T ni en H fuera de `tests/orden/conexion-03/`; ninguno escribe en Storage ni en Firestore (`show` sólo lee; el tenant A lo reescribe B con `recrear --paleta a` con Firestore, D-16); `_util.ts`, `d`, `e` idénticos T/H por `cmp`; `a` sólo en H; `b` en los dos por `SOY`/`REPO`; `c` sólo en T.

## Notas para las sesiones A y B

A: al commit rojo, los doce tests (10 afirmaciones; H 7, T 5; D1 y E1 en los dos) fallan por su primera aserción: A1–A4 por `existsSync`/«no exporta» de las funciones nuevas; B1 por `ui: null`; B2 por el aviso «exactamente 2» presente o por `images[0]` local sin error; B3 por «featured no está en el fixture A» / 5/36; C1 pasa hoy en recrear pero cae porque el test exige antes que los fixtures tengan `featured` (D-44); D1 por copias ausentes; E1 por el párrafo. Nombre del test = frase de la hoja (A5). Ninguna afirmación de T mira a H ni al revés (inciso n): la única que cruza es A4, en H, y su rojo es del árbol de H.
B: las casillas nuevas van dentro del `CustomServicesEditor` existente por servicio (priceMax, mode, foto con `ImageUploadField` si sirve, destacado) y un select `surface` en la cabecera de la sección; escriben con las funciones puras de A1; sólo con `niche === "peluqueria"` (D-46). D-44: `featured` en los dos fixtures antes de `recrear`; después `recrear --paleta a` y `--paleta c` **con Firestore** (D-16) para que el tenant A tenga `featured` y A4 pase; citar tablas y brechas. `layout-variants-editor.tsx` y el modo preset no cambian.

## Fuera de esta hoja

`pagina.servicios` (D-45); galería (6 filas), branding/fondo (5), paleta y hero.eyebrow/mask/alto (CONEXION-04 en adelante); gama al subir (5.1); AUDITORIA-01; Playwright en el hub (D-34).
