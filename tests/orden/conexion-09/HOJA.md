# HOJA · CONEXION-09 · `heroToBackdrop` sin casilla y sin datos falsos; guard de Instagram sin cuenta y brechas con nombre; retiro de PRESET-01 · 2026-09-24

Escrita por el revisor desde la línea base de PRESET-01 (aprobada por Liam 2026-09-24, T 1b0ccd6 · H 22845be). Estado esperado al abrir A: T 1b0ccd6 = origin, H 22845be = origin.
Fuentes: `H:`/`T:` = archivos en HEAD; `revisor` = lecturas del 2026-09-24 con salida citada; `hueco` = `T tools/verdad/hueco.mjs --json` en HEAD; `CH` = bloque-04/CONTRATOS-HUECOS.md.
Formato: cada afirmación es una línea `- [ID] (repo) frase · fuente: …`. El nombre del test es la frase exacta entre `(repo) ` y ` · fuente:`. `(T)` sólo en T; `(H)` sólo en H; `(T+H)` en los dos.
Circuito: PLAN.md § Cómo se trabaja, incisos i) a n). Decisiones vigentes: D-1..D-5, D-9..D-11, D-13, D-14, D-16..D-89. Decisiones para esta hoja:

- **D-90 (Liam, 2026-09-24: «Arreglar y no construir»).** Reemplaza a D-83. `branding.heroToBackdrop` **no tiene casilla en el hub**: desde TRANSICION-02 la página no lo consume (T:src/components/landing/LocalBackdrop.tsx:22–23: «sigue siendo dato del fixture, pero … la página no pinta ninguna banda con él»; revisor: ningún otro uso en `src/` fuera de `types.ts`), Storage no responde con `Access-Control-Allow-Origin` (revisor: GET con `Origin: https://arzac.studio` a la foto y al vídeo de A → 206 sin cabecera CORS, así que el navegador del hub no puede leer sus píxeles) y construir la medida en el hub costaría una ruta proxy nueva por un valor que no se pinta. Pasa a **derivado sin casilla**, como los otros cinco: el techo real del constructor es 30/36 (36 − seis derivados), con `features.themeToggle` en DISEÑO-01. El dato se mantiene **verdadero**: lo calcula `transicion.mjs` desde el material real.
- **D-91 (del revisor, se sigue de D-90).** `validateReplanteoHuecos` deja de avisar «Hay vídeo del hero y foto del local sin la relación hero → fondo escrita»: un cliente real que sube vídeo y foto en el hub no puede producir ese valor, y un aviso sin acción posible es ruido. Siguen siendo **error** `relation`, `mechanism` y `foot.hex` inválidos cuando el valor está.
- **D-92 `transicion.mjs` arreglado.** (1) Lee el material de Storage por su archivo local `dev-fixtures/media/paleta-<p>/<nombre>` como ya hace `gama.mjs` (`deStorage`, D-22: el token es el sha256 del contenido, así que medir el archivo local es medir lo que Storage sirve); hoy sale exit 1 en A y en C («Error opening input file …\https:\firebasestorage…»). (2) La aritmética (`dH`, `dL`, `neutro`, `relation`, `mechanism`, `foot`, `footPortrait`) se extrae a una función pura `relacionHeroFondo` en un módulo propio de `tools/material/` que **no importa `gama.mjs`** (que importa `playwright`), y `transicion.mjs` la usa. (3) **Sin comodín**: fuera de los tres casos `relation` es `null`, `--escribir` no escribe nada y sale 1 (hoy escribe `relation ?? "adjacent-hue"`, T:tools/material/transicion.mjs:56). (4) `gama.mjs` exporta su resolución de rutas en vez de duplicarla.
- **D-93 los datos de A y C, recalculados.** El fixture C guarda desde 7dbcb19 (2026-09-19) `{ relation: "adjacent-hue", dH: 100, foot L 0,215 H 293° }`: un clip anterior y una relación que el comodín inventó (ΔH 100 está fuera de los tres casos). Medido por el revisor con una copia de `transicion.mjs` que lee los archivos locales (sin `--escribir`): **A** `same-hue` · ΔH 0 (neutro) · ΔL 0,079 · `veil-from-first-pixel` · pie 9:16 `#908b8a` (el guardado es `#908e92`); **C** `same-hue-different-light` · ΔH 6° · ΔL 0,118 · `scrim-dies-into-photo` · pie L 0,401 H 27° · pie 9:16 `#0e090a`. B los escribe con `transicion.mjs --escribir` y reescribe los tenants. La página no cambia (no los consume).
- **D-94 pendientes de la corrida en limpio de PRESET-01.** (a) D-87 no quedó vigilado: un guard de T en `test:browser` monta la página y comprueba que la sección de Instagram sin cuenta no pinta enlaces y que con cuenta sí. (b) Las copias `conexion-02-c`, `conexion-03-c` y `conexion-05-b` comparan `informe.brechas.length <= tope`: una brecha distinta puede ocupar el lugar de otra sin que nadie se entere; pasan a comparar el conjunto de `tipo · campo` con una lista escrita.
- **D-95** PRESET-01 se promueve: T `a`, `d`, `e` = 5 tests; H `b` (B1 y B2; B3 lee Firestore), `d`, `e` = 5 tests. Toda copia promovida que compare los fixtures con una línea base (al menos `conexion-05-b`, `conexion-08-a` y la nueva `preset-01-a`) excluye también `branding.heroToBackdrop`, hoja por hoja.

Por qué: la hoja de CONEXION-09 iba a construir en el hub la medida de `heroToBackdrop` (D-83). Al prepararla, el revisor midió que la página no usa ese valor, que el navegador del hub no puede leer el material de Storage, que la herramienta que lo calcula está rota desde CONEXION-01 y que el valor de C es falso. Liam eligió no construir y dejar el dato verdadero.

## A · La transición, calculada de verdad (T)

- [A1] (T) tests/transicion.test.ts existe, está en `test:unit` de package.json y nombra literalmente «heroToBackdrop»; afirma sobre `relacionHeroFondo` (importada de su módulo en `tools/material/`, que no importa `gama.mjs` ni `playwright`): ΔH 10° y ΔL 0,10 → `same-hue`; ΔH 10° y ΔL 0,11 → `same-hue-different-light` con `scrim-dies-into-photo`; ΔH 35° → `adjacent-hue`; ΔH 36° → `relation` `null`; croma < 0,01 en el pie o en la foto → `neutro` y ΔH 0; y los números de A y C de D-93 dan su relación y su mecanismo. Además, `transicion.mjs` importa `relacionHeroFondo` y no contiene `?? "adjacent-hue"` · fuente: T:tools/material/transicion.mjs:43–56 (aritmética y comodín); CH § Transición hero → fondo (umbrales); D-92.
- [A2] (T) `node tools/material/transicion.mjs peluqueria-paleta-a --json` y `… -c --json` salen 0, y `branding.heroToBackdrop` de cada fixture es igual a lo que imprimen (`relation`, `mechanism`, `dH`, `dL`, `foot`, `footPortrait`): A `same-hue` con `veil-from-first-pixel`, C `same-hue-different-light` con `scrim-dies-into-photo`; nada más cambia en los fixtures (`git diff 1b0ccd6 HEAD -- dev-fixtures/*.json`, comparado hoja por hoja, sólo toca `branding.heroToBackdrop`) · fuente: revisor (hoy exit 1 en los dos; C guarda `adjacent-hue` con `dH: 100`); D-92, D-93.

## B · El contrato (T)

- [B1] (T) verdad/contratos.json declara en la fila `branding.heroToBackdrop` `guard` = `{ archivo: "tests/transicion.test.ts", clave: "heroToBackdrop" }` y un `tipo` que contiene «sin casilla (D-90)»; CH gana «sin casilla (CONEXION-09)» en la línea donde vive su contrato; las otras 35 filas del .json byte a byte como en 1b0ccd6; y `hueco.mjs --json` da en esa fila guard «sí» y UI «NO», y el total sigue en «29/36 huecos hechos» · fuente: T:verdad/contratos.json (hoy `tipo: "derivado (transicion.mjs)"`, `ui: null`, `guard: null`); hueco (hoy 29/36); D-90.

## C · Pendientes de PRESET-01 (T)

- [C1] (T) tests/instagram-sin-cuenta.test.ts existe, está en `test:browser` de package.json y nombra literalmente «instagram»; monta la página real (Vite en proceso) y afirma que con el fixture A la sección de Instagram pinta sus seis fotos y ningún `<a>` (ni `href=""` ni `instagram.com/`), y que con una cuenta (un nicho de la flota cuyo preset trae `sections.instagram.url`) la misma sección sí pinta el enlace a esa url · fuente: T:src/components/landing/InstagramTeaser.tsx (PRESET-01, D-87); revisor (página de A y C sin enlaces a Instagram, medido 2026-09-24); D-94 a.
- [C2] (T) tests/conexion-02-c.test.ts, tests/conexion-03-c.test.ts y tests/conexion-05-b.test.ts no contienen `brechas.length <=` y comparan, por paleta, el conjunto de `tipo · campo` de las brechas de `recrear` con una lista escrita en el propio test · fuente: revisor (`grep -n "brechas.length"`: las tres, con topes A ≤ 2 / C ≤ 3 desde PRESET-01); D-94 b.

## V · El validador del hub (H)

- [V1] (H) `validateReplanteoHuecos` sobre un config con `hero.video` y `branding.localPhoto` de Storage y sin `branding.heroToBackdrop` no da ningún aviso ni error con ruta `branding.heroToBackdrop`; con `relation: "vecino"` da error en `branding.heroToBackdrop.relation`; y los fixtures A y C de T (el hermano, leídos del disco) no dan ningún error · fuente: H:src/lib/config-validator.ts:835–837 (el aviso), :841–842, :861 (los errores); D-91.
- [V2] (H) `scripts/b4-tenant.ts show --id test-b4-peluqueria-a` y `--id test-b4-peluqueria-c` (con Firestore, sólo lectura) devuelven un `branding.heroToBackdrop` igual al del fixture de T de su paleta, con A `same-hue` y `footPortrait.hex` `#908b8a`, y C `same-hue-different-light` con `scrim-dies-into-photo` · fuente: D-16; D-93 (valores medidos por el revisor); inciso n.

## D · Retiro de PRESET-01 (D-95)

- [D1] (T+H) preset-01 figura en tests/orden/APROBADAS.md con fecha 2026-09-24, T 1b0ccd6 y H 22845be, y rojo-verde --todas en HEAD imprime «preset-01 · retirada (aprobada 2026-09-24)» y no «orden preset-01 ·» · fuente: aprobación de Liam 2026-09-24.
- [D2] (T+H) npm test corre las copias tests/preset-01-a.test.ts, -d, -e en T (5 tests) y tests/preset-01-b.test.ts, -d, -e en H (5 tests; la `b` sin B3), que importan ./orden/preset-01/_comun.ts, sin volver a correr copias anteriores (la copia `d` excluye las órdenes vivas con `excluidas()`, sin nombrarlas); en T van a `test:unit` salvo que abran Chromium; tests/orden/preset-01/ no cambia desde su último rojo (416e6d4 en T, 2faac7b en H) · fuente: PRESET-01 (aprobada); D-95.

## E · Registro

- [E1] (T+H) CLAUDE.md § Puertas de T y de H tiene un párrafo CONEXION-09 que nombra «sin casilla», «transicion», «D-90» y «30/36»; el bloque CONTRATO-DECLARADO es idéntico al del commit aprobado de PRESET-01 (T 1b0ccd6 / H 22845be) · fuente: E1 anteriores.
- Sin test (Nichos): PLAN.md § Estado fila 4 «CONEXION-09-A (2026-09-24): hoja + 10 afirmaciones (T 8 tests: A1, A2, B1, C1, C2, D1, D2, E1; H 5: V1, V2, D1, D2, E1), tests rojos (T <sha>, H <sha>); B pendiente del «andá»»; CH: la fila y § Constructor («techo 30/36»); `4.3.md` § CONEXION-09 · A; D-83 reemplazada por D-90.

## Interfaz de prueba (contrato entre los tests de A y el código de B)

Como en PRESET-01: `node --experimental-strip-types --test`; utilidad `_comun.ts` (copia, no import, del cargador de `tests/orden/preset-01/_comun.ts`); imports de `tools/` y de `src/` sólo con `import()` dinámico dentro del test; ninguna afirmación cuenta órdenes (inciso m); cada afirmación en el repo cuyo árbol la pone en rojo (inciso n): A1, A2, B1, C1, C2 en T; V1, V2 en H. **Runner anidado (lección de PRESET-01-A):** A1 y C1 corren su guard con el mismo runner que su fase en la suite de T (`tsx --test`, con `NODE_TEST_CONTEXT: undefined`) y lo dicen en un comentario; si el guard además debe correr con `node` a secas, la afirmación lo dice. A1: primero `existsSync` del guard → rojo por eso; después lo corre y exige exit 0 y sus nombres en el TAP, y llama él mismo a `relacionHeroFondo` con los casos de la frase. A2 corre `transicion.mjs` real (ffmpeg y Chromium: el test va en la fase de a uno al promoverse) y compara con `git show 1b0ccd6:…` hoja por hoja. C1: `existsSync` → rojo; después corre el guard. C2 lee los tres archivos. V1 carga `config-validator.ts` y los fixtures del hermano (ruta por `ROOTS` de `tools/_git.mjs`); V2 corre `b4-tenant.ts show` real (sólo lectura). D1, D2, E1 como en las hojas anteriores.
Ningún test escribe en T ni en H fuera de `tests/orden/conexion-09/`; ninguno escribe en Storage ni en Firestore; `_comun.ts`, `d`, `e` idénticos T/H por `cmp`; `a`, `b`, `c` sólo en T; `v` sólo en H.

## Notas para las sesiones A y B

A: al commit rojo, los tests (10 afirmaciones; T 8, H 5) fallan por su primera aserción: A1 por «no existe tests/transicion.test.ts»; A2 por el exit 1 de `transicion.mjs` en A («Error opening input file …https:…»); B1 por `guard: null`; C1 por «no existe tests/instagram-sin-cuenta.test.ts»; C2 por el primer `brechas.length <=` de `conexion-02-c`; V1 por el aviso de :835–837; V2 por el `relation` del tenant C (`adjacent-hue`, no `same-hue-different-light`); D1, D2, E1 como siempre. Nombre del test = frase de la hoja.
B: en T, el módulo puro, `transicion.mjs` (D-92), la exportación de `gama.mjs`, los dos fixtures con `--escribir`, los dos guards nuevos (`transicion` en `test:unit`, `instagram-sin-cuenta` en `test:browser`, D-57), las tres copias con brechas con nombre, contratos.json y CH. En H, sólo el aviso de `config-validator.ts` (y sus tests si lo afirmaban), APROBADAS.md, las copias y CLAUDE.md. Los tenants `test-b4-peluqueria-a` y `-c` se reescriben con `recrear --paleta a|c` **con Firestore** (0 brechas de validador y de material). `hueco.mjs` sigue en 29/36. La línea de APROBADAS.md de preset-01 la escribe B (D1).


### Interfaz fijada por A (2026-09-24)

§ Interfaz de la hoja habilita a A a llamar a `relacionHeroFondo` «con los casos de la frase», así que A fija su firma; B la respeta o A la corrige.

- **`relacionHeroFondo(pie, foto)`**, exportada del módulo puro de `tools/material/` que `transicion.mjs` importa (el nombre del archivo lo elige B: A1 lo localiza por ese import). `pie` y `foto` son la medida OKLCH que `transicion.mjs` ya calcula: `{ L, C, H }` (el `pie` es la media de los dos fotogramas del clip; la `foto`, el tono dominante del local).
- Devuelve al menos `{ dH, dL, neutro, relation, mechanism, foot }`, con `foot` = `{ hex, L, C, H }` del pie. Un tercer argumento opcional (el pie 9:16, para `footPortrait`) no molesta a las llamadas de A1.
- `relation` es `null` fuera de los tres casos, y `--escribir` no escribe nada y sale 1 (D-92 (3)).
- El módulo **no** puede contener «gama.mjs» ni «playwright» en su fuente: el guard de A1 corre en la fase concurrente `test:unit`.
- `transicion.mjs --json` imprime, además de lo de hoy, **`footPortrait`**: A2 compara las seis claves de `branding.heroToBackdrop` con lo que imprime, y hoy `out` no lo lleva.

### Ajustes de A (2026-09-24)

- **A1 y C1 corren su guard con `tsx`, no con `node` a secas** (§ Interfaz lo pide, lección de PRESET-01-A). `_comun.ts` trae `correrGuardTsx`, que ejecuta `node_modules/tsx/dist/cli.mjs --test <guard>` —el mismo binario que `npm run test:unit` y `npm run test:browser`— con `NODE_TEST_CONTEXT` borrado. El guard de C1 monta Vite en proceso: con `node` a secas no cargaría.
- **A1 mide los números de D-93 por sus ΔH y ΔL, no por valores absolutos inventados.** D-93 publica la relación, ΔH, ΔL, el mecanismo y el pie 9:16 de A, y de C además el pie del local (L 0,401 H 27°), pero no el L del pie del clip de C ni el tono del local de A. A1 construye dos pares `{L,C,H}` que dan exactamente esos ΔH y ΔL (A neutra con ΔL 0,079; C con ΔH 6° y ΔL 0,118) y comprueba la relación y el mecanismo, que es lo que la frase afirma. Los valores absolutos los comprueba A2 contra la corrida real.
- **C2 añade cómo se ve una brecha con nombre.** La frase pide que las tres copias «comparen el conjunto de `tipo · campo` con una lista escrita»; para que eso sea comprobable por lectura, el test exige además que cada copia arme el nombre como `${b.tipo} · ${b.campo}`, que use `assert.deepEqual` y que la lista escrita **nombre «diff ≠ 0»**, que es la brecha que la corrida de las tres deja de verdad. Sin la tercera, «no contiene `brechas.length <=`» se cumpliría borrando la aserción: las dos primeras ya son ciertas hoy (las tres copias imprimen ese nombre en su mensaje de fallo y ya usan `deepEqual` para otra cosa), «diff ≠ 0» no aparece en ninguna.

## Fuera de esta hoja

DISEÑO-01 (`features.themeToggle`, D-82): la siguiente. FLOTA-01 (D-88): decisión de Liam; si saca las cuentas de Instagram de la flota, la dirección «con cuenta» de C1 (hoy barberia, estetica, nails y tattoo traen `sections.instagram.url`, revisor) necesitará otra fuente. `costura.mjs` sigue leyendo `foot` (herramienta de material, no la página). AUDITORIA-01 (flake de navegador).
