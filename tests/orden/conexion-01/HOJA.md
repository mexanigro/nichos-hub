# HOJA · CONEXION-01 · el material vive donde producción lo sirve: puerta de subida del hub (imagen y vídeo), material de A y C en Storage por el hub, CSP de la plantilla, fixtures apuntando a Storage, hueco.mjs y recrear.mjs sin brechas de material · 2026-09-21

Escrita por el segundo pensamiento (sólo lectura) desde la línea base de VERDAD-05 (aprobada por Liam el 2026-09-21: T 30b7daa, H 8239e2d) y el disco de hoy. Ninguna afirmación nace de la ejecutora.
Fuentes: `H:` / `T:` = archivos en HEAD; `base` = tabla de `hueco.mjs` y desgloses de `recrear` de VERDAD-05-B (`4.3.md` § VERDAD-05 · B); `orden` = C:/Users/liama/Desktop/VERDAD-01 · el método antes que el.txt; `HOJA-05` = `tests/orden/verdad-05/HOJA.md`; `revisor` = lecturas del 2026-09-21 con salida citada en el chat.
Formato: cada afirmación es una línea `- [ID] (repo) frase · fuente: …`. El nombre del test es la frase exacta entre `(repo) ` y ` · fuente:`. `(T)` sólo en T; `(H)` sólo en H; `(T+H)` en los dos.
Circuito: PLAN.md:30 (inciso i), j) y k). Decisiones vigentes: D-1..D-5, D-9..D-11, D-13, D-14, D-16..D-19. Decisiones de Liam para esta hoja (2026-09-21): D-20 `b4-material.ts` puede escribir bajo `clients/test-b4-peluqueria-a/media/**` y `-c/media/**` en el bucket `barbertemplate-madre.firebasestorage.app` (sólo en manos de B; los tests nunca) · D-21 la CSP de la flota lleva `media-src 'self' https://firebasestorage.googleapis.com blob:` (host exacto) · D-22 los fixtures A y C pasan a apuntar a Storage; los archivos locales quedan en `dev-fixtures/media` como fuente de trabajo y `-Clip` sigue en local · D-23 `gama.mjs` como validador al subir queda para el bloque 5.1 · D-24 la primera casilla del hub (CONEXION-02) será el hero. Regla de CONEXION-01 (HOJA-05 D-17): cada orden trae su prueba real; ésta no tiene UI (la casilla es la orden siguiente), así que su prueba real es la URL servida y `recrear` sin brechas de material.

## A · La puerta del hub: una función para todo el material

- [A1] (H) src/lib/media-upload.ts exporta subirMaterial({ clientId, rol, nombre, buffer, contentType }, deps?) que guarda en Storage en `clients/<clientId>/media/<rol>/<nombre saneado>` con `cacheControl: public, max-age=31536000` y un token de descarga igual a los 32 primeros hex del sha256 del contenido, y devuelve { url, path, bytes } con url `https://firebasestorage.googleapis.com/v0/b/<bucket>/o/<path codificado>?alt=media&token=<token>`; admite image/jpeg, image/png, image/webp, image/avif (≤ 5 MB) y video/mp4, video/webm (≤ 6 MB); otro tipo o un exceso lanza un error que nombra el archivo y el límite; el mismo contenido en el mismo `rol/nombre` produce la misma url (idempotente) y un contenido distinto produce otra; `deps.bucket` sustituye el bucket real sólo para pruebas (los tests nunca escriben en Storage) · fuente: H:src/app/api/upload/[clientId]/route.ts:6–17 (sólo imágenes, 5 MB), :88 (`clients/<id>/images/<ts>-<nombre>`: nombre no reproducible), :103–108 (token aleatorio); H:src/lib/firebase-admin.ts:88–93 (bucket `barbertemplate-madre.firebasestorage.app`); PLAN.md:26 (g: vídeo ≤ 6 MB, D2); T:dev-fixtures/media/paleta-a (hero.mp4 5487 KB, hero-v.mp4 5760 KB: no entran en 5 MB).
- [A2] (H) POST /api/upload/[clientId] usa subirMaterial: acepta además video/mp4 y video/webm (≤ 6 MB), toma el `rol` del campo `rol` del formulario (por defecto `images`, para la flota) y sigue devolviendo { urls, errors }; `resolveContentType` sale a src/lib/media-upload.ts y resuelve .mp4 y .webm por extensión · fuente: H:src/app/api/upload/[clientId]/route.ts:19–36 (extensiones), :45 (`withOwner`), :59–122; usos en la flota: H:src/components/image-upload-field.tsx:53,206, brand-package-import.tsx:236, transparent-png-uploader.tsx:58.
- [A3] (H) scripts/b4-material.ts --paleta a|c [--fixture <ruta>] [--media <dir>] [--bucket-falso <dir>] [--solo <rol>] lee el fixture de T (`dev-fixtures/peluqueria-paleta-<p>.json`), sube con subirMaterial cada archivo referenciado bajo /dev-fixtures/media/ (hero.video.{mp4,webm,poster}, hero.video.medium.*, hero.video.portrait.*, sections.services.images[], sections.gallery.items[].src, gallery[], staff[].photoUrl, branding.localPhoto, branding.localPhotoMobile, branding.texture) a `clients/test-b4-peluqueria-<p>/media/<rol>/<nombre>` con rol hero|services|gallery|staff|branding, reescribe en el fixture de T cada referencia por su url (todas las apariciones del mismo archivo, la misma url), imprime una tabla archivo · rol · bytes · url y «subidos N · iguales M · reescritos K», sale 2 si un archivo referenciado no existe en disco; sólo ids con prefijo test-b4-peluqueria; con --bucket-falso <dir> escribe los archivos ahí y produce urls con bucket `falso` (para pruebas, sin red) · fuente: T:dev-fixtures/peluqueria-paleta-a.json (37 referencias) y -c (32) (revisor); T:.gitignore:50 (`dev-fixtures/media/` no está en git: hoy el material vive sólo en el disco de Liam); base (recrear A 25 y C 20 brechas «material que producción no sirve»); orden:11 (V5).

## B · La plantilla sirve ese material en producción

- [B1] (T) La CSP de server.ts, api/index.ts y vercel.json lleva `media-src 'self' https://firebasestorage.googleapis.com blob:`, y una respuesta de server.ts levantado con PORT libre trae esa directiva en el encabezado Content-Security-Policy · fuente: T:server.ts:556–560 (`default-src 'self'` sin `media-src`: un `<video>` desde Storage queda bloqueado), T:api/index.ts:460–466, T:vercel.json:16; T:src/types.ts:541–542 (`<source media="(orientation: portrait)">`).
- [B2] (T) applyHeroClip (VITE_HERO_CLIP) sigue funcionando con fixtures que apuntan a Storage: con VITE_HERO_CLIP=<c> el hero pasa a `/dev-fixtures/media/paleta-<p>/hero-<c>{,-1280,-poster,-v,-v-poster}.{mp4,webm,avif}` (local, sólo dev) aunque el fixture tenga urls de Storage; sin la variable no cambia nada; los casos de tests/material.test.ts:26–33 siguen iguales · fuente: T:src/services/tenant.ts:179–182 (regex sobre `/hero…"`, que una url codificada no cumple); T:tools/material/ver-paleta.ps1 (`-Clip`, el visor de Liam en Android).
- [B3] (T) gama.mjs mide un fixture cuyas rutas son urls de Storage resolviendo cada url a su archivo local `dev-fixtures/media/paleta-<p>/<nombre>` por el nombre del final del path, y da para A y C exactamente lo mismo que con las rutas locales (misma tabla, mismo total) · fuente: T:tools/gama.mjs:112–124 (`local()` acepta `/dev-fixtures/…`, absoluta o url); base (A 17/22, C 15/22 al cierre de GALERIA-05 / SERVICES-02).

## C · El material de A y C, por la puerta, y la promesa medida

- [C1] (T) dev-fixtures/peluqueria-paleta-a.json y -c.json no contienen ninguna referencia a /dev-fixtures/media/; las 37 y 32 referencias de la línea base son urls `https://firebasestorage.googleapis.com/v0/b/…/o/clients%2Ftest-b4-peluqueria-<p>%2Fmedia%2F…?alt=media&token=…`; y cada url distinta responde 200 a un GET con `Range: bytes=0-0` y el content-type de su extensión · fuente: base (25 + 20 brechas de material); HOJA-05 D-18; A3.
- [C2] (T) hueco.mjs --json da material «sí» en hero.video, hero.video.portrait, hero.video.poster, staff.photoUrl, services.images, gallery.items, branding.texture, branding.localPhoto y branding.localPhotoMobile (nueve filas, vive=storage), y el total pasa de 2/36 a N/36 con N ≥ 2 (las nueve siguen sin UI: no cuentan como hechas) · fuente: base (12 «producción no sirve dev-fixtures/media»); T:verdad/contratos.json (vive=storage en las once filas de material; brand.logo y brand.logoDark no están en el fixture A).
- [C3] (T) recrear.mjs --paleta a --sin-firestore --paginas home --vistas 375 --puerto <libre> no produce ninguna brecha «material que producción no sirve» ni registra petición alguna a /dev-fixtures/media/; las brechas que queden son de otro tipo (diff, sin contrato) · fuente: base (A: 31 brechas = 25 material + 6 diff); HOJA-05 C1.

## D · Retiro de verdad-05

- [D1] (T+H) verdad-05 figura en tests/orden/APROBADAS.md con fecha 2026-09-21, T 30b7daa y H 8239e2d, y rojo-verde --todas en HEAD imprime «verdad-05 · retirada (aprobada 2026-09-21)» y no «orden verdad-05 ·» · fuente: chat 2026-09-21 («Aprobado»); VERDAD-03 A1/A2.
- [D2] (T+H) npm test corre las copias tests/verdad-05-a.test.ts, -b, -c, -d, -e en T (9 tests) y tests/verdad-05-c.test.ts, -d, -e en H (4 tests), que importan ./orden/verdad-05/_util.ts; en la copia c de T, C1 conserva sólo las funciones puras y C2 sólo la lectura de PORT en server.ts, sin levantar recrear (D-19); pasan 9/9 y 4/4; tests/orden/verdad-05/ no cambia desde su último rojo · fuente: VERDAD-04/05 D2 (patrón); HOJA-05 D-19.

## E · Registro

- [E1] (T+H) CLAUDE.md § Arquitectura mínima dice dónde vive el material de un cliente (`clients/<id>/media/<rol>/` en Storage, url con token, por `subirMaterial` en H) y, en T, que la CSP permite `media-src` desde firebasestorage; y el bloque CONTRATO-DECLARADO no cambia · fuente: PLAN.md:23 (e: registro); H:CLAUDE.md § Arquitectura mínima; T:CLAUDE.md § Arquitectura mínima.
- Sin test (Nichos, sin repo): PLAN.md § Estado fila 4: «VERDAD-05 aprobada por Liam 2026-09-21 (T 30b7daa, H 8239e2d); CONEXION-01 …»; `CONTRATOS-HUECOS.md` columna «Lo rellena»/«Valida» de las once filas de material: «Storage vía `subirMaterial` (CONEXION-01)»; `MATERIAL-PIPELINE.md` § entrega: el paso final es `b4-material.ts` (o la casilla del hub, orden siguiente), no copiar a `dev-fixtures/media`; `4.3.md` § CONEXION-01.

## Interfaz de prueba (contrato entre los tests de A y el código de B)
`subirMaterial(entrada, deps = { bucket: getStorageBucket() })`: `deps.bucket` con la forma mínima de `@google-cloud/storage` que use la función (`file(path).save(buffer, {metadata})`, `.setMetadata`, `.exists`); los tests pasan un bucket en memoria. `resolveContentType(nombre, tipo)` exportada. `b4-material.ts`: `--fixture` y `--media` sustituyen el fixture y la carpeta de T sólo para pruebas; `--bucket-falso <dir>` guarda en disco y devuelve urls con bucket `falso` y el mismo token por contenido; la reescritura del fixture es en el archivo que `--fixture` señala. CSP: la directiva exacta `media-src 'self' https://firebasestorage.googleapis.com blob:`. `applyHeroClip(fixtureJson, clip)` acepta rutas locales y urls de Storage. `gama.mjs`: url → `dev-fixtures/media/paleta-<p>/<nombre>` por el sufijo `clients%2Ftest-b4-peluqueria-<p>%2Fmedia%2F<rol>%2F<nombre>`. Copias de verdad-05 en `tests/verdad-05-<letra>.test.ts`.

## Fuera de esta hoja
La casilla del hub para cargar cada hueco (CONEXION-02, empezando por el hero: vídeo 16:9 + 9:16 + pósteres, con `subirMaterial` detrás y prueba Playwright sobre el hub); los seis campos que no están en el fixture A (`branding.mode`, `contact.phone`, `services.featured`, `brand.logo`, `brand.logoDark`, `features.themeToggle`) y los guards que faltan (CONEXION-03); `gama.mjs` como validador al subir (bloque 5.1); el diff de 36 px con `VITE_CLIENT_ID` y los 1300 px de ancho de la home de A en 375 (AUDITORIA-01 / diseño); VERDAD-06.

## Notas para las sesiones A y B
A: A1 con un bucket en memoria (sin red); A2 por lectura del fuente y `resolveContentType`; A3 sobre una copia temporal de un fixture mínimo y una carpeta `--media` temporal con archivos pequeños, `--bucket-falso`; B1 lee los tres fuentes y levanta `server.ts` con PORT libre (como recrear) para leer el encabezado; B2 por el mismo import que tests/material.test.ts; B3 con `gama.mjs` sobre un fixture temporal de dos archivos, en local y en url; C1 lee los fixtures reales y hace los GET (≤ 10 s cada uno, cinco a la vez); C2 y C3 sobre lo real (C3 reutiliza el e2e de VERDAD-05 C1 con `--sin-firestore`). **Ningún test escribe en Storage ni en Firestore.** Prefijo temporal «conexion-01-», `finally`, inciso j). Al commit rojo, todos los tests de A fallan. B: sube A y C con `b4-material.ts` (D-20) y después corre `recrear --paleta a` y `--paleta c` completos con Firestore (D-16) y registra la línea base nueva (brechas de material esperadas: 0; diffs: lo que dé); `server.ts`, `api/index.ts` y `vercel.json` son infraestructura de la flota: el cambio es una directiva y se declara con `git diff`; los archivos alternativos de `-Clip` siguen en local, no se suben.
