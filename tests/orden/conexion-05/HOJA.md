# HOJA · CONEXION-05 · fondo y branding en el constructor del hub (cuatro huecos más: 14/36 → 18/36); retiro de VERDAD-09 · 2026-09-22

Escrita por el revisor desde la línea base de CONEXION-04 (aprobada 2026-09-22, T 4a22d7b · H f8bd9bb) y VERDAD-09 (aprobada 2026-09-22, T 49d5121 · H b7952bf). Estado esperado al abrir A: T 49d5121 = origin, H b7952bf = origin.
Fuentes: `H:`/`T:` = archivos en HEAD; `revisor` = lecturas del 2026-09-22 con salida citada; `hueco` = `T tools/verdad/hueco.mjs --json` en HEAD; `CH` = bloque-04/CONTRATOS-HUECOS.md.
Formato: cada afirmación es una línea `- [ID] (repo) frase · fuente: …`. El nombre del test es la frase exacta entre `(repo) ` y ` · fuente:`. `(T)` sólo en T; `(H)` sólo en H; `(T+H)` en los dos.
Circuito: PLAN.md § Cómo se trabaja, incisos i) a n). Decisiones vigentes: D-1..D-5, D-9..D-11, D-13, D-14, D-16..D-63. Decisiones para esta hoja (revisor, 2026-09-22, tomadas salvo que Liam diga lo contrario): D-64 `branding.heroToBackdrop` queda fuera de esta hoja: es un derivado que calcula `T tools/material/transicion.mjs` desde el vídeo del hero y la foto del local (CH:155 lo llama «derivado»); tendrá su casilla cuando el cálculo viva en H (H ya tiene `sharp`), en una orden propia · D-65 el fixture A gana `branding.mode: "light"` (es su modo efectivo: `getNicheDefaultMode` devuelve `light` para peluquería sin `mode`; sin cambio en la página) para que la fila tenga material; C ya tiene `dark` · D-66 nombres fijos del material de fondo: `textura.<ext>`, `local.<ext>`, `local-v.<ext>` (jpg, webp, avif, png), rol `branding`: misma entrada, misma url que `b4-material.ts` · D-67 casilla nueva `src/components/config-editors/fondo-editor.tsx`, sólo con `niche === "peluqueria"` (patrón D-35/D-46/D-47); `global-style-editor` (flags globales de la flota) no cambia · D-68 VERDAD-09 se promueve entera (T a, b, c, d = 7 tests; H ídem).

Por qué: cinco filas de fondo y branding tienen contrato y validador pero no casilla: `branding.mode`, `branding.texture`, `branding.localPhoto`, `branding.localPhotoMobile`, `branding.heroToBackdrop`; además `branding.mode` no está en el fixture A (material «no»), `branding.texture` y `branding.localPhotoMobile` no tienen guard que nombre su clave, y `heroToBackdrop` es un derivado (D-64). Con esta orden `hueco.mjs` pasa de 14/36 a 18/36. El material ya vive en Storage bajo `clients/<id>/media/branding/{textura,local,local-v}.jpg` (CONEXION-01).

## A · La casilla (H)

- [A1] (H) src/components/config-editors/fondo-editor.tsx exporta funciones puras que devuelven un config nuevo sin tocar otras claves: `aplicarModo(config, valor)` sobre `branding.mode` («light» | «dark»; vacío borra la clave); `aplicarFondo(config, campo, url)` con campo `texture`, `localPhoto` o `localPhotoMobile` sobre `branding.<campo>` (url vacía borra la clave); y `nombreFondo(campo, nombreArchivo)` = `textura.<ext>` | `local.<ext>` | `local-v.<ext>` (D-66). Aplicadas sobre el fixture A sin `branding.mode`, `branding.texture`, `branding.localPhoto` ni `branding.localPhotoMobile`, con los valores de A (T dev-fixtures/peluqueria-paleta-a.json, con D-65), devuelven un `branding` igual al de A (deepEqual: `colors`, `paletteMeta` y `heroToBackdrop` intactos) · fuente: revisor (A: `branding` = localPhoto, localPhotoMobile, heroToBackdrop, colors, paletteMeta, texture; C además `mode: "dark"`; urls bajo `clients/test-b4-peluqueria-a/media/branding/textura.jpg`, `local.jpg`, `local-v.jpg`); D-65, D-66.
- [A2] (H) `renderToString(<FondoEditor …>)` con `niche="peluqueria"` y el `branding` de A muestra el select `mode` (con `light` y `dark`, y el valor de A), los tres campos de foto con su url actual (`texture`, `localPhoto`, `localPhotoMobile`) y, sólo lectura, la relación `heroToBackdrop` actual (`relation`, `mechanism` y `foot.hex` de A) con la nota «se calcula con transicion.mjs (D-64)»; con `niche="barberia"` devuelve cadena vacía (D-67); y src/components/client-config-tab.tsx monta `FondoEditor` pasando `niche` y escribe en el mismo `config` que guarda por `/api/config/${clientId}` · fuente: H:src/lib/config-validator.ts:844–847 (mode light|dark; texture url); H:src/components/client-config-tab.tsx (patrón de las casillas de hero, servicios y galería); D-34 (render en servidor); D-64.
- [A3] (H) cada foto de fondo sube por `/api/upload/${clientId}` con `rol` = `branding` y el archivo renombrado a `nombreFondo(campo, nombre)` (`formularioFondo(archivo, campo)` devuelve el FormData; lanza si el tipo no es image/jpeg, png, webp o avif); el path que `subirMaterial` produce para cada uno de los tres campos (`clients/<id>/media/branding/<nombreFondo>`) es igual al path decodificado de la url de ese campo en el fixture A (tres de tres) · fuente: CONEXION-01 A1 (path `clients/<id>/media/<rol>/<nombre saneado>`); revisor (nombres de A: textura.jpg, local.jpg, local-v.jpg).
- [A4] (H) `scripts/b4-tenant.ts show --id test-b4-peluqueria-a` (con Firestore, sólo lectura) devuelve un `config/{id}` cuyo `branding` es igual al que A1 construye desde el fixture A (con `mode: "light"`, D-65) · fuente: CONEXION-03/04 (A4: mismo patrón); D-16 (`show` no escribe; el tenant A lo reescribe B con `recrear --paleta a` con Firestore tras D-65); inciso n).

## B · Contrato, validador, guard y material

- [B1] (T) verdad/contratos.json y CH: `ui` en `branding.mode`, `branding.texture`, `branding.localPhoto` y `branding.localPhotoMobile` = `{ ruta: "/clients/[clientId]", componente: "src/components/config-editors/fondo-editor.tsx", campo: "mode" | "texture" | "localPhoto" | "localPhotoMobile" }`; `guard` de `branding.localPhotoMobile` = `{ archivo: "tests/hero-viewport.test.ts", clave: "localPhotoMobile" }`; `branding.heroToBackdrop` sin `ui` (D-64) y con `tipo` «derivado (transicion.mjs)»; en CH las filas de `branding.texture` (:100 y :156), `branding.localPhoto` (:147), `branding.localPhotoMobile` (:148) y `palette.mode → branding.mode` (:157) ganan la nota «casilla de fondo (CONEXION-05)»; las otras 31 filas del .json byte a byte como en 49d5121 · fuente: T:verdad/contratos.json (hoy `ui: null` en las cinco; `guard: null` en localPhotoMobile y heroToBackdrop); hueco («tests/galeria-03.test.ts no nombra «texture»», «sin guard»).
- [B2] (H) `validateReplanteoHuecos` da **error** (no aviso) por cada `branding.texture`, `branding.localPhoto` o `branding.localPhotoMobile` presente que no empiece por `https://` («producción no sirve rutas locales», nombra la clave), además de sus reglas actuales (par local/localMobile, mode light|dark, heroToBackdrop cuando hay vídeo y foto); los fixtures A (con D-65) y C pasan sin errores; un config con `branding.localPhoto = "/dev-fixtures/media/paleta-a/local.jpg"` es rechazado con path `branding.localPhoto` · fuente: H:src/lib/config-validator.ts:826–847 (`isUrl` acepta rutas); CONEXION-02/03/04 (misma regla para hero, servicios y galería).
- [B3] (T) tests/galeria-03.test.ts nombra literalmente «texture» y tests/hero-viewport.test.ts nombra literalmente «localPhotoMobile» (en el caso de 375, donde la capa fija monta la foto vertical) sin cambiar lo que afirman; `hueco.mjs --json` da las cinco casillas en «sí» y `hecho: true` en `branding.mode`, `branding.texture`, `branding.localPhoto` y `branding.localPhotoMobile`, y el total es «18/36 huecos hechos»; `branding.heroToBackdrop` sigue sin hacer (D-64) · fuente: hueco (hoy 14/36); T:verdad/contratos.json (guard.clave «texture», «local-backdrop»); T:tests/hero-viewport.test.ts:71 (nombra `localPhoto`, no la vertical); D-11 (guards nombrados: galeria-03, hero-viewport).
- [B4] (T) dev-fixtures/peluqueria-paleta-a.json tiene `branding.mode` = `"light"` y nada más cambia en los fixtures (`git diff 49d5121 HEAD --stat -- dev-fixtures/` = una línea en A); `recrear.mjs --paleta a --sin-firestore --paginas home --vistas 375 --puerto <libre>` y `--paleta c` no producen ninguna brecha «validador H rechaza» ni «material que producción no sirve», y las brechas totales no superan la línea base (A ≤ 1, C ≤ 2): D-65 no cambia la página · fuente: T:src/lib/site-theme.ts:84–91 (`getNicheDefaultMode`: peluquería sin `mode` → `light`); CONEXION-04-B (línea base A 1 diff, C 2); D-65.

## D · Retiro de VERDAD-09 (D-68)

- [D1] (T+H) verdad-09 figura en tests/orden/APROBADAS.md con fecha 2026-09-22, T 49d5121 y H b7952bf, y rojo-verde --todas en HEAD imprime «verdad-09 · retirada (aprobada 2026-09-22)» y no «orden verdad-09 ·» · fuente: aprobación de Liam 2026-09-22.
- [D2] (T+H) npm test corre las copias tests/verdad-09-a.test.ts, -b, -c, -d en T y en H (7 tests cada uno), que importan ./orden/verdad-09/_comun.ts, sin recorte y sin volver a correr copias anteriores; en T van a `test:unit`; tests/orden/verdad-09/ no cambia desde su último rojo (753e027 en T, 7482ad4 en H) · fuente: VERDAD-09 (aprobada); D-68.

## E · Registro

- [E1] (T+H) CLAUDE.md § Arquitectura mínima de H nombra `fondo-editor` y las cuatro casillas (modo, textura, foto del local escritorio y móvil) y que `heroToBackdrop` se calcula con `transicion.mjs` (D-64); CLAUDE.md § Puertas de T y de H tiene un párrafo CONEXION-05 que nombra «18/36»; el bloque CONTRATO-DECLARADO es idéntico al del commit aprobado de VERDAD-09 (T 49d5121 / H b7952bf) · fuente: E1 anteriores.
- Sin test (Nichos): PLAN.md § Estado fila 4 «CONEXION-05-A (2026-09-22): hoja + 11 afirmaciones (H 8 tests: A1–A4, B2, D1, D2, E1; T 6: B1, B3, B4, D1, D2, E1), tests rojos (T <sha>, H <sha>); B pendiente del «andá»»; CH: las cinco filas y § Constructor («24/36 con UI» → «28/36»); `4.3.md` § CONEXION-05 · A.

## Interfaz de prueba (contrato entre los tests de A y el código de B)

Como en CONEXION-04: `node --experimental-strip-types --test`; el cargador de `.tsx`/`@/x`/sin extensión se copia (no se importa) de `tests/orden/conexion-04/_util.ts`; los imports de `tools/` sólo con `import()` dinámico dentro del test; ninguna afirmación cuenta órdenes ni `_util.ts` (inciso m); el editor se importa con extensión y no puede necesitar `next/*` al cargar. A1: `existsSync` del archivo y «exporta <función>» antes de usar cada función; deepEqual contra A leído de T por ruta fija con `mode: "light"` añadido en memoria mientras D-65 no esté en disco (el test lo declara: `A.branding.mode ??= "light"`). A2: `renderToString` con las props que la pestaña le pasa; comparación por `includes` de claves literales (`mode`, `light`, `dark`, `texture`, `localPhoto`, `localPhotoMobile`, las tres urls, `same-hue`, `veil-from-first-pixel`, `#827f82`, «transicion.mjs»); con barbería `=== ""`. A3: `File` de Node; paths decodificando `/o/<path>?`. A4: `show` real (JSON entero, `.env.local` presente; si falta, falla nombrándolo); rojo por «no exporta» antes de `show`. B1 lee el .json (fila por id) y el .md (fila por campo). B2 importa el validador con extensión. B3 lee los dos guards por texto y corre `hueco.mjs --json` real. B4 lee el fixture en disco y corre recrear real (`--sin-firestore`, prefijo «conexion-05-», ≤ 10 min; el test exige ANTES que el fixture en disco tenga `mode`, D-65: hoy cae ahí). D1 y D2 como en las hojas anteriores. E1 lee disco y compara el bloque por `git show`.
Ningún test escribe en T ni en H fuera de `tests/orden/conexion-05/`; ninguno escribe en Storage ni en Firestore (`show` sólo lee; el tenant A lo reescribe B con `recrear --paleta a` con Firestore, D-16); `_util.ts`, `d`, `e` idénticos T/H por `cmp`; `a` sólo en H; `b` en los dos por `REPO`; no hay `c`. · ajuste de A (2026-09-22): el archivo de utilidades de ESTA orden se llama `_comun.ts` y no `_util.ts` (lo idéntico T/H por `cmp` es `_comun.ts`). Medido: con `tests/orden/conexion-05/_util.ts` en el árbol, la copia promovida `tests/verdad-08-a.test.ts` (A2 de verdad-08, que recorre todos los `tests/orden/*/_util.ts` y le exige a cada uno su commit rojo) cae en el pre-commit del propio commit rojo —«tests/orden/conexion-05/ tiene commit rojo», con HEAD todavía en el commit anterior— y esta orden deja fuera de alcance tocar esa copia. El nombre distinto deja intactos los `_util.ts` congelados, no toca ninguna frase (ninguna afirmación lo nombra) y mantiene los catorce tests en rojo; es el mismo camino que tomó VERDAD-09 con su `_comun.ts` y por la misma causa.

## Notas para las sesiones A y B

A: al commit rojo, los catorce tests (11 afirmaciones; H 8, T 6; D1, D2 y E1 en los dos) fallan por su primera aserción: A1–A4 por «no existe fondo-editor.tsx»; B1 por `ui: null`; B2 por `localPhoto` local sin error; B3 por «no nombra «texture»» / 14/36; B4 por «el fixture A no tiene branding.mode»; D1 por la línea ausente; D2 por copias ausentes; E1 por el párrafo. Nombre del test = frase de la hoja (A5).
B: la casilla vive en `src/components/config-editors/fondo-editor.tsx`, se monta en la pestaña Config junto a las de hero, servicios y galería, sólo con `niche === "peluqueria"`; escribe con las puras de A1; las fotos suben con `ImageUploadField` si sirve (rol `branding`, nombre fijo); `heroToBackdrop` se muestra, no se edita (D-64). D-65 va al fixture A antes de `recrear`; después `recrear --paleta a` **con Firestore** (D-16) para que el tenant A tenga `mode` y A4 pase (C no cambia). `global-style-editor.tsx` no cambia. La línea de APROBADAS.md de verdad-09 la escribe B (D1).

## Fuera de esta hoja

`branding.heroToBackdrop` como casilla (D-64: cuando el cálculo de `transicion.mjs` viva en H); `paleta` (`branding.colors`, sin validador declarado: CONEXION-06 con `derivePalette`); `hero.eyebrow`, `hero.mask`, `hero.alto`; las filas con casilla pero sin guard o sin material (`brand.logo`, `brand.logoDark`, `hero.titular/subtitle/cta`, `staff.photoUrl`, `contact.phone`, `features.themeToggle`: CONEXION-07); gama al subir (5.1); AUDITORIA-01 (flakes de navegador, diffs de 36 px).
