# HOJA · CONEXION-07 · guards de las seis filas que ya tienen casilla (20/36 → 26/36); retiro de CONEXION-06 · 2026-09-23

Escrita por el revisor desde la línea base de CONEXION-06 (aprobada 2026-09-23, T b5b78f7 · H 07739a1). Estado esperado al abrir A: T b5b78f7 = origin, H 07739a1 = origin.
Fuentes: `H:`/`T:` = archivos en HEAD; `revisor` = lecturas del 2026-09-23 con salida citada; `hueco` = `T tools/verdad/hueco.mjs --json` en HEAD; `CH` = bloque-04/CONTRATOS-HUECOS.md.
Formato: cada afirmación es una línea `- [ID] (repo) frase · fuente: …`. El nombre del test es la frase exacta entre `(repo) ` y ` · fuente:`. `(T)` sólo en T; `(H)` sólo en H; `(T+H)` en los dos.
Circuito: PLAN.md § Cómo se trabaja, incisos i) a n). Decisiones vigentes: D-1..D-5, D-9..D-11, D-13, D-14, D-16..D-74. Decisiones para esta hoja (revisor, 2026-09-23, tomadas salvo que Liam diga lo contrario): D-75 esta orden cierra sólo las filas a las que les falta **únicamente** el guard (hueco: `hero.titular`, `hero.subtitle`, `hero.cta`, `testimonials.rating`, `staff.photoUrl`, `navbar.variant`); las que además no tienen material en el fixture A (`contact.phone`, `brand.logo`, `brand.logoDark`, `features.themeToggle`) necesitan decisiones de Liam y van en CONEXION-08 · D-76 un guard protege lo que la página de peluquería hace con la clave, no sólo la nombra: se prueba la función pura cuando la hay (`clampWords`, `warnWords` de `src/lib/words.ts`, el resolvedor de variantes) y, cuando la clave sólo se pinta, la lectura del componente que la página monta (`Team.tsx`, `Testimonials.tsx`, `Navbar.tsx` en su camino por defecto o `v6`) · D-77 los guards nuevos viven en dos archivos de T en `test:unit`: `tests/hero-textos.test.ts` (ya existe, CONEXION-06) gana titular, subtítulo y CTA; `tests/secciones-datos.test.ts` (nuevo) cubre `rating`, `photoUrl` y `navbar.variant` · D-78 CONEXION-06 se promueve entera (H a, d, e = 9; T b, d, e = 5).

Por qué: seis filas tienen contrato, validador, casilla y material pero ningún guard en `npm test` (hueco: «guard: sin guard» y nada más): si el hero v6 dejara de recortar el subtítulo, si la sección del equipo dejara de pintar la foto o si el navbar de peluquería dejara de ser el v6, ningún test de T se enteraría. Los cinco lugares de cada fila están medidos salvo éste. Con esta orden `hueco.mjs` pasa de 20/36 a 26/36; quedan cinco derivados sin casilla por definición (`hero.mask`, `hero.alto`, `pagina.servicios`, `gallery.presion`, `pagina.galeria`), `branding.heroToBackdrop` (D-64) y las cuatro de CONEXION-08.

## A · Guards de los textos del hero (T)

- [A1] (T) tests/hero-textos.test.ts nombra literalmente «titleHighlight», «subtitle» y «ctaPrimary», y afirma: que `clampWords` recorta un subtítulo de 14 palabras a las 12 primeras y deja intacto uno de 12, y que hero-v6.tsx declara `subtitle: 12` en `LIMITS` y llama `clampWords(hero.subtitle, LIMITS.subtitle, "subtitle")`; que `warnWords` (con `import.meta.env.DEV` verdadero inyectado por el test) avisa con un titular de 1 y de 7 palabras y calla con uno de 4, y que hero-v6.tsx lo llama sobre `titlePrefix`, `titleHighlight` y `titleSuffix` con 2 y 6; y que `warnWords` avisa con un CTA de 3 palabras y calla con uno de 2, y que hero-v6.tsx lo llama sobre `hero.ctaPrimary` con 1 y 2 · fuente: T:src/components/landing/hero/hero-v6.tsx:32, :179, :182–184; T:src/lib/words.ts:2–6, :29–32 (`warnWords` sólo avisa en DEV: el test lo prueba espiando `console.warn` con la marca inyectada); hueco («hero.titular/subtitle/cta · guard: sin guard»); D-76, D-77.

## B · Guards de equipo, testimonios y navbar (T)

- [B1] (T) tests/secciones-datos.test.ts existe, está en `test:unit` de package.json y nombra literalmente «photoUrl», «rating» y «navbar»; afirma que `src/components/landing/Team.tsx`, en el camino que monta la página de peluquería (sin `teamVariant` ni variante de estética), pinta la foto de cada miembro con `src={…photoUrl}`, y que el fixture A tiene tres miembros con `photoUrl` https; que `src/components/landing/Testimonials.tsx`, en su camino por defecto, pinta tantas estrellas como `review.rating` (`[...Array(review.rating)]`) y que las ocho reseñas del fixture A tienen `rating` entre 1 y 5; y que `src/components/layout/Navbar.tsx` resuelve `navbar.variant: "v6"` a `navbar-v6` por `pickVariantModule` y que el fixture A declara `navbar.variant: "v6"` · fuente: T:src/components/landing/Team.tsx:92, :347; T:src/components/landing/Testimonials.tsx:226; T:src/components/layout/Navbar.tsx:22, :38; revisor (fixture A: staff ×3 con photoUrl en Storage, testimonials ×8 con rating 5, navbar {variant: "v6"}); D-76, D-77.

## C · Contrato y línea base (T)

- [C1] (T) verdad/contratos.json declara `guard` en las seis filas: `hero.titular` = `{ archivo: "tests/hero-textos.test.ts", clave: "titleHighlight" }`, `hero.subtitle` = `… "subtitle"`, `hero.cta` = `… "ctaPrimary"`, `testimonials.rating` = `{ archivo: "tests/secciones-datos.test.ts", clave: "rating" }`, `staff.photoUrl` = `… "photoUrl"`, `navbar.variant` = `… "navbar"`; CH gana la nota «guard (CONEXION-07)» en la línea donde vive el contrato de cada una de las seis; las otras 30 filas del .json byte a byte como en b5b78f7; y `hueco.mjs --json` da las cinco casillas en «sí» y `hecho: true` en las seis, y el total es «26/36 huecos hechos» · fuente: T:verdad/contratos.json (hoy `guard: null` en las seis); hueco (hoy 20/36); D-75; **ajuste de A** (2026-09-23): la frase decía «CH gana la nota «guard (CONEXION-07)» en esas seis filas», pero sólo cinco son filas de tabla — el contrato de `staff.photoUrl` (`staff[].photoUrl`) vive en el párrafo de § Gama de color (CONTRATOS-HUECOS.md:141), no en una fila; se exige la nota en la línea donde vive cada contrato, que es la misma que mide `hueco.mjs`.

## D · Retiro de CONEXION-06 (D-78)

- [D1] (T+H) conexion-06 figura en tests/orden/APROBADAS.md con fecha 2026-09-23, T b5b78f7 y H 07739a1, y rojo-verde --todas en HEAD imprime «conexion-06 · retirada (aprobada 2026-09-23)» y no «orden conexion-06 ·» · fuente: aprobación de Liam 2026-09-23.
- [D2] (T+H) npm test corre las copias tests/conexion-06-a.test.ts, -d, -e en H (9 tests) y tests/conexion-06-b.test.ts, -d, -e en T (5 tests), que importan ./orden/conexion-06/_comun.ts, sin recorte y sin volver a correr copias anteriores; en T van a `test:unit`; tests/orden/conexion-06/ no cambia desde su último rojo (014523d en T, 6ad11a4 en H) · fuente: CONEXION-06 (aprobada); D-78.

## E · Registro

- [E1] (T+H) CLAUDE.md § Puertas de T y de H tiene un párrafo CONEXION-07 que nombra «26/36», «hero-textos» y «secciones-datos»; el bloque CONTRATO-DECLARADO es idéntico al del commit aprobado de CONEXION-06 (T b5b78f7 / H 07739a1) · fuente: E1 anteriores.
- Sin test (Nichos): PLAN.md § Estado fila 4 «CONEXION-07-A (2026-09-23): hoja + 6 afirmaciones (T 6 tests: A1, B1, C1, D1, D2, E1; H 3: D1, D2, E1), tests rojos (T <sha>, H <sha>); B pendiente del «andá»»; CH: las seis filas y § Constructor; `4.3.md` § CONEXION-07 · A.

## Interfaz de prueba (contrato entre los tests de A y el código de B)

Como en CONEXION-06: `node --experimental-strip-types --test`; utilidad `_comun.ts` (copia, no import, del cargador de `tests/orden/conexion-06/_comun.ts`, incluida la inyección de `import.meta.env`); imports de `tools/` y de `src/` sólo con `import()` dinámico dentro del test; ninguna afirmación cuenta órdenes (inciso m); cada afirmación en el repo cuyo árbol la pone en rojo (inciso n): A1, B1, C1 en T. A1 y B1: primero `existsSync` / lectura de los guards (`tests/hero-textos.test.ts` existe y hoy no nombra «subtitle»; `tests/secciones-datos.test.ts` no existe) → rojo por eso; después el test **corre el guard** (`node --test` anidado con `NODE_TEST_CONTEXT: undefined`, lección de CONEXION-06-A2) y exige exit 0 y los nombres de sus tests en el TAP; y mide él mismo, por su cuenta, los hechos que el guard afirma (las funciones puras con la marca `DEV` inyectada y un espía de `console.warn`; las líneas del fuente de hero-v6, Team, Testimonials y Navbar; los datos del fixture A). C1 lee el .json (fila por id) y el .md, y corre `hueco.mjs --json` real. D1 y D2 como en las hojas anteriores. E1 lee disco y compara el bloque por `git show`.
Ningún test escribe en T ni en H fuera de `tests/orden/conexion-07/`; ninguno toca Storage ni Firestore; `_comun.ts`, `d`, `e` idénticos T/H por `cmp`; `a`, `b`, `c` sólo en T; no hay tests de A en H salvo `d` y `e`.

## Notas para las sesiones A y B

A: al commit rojo, los nueve tests (6 afirmaciones; T 6, H 3) fallan por su primera aserción: A1 por «tests/hero-textos.test.ts no nombra «subtitle»»; B1 por «no existe tests/secciones-datos.test.ts»; C1 por `guard: null`; D1, D2, E1 como siempre. Nombre del test = frase de la hoja (A5).
B: en T, `tests/hero-textos.test.ts` gana los tres casos (no se reescribe lo de CONEXION-06) y `tests/secciones-datos.test.ts` nuevo va en `test:unit`; si leer `Team.tsx`/`Testimonials.tsx` por texto resulta frágil, B puede extraer la parte pura a una función exportada y probarla, siempre que la página siga igual (declarar). H sólo recibe APROBADAS.md, las copias y el párrafo de CLAUDE.md. Nada de código de producción del hub. Los fixtures no cambian; `recrear --paleta a --sin-firestore` una vez como no regresión. La línea de APROBADAS.md de conexion-06 la escribe B (D1).

## Fuera de esta hoja

CONEXION-08 (decisiones de Liam): `contact.phone` (el fixture A no tiene teléfono y el validador avisa que «consulta sin contact.phone» no tiene a dónde mandar la foto), `brand.logo` y `brand.logoDark` (A es «peluquería sin logo», camino A de SISTEMA-COLOR § 5), `features.themeToggle` (sin validador ni valor en el fixture); `branding.heroToBackdrop` cuando `transicion.mjs` viva en H (D-64); los cinco derivados; AUDITORIA-01.
