# HOJA · ARREGLOS-02 · la medición contra las webs REALES, estable; retiro de ARREGLOS-01 · 2026-09-25

Escrita por la **sesión A** (inciso p). Línea base: ARREGLOS-01 aprobada por Liam 2026-09-25, T d026b0f · H 951932c. Estado esperado al abrir A: T d026b0f = origin, H 951932c = origin, los dos limpios.
Fuentes: `H:`/`T:` = archivos en HEAD; `medido` = lectura de A del 2026-09-25 con su salida citada; `revisor` = la corrida en limpio de ARREGLOS-01; `Liam` = decisiones del 2026-09-25.
Formato: cada afirmación es una línea `- [ID] (repo) frase · fuente: …`. El nombre del test es la frase exacta entre `(repo) ` y ` · fuente:`. `(T)` sólo en T; `(H)` sólo en H; `(T+H)` en los dos.
Circuito: PLAN.md § Cómo se trabaja, incisos i) a p) y o-bis. Decisiones vigentes: D-1..D-5, D-9..D-11, D-13, D-14, D-16..D-112. Decisiones para esta hoja:

- **D-113 (Liam, 2026-09-25) ARREGLOS-01 aprobada.** T d026b0f · H 951932c. Esta orden la retira en `tests/orden/APROBADAS.md` y promueve sus copias a `npm test`; lo que salga a la red se recorta y se declara.
- **D-114 (Liam, 2026-09-25) lo que falta cerrar es la estabilidad contra las webs REALES.** ARREGLOS-01 dejó `asentar` esperando condiciones y el informe diciendo `estable` por zona, y su guard corre sobre una página local. Pero contra las dos webs desplegadas la medición **sigue sin poder afirmarse**: eso es lo que esta orden cierra, antes del diseño.
- **D-115 (A, medido) la causa: la primera rasterización de la pastilla de la navbar, que queda DENTRO de la captura.** Medido sobre las dos webs, a 1280, con `asentar` aplicado y cuatro capturas seguidas del mismo elemento: **la primera difiere de las tres siguientes**, que son idénticas entre sí. La diferencia es de **13 a 17 píxeles**, siempre en `x = 1150–1151` y en las filas `19–23` y `64–68` del elemento, con cambios de **±1 por canal** (web A, clara: `239,243,237 → 238,242,236`; web C, oscura: `37,31,28 → 36,31,28`). `document.elementFromPoint` sobre esos puntos da, en las dos webs, `div.relative.mx-3 < nav.fixed.inset-x-0 < div.min-h-screen.bg-background < div#root`, con **`backdrop-filter: blur(16px)`** y fondo `color(srgb … / 0.85)`. Es la pastilla de `T:src/components/layout/navbar/navbar-v6.tsx:162–168` (`mx-3 mt-3 h-14 lg:h-16 lg:mx-auto lg:max-w-5xl rounded-xl border … backdrop-blur-[16px]`): a 1280 va de x=128 a x=1152 y de y=12 a y=76, así que **x 1150–1151 con las filas 19–23 y 64–68 son exactamente las dos esquinas redondeadas de su borde derecho** — el antialias del recorte del desenfoque. Entra en la captura porque `#main-content` empieza en `y=0` y mide 1149 px (`/servicios`) y 1377 px (`/galeria`) con un viewport de 800: la barra fija se compone encima del elemento.
- **D-116 (A, medido) el arreglo: una captura de calentamiento, descartada, por lado y por zona.** Medido zona por zona en las dos webs a 1280, con tres candidatos: **base** (lo que hace `e2e.mjs` hoy: N capturas seguidas) falla en `services`, `gallery`, `pagina-servicios` y `pagina-galeria` de A y en `services`, `pagina-servicios` y `pagina-galeria` de C; **A** (una captura de calentamiento descartada + N) sale bien en las **24** entradas; **B** (`scrollIntoView` + quietud + calentamiento + N) también en las 24. Se toma **A**: es una captura de más y cubre los dos mecanismos medidos —la primera rasterización del desenfoque, y el scroll que la propia captura provoca (`scrollY` 0 → 803 en `#services`, que mueve la capa `sticky.local-backdrop-layer` de `y=800` a `y=0`)—. A **375 no pasa nunca**: las 12 entradas de las dos webs salen estables con o sin calentamiento.
- **D-117 (A, medido) techo: una página local NO reproduce la causa.** Ocho variantes probadas, todas con `asentar` aplicado y cuatro capturas: pastilla idéntica a la de navbar-v6 (`rounded-xl` 12 px, borde 1 px, `color-mix(in srgb, var(--background) 85%, transparent)`, `backdrop-filter: blur(16px)`, la misma `transition` de 240 ms), zona más alta que el viewport, zona bajo el pliegue, capa `sticky` a viewport completo (como `LocalBackdrop`), imagen decodificada detrás, `<video>`, fondo de ruido de alta frecuencia, y con y sin el desenfoque. **Las cuatro capturas salieron idénticas en las ocho.** Es el mismo techo que declaró la sesión B de ARREGLOS-01 con su guard local, y por eso la falsación de esta orden **no** va sobre una página: va sobre el protocolo de captura, con un elemento de prueba que reproduce la **forma** medida (la primera captura distinta) sin depender del compositor. La prueba contra lo real es A1, y es la que manda.
- **D-118 (A, medido) la repetición dentro de un lado no lo atrapa todo, por eso tres corridas.** En la corrida completa de `e2e.mjs --web a`, `pagina-galeria 375` dio **7 px de 440250 con las dos capturas de cada lado iguales**: una diferencia entre LADOS que `estable` no ve. Tres corridas acotadas a esa misma zona dieron 0 px 3/3, y el revisor no la vio en sus tres corridas completas: es intermitente y depende de la historia de la sesión del navegador. Por eso A1 exige **tres corridas** y no una, y por eso cada corrida abre su **propio** `chromium.launch()`.
- **D-119 (A) tres corridas, una sola construcción de la referencia.** La referencia es un `vite build` de T en el commit que Vercel desplegó (D-100): reconstruirla tres veces no mide nada nuevo, porque el commit no cambia. `e2e.mjs` gana `--corridas <n>`: construye una vez y repite la medición `n` veces, cada una con su navegador nuevo, y el informe dice a qué corrida pertenece cada entrada. Lo que se repite es lo que puede variar (la sesión del navegador, la carga de las páginas, la rasterización), no lo que no puede.
- **D-120 (A, medido) la afirmación de `rojo-verde --orden e2e-01` vive SÓLO en T.** Medido hoy: en T sale **2** y cae exactamente en C2 («tests que fallan en HEAD d026b0f»); en **H sale 0** con sus siete tests verdes, porque las afirmaciones de H (A1, A2, B1, B2, E1, E2, F1) no corren `e2e.mjs`. Una afirmación en H nacería verde, y eso no se hace (inciso n).
- **D-121 (A, medido) `suite-fases.test.ts` no ve un `import()` dinámico de playwright.** `T tests/suite-fases.test.ts:15` detecta la fase por el literal `from "playwright"`. `tests/orden/arreglos-01/f.test.ts` abre Chromium con `await import("playwright")`, así que su copia promovida **abre navegador y el guard de fases no lo sabe**. En esta orden se resuelve declarándolo en C2 (la copia va a `test:browser` y el test lo exige); que el guard lo vea es un pendiente, no un arreglo de esta hoja.

Por qué: ARREGLOS-01 arregló lo que se podía ver desde una página local y dejó el informe capaz de **decir** que una medición no se puede afirmar. Eso es lo que pasó: contra las webs reales, `e2e.mjs` sale 2 en las dos, la copia congelada de E2E-01 ya no pasa, y `rojo-verde --orden e2e-01` cae. Esta orden cierra la causa medida —no supuesta— y devuelve la comparación punta a punta a verde antes de empezar el diseño.

## A · La medición contra las webs reales (T)

- [A1] (T) tools/verdad/e2e.mjs acepta `--corridas <n>` y, con `--web a --corridas 3` y `--web c --corridas 3`, las 72 entradas zona·vista·corrida (12 por web y corrida) salen en 0 px, sin cambio de tamaño y con `estable` verdadero —ninguna «NO ESTABLE»—, los tokens de `:root` iguales en las dos vistas de cada corrida, y las dos invocaciones salen 0 · fuente: medido 2026-09-25 (`--web a`: `pagina-servicios 1280` y `pagina-galeria 1280` «NO ESTABLE (2 capturas distintas)» y `pagina-galeria 375` «7 px de 440250»; `--web c`: **exit 2** con las mismas dos zonas «NO ESTABLE»); revisor (tres corridas de `tests/orden/e2e-01/c.test.ts`, las tres con las mismas dos zonas); D-115, D-118, D-119.
- [A2] (T) tools/verdad/e2e.mjs exporta `capturasEstables(elemento, repeticiones)`, que pide `repeticiones + 1` capturas y descarta la primera: devuelve las últimas `repeticiones`, da `estable` verdadero cuando sólo la primera captura difiere —la forma medida en las dos webs— y falso cuando difiere una posterior · fuente: medido (candidato A: una captura de calentamiento descartada deja las 24 entradas de las dos webs iguales; el `base` de hoy falla en siete de ellas); T:tools/verdad/e2e.mjs:224–228 (`repetir`, que hoy no descarta nada); D-116, D-117.

## B · La copia congelada de E2E-01 vuelve a pasar (T)

- [B1] (T) rojo-verde --orden e2e-01 sale 0 en T, con la afirmación C2 de E2E-01 entre sus tests verdes y sin tocar tests/orden/e2e-01/ · fuente: medido 2026-09-25 (hoy sale **2**: «- e2e-01: tests que fallan en HEAD d026b0f:» seguido de la frase de C2; en H sale 0, D-120); D-114.

## C · Retiro y promoción de ARREGLOS-01 (D-113)

- [C1] (T+H) arreglos-01 figura en tests/orden/APROBADAS.md con fecha 2026-09-25, T d026b0f y H 951932c, y rojo-verde --todas en HEAD imprime «arreglos-01 · retirada (aprobada 2026-09-25)» y no «orden arreglos-01 ·» · fuente: aprobación de Liam 2026-09-25.
- [C2] (T+H) npm test corre las copias de arreglos-01 que su hoja promueve, que importan ./orden/arreglos-01/_comun.ts, sin volver a correr copias anteriores (la copia `d` excluye las órdenes vivas con `excluidas()`, sin nombrarlas); en T, toda copia que abra Chromium va a `test:browser` aunque importe playwright con `import()` dinámico; y tests/orden/arreglos-01/ no cambia desde su último rojo (ae2d5ac en T, 2df3e8f en H) · fuente: ARREGLOS-01 (aprobada); D-113, D-121.

## D · Registro

- [D1] (T+H) CLAUDE.md § Puertas de T y de H tiene un párrafo ARREGLOS-02 que nombra «calentamiento», «webs reales», «corridas» y «backdrop-filter»; el bloque CONTRATO-DECLARADO es idéntico al del commit aprobado de ARREGLOS-01 (T d026b0f / H 951932c) · fuente: C1 anteriores.
- Sin test (Nichos, sin git): PLAN.md § Estado fila 4 «ARREGLOS-02-A (2026-09-25): hoja + 6 afirmaciones (T 6 tests: A1, A2, B1, C1, C2, D1; H 3: C1, C2, D1), tests rojos (T <sha>, H <sha>); B pendiente del «andá»»; `4.3.md` § ARREGLOS-02 · A; y los tres pendientes que Liam pidió anotar sin hacerlos (el candado de la flota sobre el hermano y su permiso por archivo; la decisión sobre `PATCH`/`DELETE` y las rutas hijas de `/api/clients/[clientId]` por slug; AUDITORIA-01 con el fallo suelto de `tests/services-v6.test.ts`), más el cuarto que salió de medir esta orden (D-121, `suite-fases` y el `import()` dinámico).

## Interfaz de prueba (contrato entre los tests de A y el código de B)

Como en ARREGLOS-01: `node --experimental-strip-types --test`; `_comun.ts` es **copia**, no import, del de `tests/orden/arreglos-01/_comun.ts`, recortado a lo que esta orden usa; imports de `tools/` y de `src/` **sólo** con `import()` dinámico dentro del test; ninguna afirmación cuenta órdenes (inciso m); cada afirmación en el repo cuyo árbol la pone en rojo (inciso n): A1, A2 y B1 sólo en T (D-120). Comparación de objetos **por valor**, con las claves ordenadas. Ningún test escribe fuera de `tests/orden/arreglos-02/`, ni en Firestore, ni en Storage, ni en Vercel: a la red se sale **sólo a leer** (los GET que hace `e2e.mjs` contra los dos dominios). Toda carpeta temporal lleva el prefijo «arreglos-02-» y se borra en `finally`. `_comun.ts`, `d` y `e` idénticos T/H por `cmp`; `f` y `g` sólo en T.

**Primera aserción barata en A1, A2 y B1.** Las tres empiezan por la misma precondición estructural —que `e2e.mjs` exporte `capturasEstables` y acepte `--corridas`—, que hoy falla en milisegundos. Así el árbol rojo (y el clon neutro de `rojo-verde`) no paga los ~30 minutos de las corridas reales, que sólo se ejercen cuando B ya puso el verde.

**Lo que B escribe:**

| afirmación | archivo | interfaz |
|---|---|---|
| A1 | `T tools/verdad/e2e.mjs` | CLI `--corridas <n>` (1 por defecto); cada entrada de `zonas` y de `tokens` del informe lleva `corrida` (1..n); cada corrida abre su propio `chromium.launch()` sobre la **misma** referencia ya construida (D-119) |
| A2 | `T tools/verdad/e2e.mjs` | `export async function capturasEstables(elemento, repeticiones)` → `{ capturas, estable }`: pide `repeticiones + 1` capturas, descarta la primera, devuelve las últimas `repeticiones` y compara sólo ésas. `repetir()` se va o se apoya en ella |
| B1 | — | no hay código nuevo: sale de A1 y A2 |

**El elemento de prueba de A2** (la forma medida, sin el compositor): un objeto con `screenshot()` que devuelve un buffer distinto en la llamada **1** y el mismo de la **2** en adelante → `estable: true` y `capturas.length === repeticiones`; y otro que cambia en la llamada **3** (la segunda medida) → `estable: false`. La segunda dirección es la falsación: si `capturasEstables` devolviera `true` siempre, cae ahí.

**El costo de A1, declarado.** Dos invocaciones de `e2e.mjs` con tres corridas cada una: ~30 minutos de reloj, con dos `vite build` y 72 cargas de página. Es la única afirmación de esta hoja que sale a la red. Al promoverse, su copia se recorta (D-89, D-95): queda la parte estructural y el informe no se vuelve a medir en cada `npm test`.

## Notas para las sesiones A y B

A: al commit rojo los seis tests fallan por su primera aserción — A1, A2 y B1 por «tools/verdad/e2e.mjs no exporta `capturasEstables`»; C1 por la línea que falta en `APROBADAS.md`; C2 por «falta tests/arreglos-01-d.test.ts»; D1 por el párrafo ARREGLOS-02 que no está. Nombre del test = frase de la hoja.

B: **los arreglos de código de producción van en commits propios, separados de tests, copias y registro.** No hace falta ningún permiso de Liam: esta orden no toca la flota, ni el hub, ni las fichas, ni las dos webs, ni Firestore, ni Vercel — sólo `T tools/verdad/e2e.mjs`, las copias y el registro. Orden sugerido: (1) `capturasEstables` y `--corridas` en `e2e.mjs`; (2) correrlo de verdad contra las dos webs y dejar la salida en el informe; (3) `rojo-verde --orden e2e-01` en T; (4) el retiro, las copias y el registro. Si al correr A1 aparece una zona con píxeles distintos y **estable** (como el 7 px de `pagina-galeria 375`, D-118), eso ya **no** es inestabilidad: es una diferencia medida entre la web y su plantilla, y se explica antes de tocar nada. `hueco.mjs` sigue en 29/36: esta orden no mueve el contrato. La línea de `APROBADAS.md` de arreglos-01 la escribe B (C1).

## Fuera de esta hoja

DISEÑO-01 (`features.themeToggle`, D-82). FLOTA-01 (D-88). AUDITORIA-01 (el fallo suelto de `tests/services-v6.test.ts`, «Element is not attached to the DOM», que pasó 3/3 corrido solo). El candado de la flota sobre el repo hermano y su permiso por archivo. `PATCH`, `DELETE` y las rutas hijas de `/api/clients/[clientId]` por slug (decisión de Liam pendiente). Que `suite-fases.test.ts` vea el `import()` dinámico de playwright (D-121). Ninguna de las dos webs se toca: esta orden las **lee**.
