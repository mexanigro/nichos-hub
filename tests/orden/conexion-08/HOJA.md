# HOJA · CONEXION-08 · teléfono y logos genéricos en la plantilla A (26/36 → 29/36); retiro de CONEXION-07 · 2026-09-23

Escrita por el revisor desde la línea base de CONEXION-07 (aprobada 2026-09-23, T 637ed2b · H 9c0f1e9). Estado esperado al abrir A: T 637ed2b = origin, H 9c0f1e9 = origin.
Fuentes: `H:`/`T:` = archivos en HEAD; `revisor` = lecturas del 2026-09-23 con salida citada; `hueco` = `T tools/verdad/hueco.mjs --json` en HEAD; `CH` = bloque-04/CONTRATOS-HUECOS.md; `Liam` = decisiones del 2026-09-23 en la conversación con el revisor.
Formato: cada afirmación es una línea `- [ID] (repo) frase · fuente: …`. El nombre del test es la frase exacta entre `(repo) ` y ` · fuente:`. `(T)` sólo en T; `(H)` sólo en H; `(T+H)` en los dos.
Circuito: PLAN.md § Cómo se trabaja, incisos i) a n). Decisiones vigentes: D-1..D-5, D-9..D-11, D-13, D-14, D-16..D-78. Decisiones para esta hoja: D-79 (Liam) A y C son plantillas: lo que les falta se completa con contenido **genérico**, marcado como material de plantilla · D-80 (revisor) el teléfono genérico de A y C es `+972 3-000-0000` (formato que acepta `PHONE_RE` y que `toWhatsAppNumber` convierte a `97230000000`; un número de red fija inexistente, no el de una persona) · D-81 (revisor) los logos genéricos de A y C son un wordmark del `brand.name` de cada una en dos versiones —`logo.png` oscuro para superficie clara y `logo-dark.png` claro para el hero—, PNG transparentes de 600×160 generados desde un SVG de texto con Chromium (`playwright`, `omitBackground`) por `T tools/material/logo-generico.mjs` (**ajuste de A 2026-09-23**: la hoja decía `sharp`, que T no tiene —ni en `package.json` ni resoluble; el que lo tiene es H—; Chromium es lo que T ya usa para rasterizar en `tools/material/*.mjs`), guardados en `dev-fixtures/media/paleta-<p>/` y subidos a Storage por `H scripts/b4-material.ts` con rol `branding` (misma entrada, misma url; el script gana las rutas `brand.logo` y `brand.logoDark`) · D-82 (revisor) `features.themeToggle` sale de esta orden: mostrar el selector en peluquería invierte R12 («la paleta la fija el sistema de color, no el visitante», T src/config/site.ts:184) y obliga a diseñar el segundo modo de A (oscuro) y de C (claro) y el control en navbar-v6; eso es diseño, y va como primera orden de la fase de diseño (DISEÑO-01), con la decisión de Liam (2026-09-23: «sí muestra el claro/oscuro, siendo los dos igual de diseñados») · D-83 (revisor) `branding.heroToBackdrop` calculado en el hub (decisión de Liam, opción A) va en CONEXION-09: necesita portar la medición de `transicion.mjs` (hoy con ffmpeg) a una medición en el navegador al subir, y probar la paridad; no cabe con esto · D-84 CONEXION-07 se promueve entera (T a, b, c, d, e = 6; H d, e = 3).

Por qué: tres filas tienen contrato, validador y casilla pero no material en el fixture A ni guard (hueco: `contact.phone`, `brand.logo`, `brand.logoDark` · «no está en el fixture A», «sin guard»). Sin teléfono, los servicios a consulta, el WhatsApp del navbar y el del hero no tienen a dónde enviar (T services-v6:64, navbar-v6:134, hero-v6:164; el validador ya avisa «Servicio a consulta sin contact.phone»); sin logo, navbar-v6 pinta sólo el nombre. Con esta orden `hueco.mjs` pasa de 26/36 a 29/36.

## A · Material genérico (T)

- [A1] (T) tools/material/logo-generico.mjs existe y, corrido sobre un fixture, escribe `dev-fixtures/media/paleta-<p>/logo.png` y `logo-dark.png`: PNG de 600×160 con canal alfa, fondo transparente (las cuatro esquinas con alfa 0), el primero con tinta oscura (luminancia media de los píxeles opacos < 0,4) y el segundo clara (> 0,6), deterministas (dos corridas dan los mismos bytes); lo prueba un guard de T en `npm test` (`tests/logo-generico.test.ts`, fase `test:browser`) que lo corre sobre un fixture temporal y decodifica los PNG en Chromium · fuente: D-81; T:src/components/layout/navbar/navbar-v6.tsx:75–81 (`logo` para superficie clara, `logoDark` sobre el hero); **ajuste de A 2026-09-23** (sin `sharp` en T): la medición decodifica el PNG en Chromium como `T tools/verdad/recrear.mjs` § `diffPng` («sin dependencias»), y el guard va a `test:browser` porque importa `playwright` (D-57, `T tests/suite-fases.test.ts`); `hueco.mjs` mira `test`, `test:unit` y `test:browser`, así que el `guard` de las tres filas sigue dando «sí».
- [A2] (T) dev-fixtures/peluqueria-paleta-a.json y -c.json tienen `contact.phone` = `+972 3-000-0000` y `brand.logo` / `brand.logoDark` como urls de Storage bajo `clients/test-b4-peluqueria-<p>/media/branding/logo.png` y `logo-dark.png` (https, un GET con `Range: bytes=0-0` responde 200/206 e `image/png`); nada más cambia en los fixtures (`git diff 637ed2b HEAD -- dev-fixtures/*.json` = esas tres claves en cada uno) · fuente: revisor (hoy `contact` y `brand.logo` ausentes en A y C); D-79, D-80, D-81.

## B · Guards y contrato (T)

- [B1] (T) tests/contacto-logo.test.ts existe, está en `test:unit` y nombra literalmente «phone», «logo» y «logoDark»; afirma que `toWhatsAppNumber("+972 3-000-0000")` da `"97230000000"` y que services-v6, navbar-v6 y hero-v6 construyen su enlace de WhatsApp con `toWhatsAppNumber(…contact.phone)`; y que navbar-v6 con `brand.logo` y `brand.logoDark` no invierte el logo sobre el hero (`invert` falso) y con `brand.logo` solo sí (la regla de navbar-v6:78, probada como función pura si B la extrae, o por lectura del fuente) · fuente: T:src/lib/whatsapp.ts:7–13; T:src/components/landing/services/services-v6.tsx:64; T:src/components/layout/navbar/navbar-v6.tsx:75–81, :134; T:src/components/landing/hero/hero-v6.tsx:164.
- [B2] (T) verdad/contratos.json declara `guard` en `contact.phone` (`tests/contacto-logo.test.ts`, `phone`), `brand.logo` (`… logo`) y `brand.logoDark` (`… logoDark`); `features.themeToggle` gana en `tipo` la nota «diseño: DISEÑO-01 (D-82)»; CH gana «material y guard (CONEXION-08)» en esas tres filas; las otras 32 filas del .json byte a byte como en 637ed2b; y `hueco.mjs --json` da las cinco casillas en «sí» y `hecho: true` en las tres, con el total «29/36 huecos hechos» · fuente: T:verdad/contratos.json (hoy `guard: null`); hueco (hoy 26/36; «no está en el fixture A», «sin guard»); D-82.

## C · Punta a punta y material en el hub (H)

- [C1] (H) scripts/b4-material.ts recorre también `brand.logo` y `brand.logoDark` con rol `branding` (sus `RUTAS`), y con `--bucket-falso` sobre un fixture temporal con `brand.logo: "/dev-fixtures/media/paleta-x/logo.png"` sube el archivo a `clients/test-b4-peluqueria-x/media/branding/logo.png` y reescribe la clave por su url · fuente: H:scripts/b4-material.ts:34–41 (hoy sin `brand.*`); CONEXION-01 A3 (patrón `--bucket-falso`).
- [C2] (H) `scripts/b4-tenant.ts show --id test-b4-peluqueria-a` (con Firestore, sólo lectura) devuelve un `config/{id}` con `contact.phone` = `+972 3-000-0000` y `brand.logo` / `brand.logoDark` iguales a las urls del fixture A · fuente: D-16 (el tenant A lo reescribe B con `recrear --paleta a` con Firestore); patrón A4/A6 de CONEXION-03..06; inciso n).

## D · Retiro de CONEXION-07 (D-84)

- [D1] (T+H) conexion-07 figura en tests/orden/APROBADAS.md con fecha 2026-09-23, T 637ed2b y H 9c0f1e9, y rojo-verde --todas en HEAD imprime «conexion-07 · retirada (aprobada 2026-09-23)» y no «orden conexion-07 ·» · fuente: aprobación de Liam 2026-09-23.
- [D2] (T+H) npm test corre las copias tests/conexion-07-a.test.ts, -b, -c, -d, -e en T (6 tests) y -d, -e en H (3 tests), que importan ./orden/conexion-07/_comun.ts, sin recorte y sin volver a correr copias anteriores (la copia `d` excluye las órdenes vivas con `excluidas()`, sin nombrarlas); en T van a `test:unit`; tests/orden/conexion-07/ no cambia desde su último rojo (3cde6db en T, e8af546 en H) · fuente: CONEXION-07 (aprobada); D-84.

## E · Registro

- [E1] (T+H) CLAUDE.md § Puertas de T y de H tiene un párrafo CONEXION-08 que nombra «29/36», «genérico», «logo-generico» y «DISEÑO-01»; el bloque CONTRATO-DECLARADO es idéntico al del commit aprobado de CONEXION-07 (T 637ed2b / H 9c0f1e9) · fuente: E1 anteriores.
- Sin test (Nichos): PLAN.md § Estado fila 4 «CONEXION-08-A (2026-09-23): hoja + 9 afirmaciones (T 7 tests: A1, A2, B1, B2, D1, D2, E1; H 5: C1, C2, D1, D2, E1), tests rojos (T <sha>, H <sha>); B pendiente del «andá»»; CH: las tres filas, la nota de themeToggle y § Constructor; `4.3.md` § CONEXION-08 · A.

## Interfaz de prueba (contrato entre los tests de A y el código de B)

Como en CONEXION-07: `node --experimental-strip-types --test`; utilidad `_comun.ts` (copia, no import, de `tests/orden/conexion-07/_comun.ts`); imports de `tools/` y `src/` sólo con `import()` dinámico dentro del test; toda llamada anidada a `node --test` con `NODE_TEST_CONTEXT: undefined`; no contar órdenes (inciso m); cada afirmación en el repo cuyo árbol la pone en rojo (inciso n). A1: `existsSync` de `tools/material/logo-generico.mjs` y de `tests/logo-generico.test.ts` primero; después corre el script sobre un fixture temporal (prefijo «conexion-08-», `finally`) y mide él mismo los PNG decodificándolos en Chromium (`playwright`, que T sí tiene en `node_modules`; `sharp` no), como `diffPng` de `tools/verdad/recrear.mjs`; corre el guard anidado. **Precisión de A (2026-09-23)**, contrato de la herramienta: `node tools/material/logo-generico.mjs --paleta <p> [--fixture <ruta>] [--media <dir>]` toma el wordmark de `brand.name` del fixture y escribe `<media>/paleta-<p>/logo.png` y `logo-dark.png` (`--media` por defecto `T/dev-fixtures/media`, como `b4-material.ts`); y la copia promovida de `a` levanta Chromium por `medirPng` de `_comun.ts` —que importa `playwright` con `import()` dinámico, así que `tests/suite-fases.test.ts` no la ve—, por lo que va a `test:browser` como `tests/verdad-05-c.test.ts`. A2 lee los fixtures en disco y hace los GET (≤ 10 s cada uno). B1 corre el guard anidado y mide por su cuenta `toWhatsAppNumber` (`import()` dinámico) y las líneas de los tres componentes. B2 lee el .json fila por id y el .md, y corre `hueco.mjs --json` real. C1 con `--bucket-falso` sobre temporales (ninguna escritura en Storage). C2 con `show` real (`.env.local` presente; si falta, falla nombrándolo). D1, D2 y E1 como en las hojas anteriores.
Ningún test escribe en T ni en H fuera de `tests/orden/conexion-08/`; ninguno escribe en Storage ni en Firestore; `_comun.ts`, `d`, `e` idénticos T/H por `cmp`; `a`, `b` sólo en T; `c` sólo en H.

## Notas para las sesiones A y B

A: al commit rojo, los doce tests (9 afirmaciones; T 7, H 5) fallan por su primera aserción: A1 por «no existe tools/material/logo-generico.mjs»; A2 por «el fixture A no tiene contact.phone»; B1 por «no existe tests/contacto-logo.test.ts»; B2 por `guard: null`; C1 por «b4-material no recorre brand.logo»; C2 por «el tenant A trae el teléfono del preset (`03-612-4477`), no el genérico» (**ajuste de A 2026-09-23**: el tenant sí tiene `contact.phone`, heredado del preset del nicho; lo que le falta es el valor genérico y los dos logos); D1, D2, E1 como siempre. Nombre del test = frase de la hoja (A5).
B: `logo-generico.mjs` en T (Chromium por `playwright`, ya instalado; **ajuste de A 2026-09-23**: T no tiene `sharp`); correrlo para A y C; `b4-material.ts --paleta a` y `--paleta c` **reales** (D-20: Storage sólo bajo `clients/test-b4-peluqueria-{a,c}/media/**`) después de añadir las rutas `brand.*`; los fixtures ganan las tres claves; `recrear --paleta a` y `--paleta c` **con Firestore** (D-16) y citar brechas (esperado: 0 validador, 0 material; los diffs de navbar pueden cambiar porque ahora hay logo: citarlos, son la línea base nueva). `features.themeToggle` no se toca (D-82). La línea de APROBADAS.md de conexion-07 la escribe B (D1).

## Fuera de esta hoja

`features.themeToggle` y el segundo modo de A y C (DISEÑO-01, D-82); `branding.heroToBackdrop` calculado en el hub (CONEXION-09, D-83); los cinco derivados; gama al subir (5.1); AUDITORIA-01.
