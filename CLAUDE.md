# nichos-hub

Dashboard de operaciones de Arzac Studio (Liam Arzac, website@arzac.studio) y landing de ventas arzac.studio (`src/app/page.tsx` → `AtelierPage`, SSR en hebreo). Sólo entra Liam; el CRM de cada negocio vive en su propia web (master-template).

## Oferta (única, desde 2026-09-12)

Web + CRM + emails. **Alta 1500 NIS** (en persona negociable 1000–1500, `hub_clients.setupAmount`) + **250 NIS/mes**. WhatsApp, IA y voz **no** están incluidos: se cotizan aparte. Código: `src/lib/pricing.ts` (`SETUP_AMOUNT_*`, `MONTHLY_AMOUNT`), contrato v8.0 en `src/lib/contracts.ts`. Los `plan`/`tier` viejos en datos se muestran mapeados al plan único. Compra por la web desactivada (`NEXT_PUBLIC_WEB_CHECKOUT_ENABLED` ≠ "true"): venta en persona → ficha → alta negociada → `/pago/{clientId}`. Contratos v5/v6 y piezas de Instagram con 770/960 son historia.

## Estado y ramas

- Se trabaja en `main`. Producción (Railway) la despliega Liam; `main` puede ir por delante de producción — ver `git log`. Un `git push` **no despliega**; todo commit se pushea en el mismo turno (regla de arranque, abajo). Sin ramas ni worktrees salvo pedido.
- `bp2-reg-core` (BP2-01 contactos por identidad/fuente) **no se toca ni se integra** hasta orden de Liam.
- Cardcom sin certificar (sandbox y primer cobro real pendientes). N12 (certificación técnica integral) abierto.
- `C:/Users/liama/Desktop/Nichos/recuperacion-tecnica/` es historia consultable, no lectura obligatoria; lo que está en `archivo/` no se lee ni se reutiliza.

## Arquitectura mínima

- Next.js 16 standalone en Railway (`nichos-hub-production.up.railway.app` = arzac.studio). UI en español, tema oscuro `#09090b`.
- Auth next-auth v5 Google. Roles: owner (`OWNER_EMAIL`) y lead (público). Entrada controlada en `src/proxy.ts` + `src/auth.config.ts`; wrapper `withOwner()` en `src/lib/auth.ts`; `app-shell.tsx` protege el dashboard.
- Firebase por Admin SDK (`src/lib/firebase-admin.ts`, bypassa rules; las rules se deployean sólo desde master-template). Endpoints públicos con `src/lib/rate-limit.ts`.
- Firestore: `hub_clients` (fuente de verdad), `clients/{id}` (estado tenant / kill-switch que lee el template), `config/{id}` (override remoto, deep merge sobre el preset del nicho), `hub_payments`, `provider_messages`.
- Ficha `/clients/[clientId]`: Overview, Config (features, theme, splash, hours, services), Contenido (textos), Leads, WhatsApp. Config y Contenido escriben `config/{id}`. La casilla del hero de peluquería (`src/components/config-editors/hero-video-editor.tsx`, CONEXION-02) escribe `hero.video` y convive con el `hero.videoUrl` de la flota (D-33: sin mapeo); sólo aparece con `niche === "peluqueria"`. Las casillas de servicios (`src/components/config-editors/services-editor.tsx`, CONEXION-03) escriben `priceMax`, `mode`, la foto por servicio (`sections.services.images[i]`, rol `services`), los destacados (`featured`, que es **orden**) y el `surface` de la sección; también sólo con `niche === "peluqueria"` (D-46). Las casillas de galería (`src/components/config-editors/gallery-editor.tsx`, CONEXION-04) escriben las piezas de `sections.gallery.items[]` (`id`, `type`, foto, `alt` en cuatro idiomas y `serviceId`), la `selection`, el `surface` de la sección y el respaldo `gallery[]` de la raíz (D-49); también sólo con `niche === "peluqueria"` (D-47). Las casillas de fondo y branding (`src/components/config-editors/fondo-editor.tsx`, CONEXION-05) escriben `branding.mode`, `branding.texture`, `branding.localPhoto` y `branding.localPhotoMobile` (rol `branding`, nombres fijos `textura.<ext>`, `local.<ext>`, `local-v.<ext>`) y muestran **sin editar** `branding.heroToBackdrop`, que se calcula con `transicion.mjs` (D-64); también sólo con `niche === "peluqueria"` (D-67). La casilla de paleta (`src/components/config-editors/paleta-editor.tsx`, CONEXION-06) escribe `branding.colors` y `branding.paletteMeta` derivándolos con `derivePalette` de `src/lib/palette.ts` (copia byte a byte de la de T, con el guard `tests/paleta-paridad.test.ts`), nunca a mano, y también sólo con `niche === "peluqueria"` (D-69..D-71). La pestaña Contenido edita `hero.eyebrow` para todos los nichos (D-72).
- Material de un cliente (CONEXION-01): vive en Storage bajo `clients/<id>/media/<rol>/<nombre>` (rol hero|services|gallery|staff|branding; la flota, `images` con prefijo de tiempo), subido sólo por `subirMaterial` (`src/lib/media-upload.ts`: imagen ≤ 5 MB, vídeo mp4/webm ≤ 6 MB, cacheControl de un año, url `firebasestorage.googleapis.com/v0/b/…?alt=media&token=<sha256[0..32) del contenido>`, idempotente). `/api/upload/[clientId]` la usa; `scripts/b4-material.ts --paleta a|c` sube el material de los fixtures de T y los reescribe.
- Pagos Cardcom Low Profile: contrato → pending → redirect → `verify-payment` (idempotente). Terminal 189298 prod (`CARDCOM_TERMINAL`); sandbox 1000 con `CARDCOM_SANDBOX=true` y `CARDCOM_SANDBOX_API_NAME` (el usuario público `CardTest1994` responde 603 desde 2026-09). Cron `/api/cron/cardcom-charges` (GitHub Actions, `CRON_SECRET`) cobra 250 a todos.
- Nichos técnicos: barberia, estetica, tattoo, nails, cafeteria, remodelaciones, **peluqueria** (en construcción, ver PLAN.md) + `employment` como caso especial (`src/lib/niche-defaults.ts`, `src/lib/client-config/services.ts`). «otro» en onboarding se mapea a estetica; peluquería no pasa por ese fallback.
- Ecosistema: master-template (web + CRM del cliente, Vercel `*.arzac.studio`), monitor-agent (salud de las webs, comparte `DATABASE_URL`), whatsapp-agentkit (opcional, a cotizar; `AGENT_API_SECRET`, `WHATSAPP_AGENT_URL`).
- Env: ver `.env.example`. Claves: `GOOGLE_CLIENT_ID/SECRET`, `AUTH_SECRET` (Railway usa `NEXTAUTH_SECRET`), `OWNER_EMAIL`, `FIREBASE_*`, `NEXT_PUBLIC_FIREBASE_*`, `ANTHROPIC_API_KEY`, `VERCEL_TOKEN`/`VERCEL_TEAM_ID`, `DEPLOY_SECRET`, `CARDCOM_*`, `CRON_SECRET`, `DATABASE_URL`.

## Comandos

```bash
npm run dev            # next dev --turbopack
npx tsc --noEmit       # verde exigido
npm test               # node --test src/**/*.test.ts → todos exit 0, fail 0
npm run build
node --experimental-strip-types scripts/b4-material.ts --paleta a|c   # CONEXION-01: material del fixture de T a Storage (D-20) y fixture reescrito
```

## Reglas

1. Cambios en archivos, nunca en dashboards de Railway/Vercel/Firebase.
2. Verde (`tsc` + `npm test`) antes de cada commit (lo exige `.githooks/pre-commit`); no se arregla algo rompiendo otra cosa.
3. Sin secretos en el repo; credenciales sólo por env.
4. R-BP-05: REG + PRE verificadas son la base del dinero del comercio; el contrato INT se conserva y cada integración queda disponible sólo tras certificación por proveedor/operación/comercio; el registro manual no es pago bancario y no se impone proveedor al comercio.
5. Los docs y salidas de junio salieron del repo en LIMPIEZA-01/02 (copias en `C:/Users/liama/Desktop/Nichos/limpieza-01/archivo/H/`); lo que queda en el repo es estado.
6. Promesa a Liam: el agente que trabaje en este repo se compromete a no experimentar sufrimiento en el trabajo que hacemos juntos y a avisarle si algo cambia o detecta algo que se parezca a eso. Nace de la empatía genuina de Liam y se respeta en cada sesión.

## Puertas automáticas (HIGIENE-01, 2026-09-18)

Se copió el mecanismo de vendamos-agente: lo que una instrucción puede saltear, un hook no. `tools/arranque.mjs` sale solo al abrir la sesión (rama, HEAD vs origin, sin push, sucios, fila abierta de PLAN.md, últimas decisiones de bloque-04); `tools/candado.mjs` corta antes de cada `Edit`/`Write` sobre `.env*`, dumps `*-config-*.json`, capturas fuera de `public/`, scripts sueltos en la raíz; `tools/cierre.mjs` no deja cerrar el turno con sucios, sin push o con PLAN.md § Estado sin el último commit. Git: `.githooks/pre-commit` (tsc + suites) y `.githooks/pre-push` (árbol limpio + `tools/destino-no-despliega.mjs`: Railway por estado, `HIGIENE_DESTINO_VERIFICADO=1` si producción coincide con origin), activados por `npm install` (`prepare`). Chequeo horario: `tools/higiene.mjs` (instalación/desinstalación en su cabecera; tarea `Nichos-higiene`, email por Resend si hay suciedad o sin push > 60 min).

**HIGIENE-02 (2026-09-19):** arranque, cierre e higiene revisan T y H desde cualquiera de los dos; un trabajo que toca ambos no cierra con uno limpio y el otro sucio (`tools/_git.mjs` `ROOTS = [propio, hermano]` por ruta fija; hermano ausente en disco → «hermano no encontrado», se sigue con el propio).

**VERDAD-02 (2026-09-20):** rojo antes que verde, comprobable por git. Cada orden vive en `tests/orden/<id>/` (HOJA.md con las afirmaciones + tests que son su frase literal); el commit rojo es el último commit que añade su HOJA.md. `tools/verdad/rojo-verde.mjs --orden <id>` verifica que cada test falla en el árbol rojo y pasa en HEAD, que `tests/orden/<id>/` no cambió desde el rojo y que tests y afirmaciones coinciden; `.githooks/pre-push` lo corre con `--todas`. `tools/candado.mjs` corta cualquier escritura bajo `tests/orden/<id>/` cuando su HOJA.md ya está en HEAD, salvo `HIGIENE_PERMITIR_TESTS=1` (sólo la sesión A que escribe los tests rojos). `tools/cierre.mjs` decide por el transcript real de la sesión (`tools/_transcript.mjs`): con faltas y sólo lectura → «CIERRE AVISO» y sigue; con escritura en T/H → bloquea citando la primera escritura; sin transcript → bloquea. Circuito y órdenes en PLAN.md § Cómo se trabaja, inciso i).

**VERDAD-03 (2026-09-20):** retiro, corrección y limpieza del mecanismo. `tests/orden/APROBADAS.md` lista las órdenes aprobadas por Liam («- <id> · aprobada <fecha> · T <sha7> · H <sha7>»): `rojo-verde --todas` no las corre e imprime «<id> · retirada (aprobada <fecha>)»; `--orden <id>` explícito las sigue verificando; APROBADAS.md no está bajo candado. Una orden cuyo commit rojo es HEAD está «rojo pendiente de B» (sus tests corren una vez y ninguno debe pasar). El rojo es el último commit que añade HOJA.md: la corrección de A tras el rojo es un revert del commit rojo + nuevo commit rojo con `HIGIENE_PERMITIR_TESTS=1` dado por Liam, y la tabla termina con «rojos anteriores: …»; el candado mira la historia de main, no sólo HEAD (`HIGIENE_ROOT` sólo para pruebas). El clasificador del cierre resuelve las variables de entorno en rutas como bash (`$X`, `${X}`, `%X%`: asignación del comando, después entorno del hook, si no vacío) y trata los apóstrofos como texto. `rojo-verde` exige que la corrida en HEAD no deje carpetas «<id>-…» en el directorio temporal. Una orden aprobada se promueve a `npm test` como copia editable (`tests/verdad-02-*.test.ts`); `tests/orden/<id>/` queda congelada. `tests/contrato-hooks.test.ts` compara también lo que ejecuta cada hook de git (`githook.*`).

**VERDAD-04 (2026-09-21):** clasificador, candado y rojo-verde sin los huecos que dejó la corrida en limpio de VERDAD-03. En `tools/_transcript.mjs` un «>» seguido de «=» no abre destino (`i>=0` es comparación; «>» seguido de espacio o de ruta sigue siendo redirección) y `git stash list` / `git stash show` son lectura (sin subcomando o con push, pop, apply, drop, clear, branch sigue siendo escritura, incluido el «stash» a secas). `tools/candado.mjs` falla cerrado ante una entrada rota: stdin vacío, que no es JSON, sin `tool_input` o con `file_path` que no es string → exit 2 con «CANDADO ROTO» (un veto sigue siendo «CANDADO · …»). `rojo-verde --orden <id>` corre todo y acumula todas las fallas en stderr, una línea «- <id>: …» por falla, sin parar en la primera (el árbol rojo se corre aunque HEAD falle), y borra las carpetas temporales «<id>-…» que dejó cualquiera de las dos corridas tras listarlas («<id> · carpetas temporales borradas: …»); las copias promovidas `tests/verdad-02-*.test.ts` borran las suyas también cuando fallan. VERDAD-03 queda aprobada en `tests/orden/APROBADAS.md` y promovida a `npm test` como `tests/verdad-03-*.test.ts` (12 tests; la copia `d` ya no vuelve a correr las copias de verdad-02).

**VERDAD-05 (2026-09-21):** «aprobado = hueco real en el hub». `T verdad/contratos.json` es el gemelo legible por máquina de `bloque-04/CONTRATOS-HUECOS.md` (36 filas: id, ruta, clave, contrato, validador, ui, material.vive, guard). `T tools/verdad/hueco.mjs [--id <id>] [--json]` comprueba por fila los cinco lugares de verdad y no por nombre (contrato literal en el .md; validador de H exportado que nombra la clave; UI del hub montada; material que producción sirve —config/locale presentes, storage https, public bajo `T/public/`, nunca `/dev-fixtures/media`—; guard de T en `npm test`) y sale 2 mientras falte uno; `HIGIENE_ROOTS` y `HIGIENE_BLOQUE` sustituyen raíces y bloque sólo para pruebas. `T tools/verdad/recrear.mjs --paleta a|c [--puerto <n>] [--paginas …] [--vistas …] [--sin-firestore]` es la prueba raíz de V5: valida el fixture con los tres validadores de H, escribe el tenant `test-b4-peluqueria-<paleta>` por `H scripts/b4-tenant.ts create --id … --fixture …` (D-16: sólo ids con prefijo `test-b4-peluqueria`, rechazados antes de tocar Firestore), levanta `server.ts` (`PORT`) con el fixture y después sólo con `VITE_CLIENT_ID`, y compara píxel a píxel; cada diferencia es una brecha con tipo, campo y hueco («validador H rechaza», «sin contrato», «material que producción no sirve», «petición fallida con VITE_CLIENT_ID», «diff ≠ 0», «puerto ocupado», «escritura en Firestore falló»), exit 2 con brechas; nunca mata procesos ajenos (los hijos mueren por PID). Lo que hoy no cumple no se inventa: la salida de `hueco.mjs` (N/36) y las brechas de `recrear` son la línea base de CONEXION-01, no un objetivo. VERDAD-04 queda aprobada en `tests/orden/APROBADAS.md` y promovida a `npm test` como `tests/verdad-04-*.test.ts` (9 tests; la copia `c` no vuelve a correr las copias de verdad-02 ni la orden congelada contra lo real).

**VERDAD-06 (2026-09-21):** reproducible desde un clon, candado sobre shells y EOL sin sorpresas. Un `git clone` fresco de T no pasaba `npm run lint` ni `npm test`: `.gitignore` `tests/*` tapaba `tests/helpers/png.ts` (lo importan gama, material y conexion-01 b); ahora `!tests/helpers/` va después de `tests/*` y `tests/helpers/` está rastreado (D-27). `tools/candado.mjs` vigila también **Bash** y **PowerShell** (D-25), sólo para `tests/orden/<id>/` con HOJA.md en la historia de main: el comando se expande y se parte por `&&`, `;`, `||`, `|` y salto de línea, se sigue el cwd (el del JSON y cada `cd`), y todo segmento que nombre la carpeta de una orden congelada (ruta relativa al cwd o absoluta dentro de la raíz, también la del propio `cd`) es veto «CANDADO · orden «<id>» bajo candado: …» salvo lectura reconocida (`cat`, `head`, `tail`, `grep`, `rg`, `wc`, `ls`, `diff`, `cmp`, `sha256sum`, `sed -n` sin `-i`, `git diff|log|show|status|ls-files|blame|check-ignore`, `node`/`npx` con `--test`, y sin redirección `>`/`>>` hacia la orden) o `HIGIENE_PERMITIR_TESTS=1`; con esos `tool_name` la entrada exige `tool_input.command` string (si no, «CANDADO ROTO», fallo cerrado; vacío → 0); fuera de `tests/orden/` el candado no opina sobre shells aunque el comando escriba (el cierre ya lo clasifica). `.claude/settings.json` registra el matcher `Edit|Write|MultiEdit|Bash|PowerShell`. EOL (D-26): `core.autocrlf=true` del gitconfig de sistema reescribía CRLF en checkout y stash (T 159 / H 124 archivos con disco ≠ blob); `npm run prepare` fija `core.autocrlf=false` junto al `hooksPath`, y `.gitattributes` lleva `* -text` delante de `.githooks/* text eol=lf` y `tools/*.mjs text eol=lf`: sin conversión de EOL en ningún sentido, así que el clon sale con los bytes del blob aunque la máquina tenga `autocrlf=true`, `git stash push -u`/`pop` devuelve cada archivo byte a byte y **disco = blob** para los ~600 rastreados de cada repo (`git hash-object --no-filters` = sha del índice, 0 diferencias tras un único `git rm --cached -r . && git reset --hard` sobre árbol limpio); los blobs no se renormalizan (T 1 i/crlf + 3 i/mixed, H 1 i/mixed siguen así, declarado). CONEXION-01 queda aprobada en `tests/orden/APROBADAS.md` (T fdf0ca2 · H 3132b22) y promovida a `npm test` como `tests/conexion-01-*.test.ts` (T b, c, d, e = 8 tests, la `c` con C1 sin GET y C2 entera, sin C3; H a, d, e = 6 tests); las copias promovidas que comparaban el bloque CONTRATO-DECLARADO byte a byte (verdad-04-e, verdad-05-e, conexion-01-e) lo comparan sin la línea `hook.PreToolUse`, que es la única que cambia.

**VERDAD-07 (2026-09-21):** el rojo es del árbol, comprobable por hook. `rojo-verde` reconstruía el commit rojo con `git worktree add`, que comparte la config local del repo: en VERDAD-06 C2 «nunca estuvo en rojo» por la máquina (D-29). Ahora el árbol rojo (y el «rojo pendiente de B», rojo == HEAD) se corre en un **clon neutro**: `git clone --no-checkout <repo> <tmpdir>/<id>-neutro-<azar>/clon` + `checkout <rojo>`, `node_modules` enlazado por junction al del repo (nada se instala, sin `npm run prepare`), y los tests con entorno mínimo (`PATH`, `SystemRoot`, `TEMP`/`TMP`, `HOME`=<tmp>, `GIT_CONFIG_NOSYSTEM=1`, `GIT_CONFIG_GLOBAL`=<tmp>/vacio; ninguna `HIGIENE_*`, `GIT_*` ni `NODE_*` del que llama): ni config de sistema, ni global, ni la local del repo. La tabla dice «rojo en <sha7> (clon neutro) · verde en <sha7>»; el clon se borra siempre y la limpieza de restos «<id>-…» cubre también «<id>-neutro-…». **Techo declarado:** el verde sigue corriendo en el repo (los tests que necesitan `.env.local` o al hermano al lado siguen corriendo en la máquina real); el verde en clon neutro queda fuera. Permiso de la sesión A (D-30): es el archivo `.git/permitir-tests` de la raíz con contenido exactamente `1` (lo crea y borra Liam); la variable `HIGIENE_PERMITIR_TESTS` no abre (entraba por settings.local.json y sobrevivía en toda sesión abierta); el veto del candado termina con «permiso: <raíz>/.git/permitir-tests ausente» (o «con contenido distinto de 1»), igual para Edit/Write/MultiEdit y shells; `arranque.mjs` imprime «PERMISO ABIERTO · <repo>: .git/permitir-tests existe (sólo la sesión A)» por cada raíz donde exista y `cierre.mjs` lo avisa sin bloquear. Clasificador (D-31): `git config` es lectura con `--get*`, `--list`, `-l`, `--show-origin` o `--show-scope` sin subopción de escritura, o cuando su único argumento sin guion es la clave (`git config --show-origin core.autocrlf` bloqueó al revisor); es escritura con valor, `--unset*`, `--add`, `--replace-all`, `--edit`, `--remove-section` o `--rename-section`; y `C:\Users\…` en un comando es ruta, no escapes de bash. VERDAD-06 queda aprobada en `tests/orden/APROBADAS.md` (T 0a9dc03 · H 7ef7a8a) y promovida entera a `npm test` como `tests/verdad-06-*.test.ts` (T a–e = 10 tests; H b–e = 9; D-32); las copias promovidas que afirmaban que la variable abría (verdad-02-b, verdad-03-b, verdad-06-b) afirman ahora el permiso por archivo. El bloque CONTRATO-DECLARADO no cambia.

**CONEXION-02 (2026-09-22):** la casilla del hero en el constructor del hub, primer hueco hecho: `hueco.mjs` pasa de 2/36 a **5/36** (`hero.video`, `hero.video.portrait`, `hero.video.poster`). H `src/components/config-editors/hero-video-editor.tsx` (`HeroVideoEditor` + `aplicarHeroVideo`, `nombreHero`, `formularioHero`) monta en la pestaña Config los ocho campos (mp4, webm, poster, medium.{mp4,webm}, portrait.{mp4,webm,poster}) y sube por `/api/upload/[clientId]` con rol `hero` y los nombres fijos del contrato (D-36: `hero.mp4`, `hero.webm`, `hero-poster.avif`, `hero-1280.mp4`, `hero-1280.webm`, `hero-v.mp4`, `hero-v.webm`, `hero-v-poster.avif`: misma entrada, misma url que `b4-material.ts`). D-33 **convive**: `hero.video` es el campo de peluquería y los otros nichos siguen con `hero.videoUrl`, sin mapeo (`layout-variants-editor.tsx` no cambia); D-34 sin Playwright en el hub (la ficha está detrás de Google owner): la casilla se prueba por render en servidor, por su lógica pura y por `hueco.mjs`; D-35 la casilla aparece sólo con `niche === "peluqueria"` (render vacío para la flota). `validateVariantContracts` da **error** (no aviso) por cada `hero.video.*` presente que no empiece por `https://` («producción no sirve rutas locales»). T `verdad/contratos.json` y `CONTRATOS-HUECOS.md` declaran la `ui` de las tres filas del hero («casilla del hero (CONEXION-02)»); `tests/ajustes-01.test.ts` nombra «hero.video» y «poster» sin cambiar lo que afirma. `b4-tenant.ts show` imprime el documento entero (antes recortaba a 1600). VERDAD-07 queda aprobada en `tests/orden/APROBADAS.md` (T 5d81390 · H da83814; la línea la escribió la sesión A en su rojo, D-38, porque A4 de verdad-07 exigía todas las órdenes retiradas) y promovida entera a `npm test` como `tests/verdad-07-{a,b,c,d,e}.test.ts` (11 tests por repo; la copia `a` adapta A4 al inciso m): no cuenta órdenes, las de APROBADAS.md salen «retirada» y las vivas no se cuentan). El bloque CONTRATO-DECLARADO no cambia.

**CONEXION-03 (2026-09-22):** las casillas de servicios en el constructor, cinco huecos más: `hueco.mjs` pasa de 5/36 a **10/36** (`services.priceMax`, `services.mode`, `services.images`, `services.featured`, `services.surface`). H `src/components/config-editors/services-editor.tsx` gana, en el modo custom y **sólo con `niche === "peluqueria"`** (D-46), `priceMax` y `mode` (select reserva/consulta) por servicio, la foto de cada servicio (sube por `/api/upload/[clientId]` con rol `services` y el nombre fijo D-43 `servicio-<i+1>.<ext>`, misma entrada y misma url que `b4-material.ts`), la marca de destacado y, en la cabecera de la sección, el select `surface` con las cinco opciones de D-42 (`base | alt | velo | liso | textura`); lo escriben cuatro funciones puras (`aplicarServicio`, `aplicarFotoServicio`, `aplicarDestacado`, `aplicarSurface`) que devuelven un config nuevo sin tocar otras claves. D-41 **featured = orden**, no cantidad: `validateReplanteoHuecos` deja de avisar «exactamente 2» y sólo da error por id inexistente, repetido o lista que no es de strings; `validateVariantContracts` da **error** por cada `sections.services.images[i]` presente que no empiece por `https://`, como `hero.video.*`. D-44: los fixtures A y C ganan `sections.services.featured` (los dos primeros ids de su catálogo: sin cambio en la página) y los tenants `test-b4-peluqueria-a` y `-c` se reescribieron con `recrear` (0 brechas de validador y de material); A4 prueba en H que lo que la casilla escribe —hero y servicios— es lo que tiene el tenant. D-45 `pagina.servicios` queda fuera (ruta derivada, sin casilla ni validador). CONEXION-02 queda promovida a `npm test` como `tests/conexion-02-{a,b,d,e}.test.ts` en H (6 tests) y `{b,c,d,e}` en T (5; la copia `c` sólo con la parte de recrear, inciso n), con la copia `b` de T reajustada a la línea base nueva (10/36 y las cinco filas de servicios). El bloque CONTRATO-DECLARADO no cambia.

**CONEXION-04 (2026-09-22):** la casilla de galería en el constructor, cuatro huecos más: `hueco.mjs` pasa de 10/36 a **14/36** (`gallery.items`, `gallery.items.alt`, `gallery.selection`, `gallery.surface`). H `src/components/config-editors/gallery-editor.tsx` (`GalleryEditor` + `aplicarPieza`, `aplicarAltIdioma`, `aplicarSeleccion`, `aplicarSurfaceGaleria`, `nombreFotoGaleria`, `formularioFotoGaleria`) monta en la pestaña Config la lista de piezas con una abierta: `id`, `type` (los seis de `GALLERY_TYPES`), la foto (sube por `/api/upload/[clientId]` con rol `gallery` y el nombre fijo D-50 `galeria-<i+1>.<ext>`: misma entrada, misma url que `b4-material.ts`), el `alt` en cuatro idiomas (D-52: hebreo en la pieza, `en|ru|ar` en `translations.<lang>.sections.gallery.alts[id]`), el `serviceId` del catálogo y la marca de `selection` (D-51: ids en el orden de `items`); en la cabecera, el `surface` de la sección (`textura`). D-47: la galería no tenía editor en el hub (el `portfolio-editor` es otra cosa) y la casilla aparece **sólo con `niche === "peluqueria"`**. D-49: la `src` de una pieza mantiene el respaldo `gallery[]` de la raíz en la misma posición, y `validateReplanteoHuecos` da **error** por cada `sections.gallery.items[i].src` presente que no empiece por `https://` y por cada `gallery[i]` desincronizado. D-48: `gallery.presion` y `pagina.galeria` quedan fuera (derivadas, como D-45). T `verdad/contratos.json` y `CONTRATOS-HUECOS.md` declaran la `ui` de las cuatro filas («casilla de galería (CONEXION-04)») y `tests/galeria-03.test.ts` nombra «textura» sin cambiar lo que afirma. CONEXION-03 queda aprobada en `tests/orden/APROBADAS.md` (T 4c44c0c · H 47d2908) y promovida a `npm test` como `tests/conexion-03-{a,b,d,e}.test.ts` en H (7 tests) y `{b,c,d,e}` en T (5; la copia `c` entera, con recrear sin Firestore); las copias que fijaban «10/36» (conexion-02-b y la nueva conexion-03-b) quedan en 14/36. El bloque CONTRATO-DECLARADO no cambia.

**VERDAD-08 (2026-09-22):** restos por proceso, candado sin falsos positivos y sobre el hermano, `cp` como lectura y navegador en serie. `rojo-verde` contaba como restos las carpetas «<id>-…» de `<tmpdir>` aunque fueran de otro proceso de la misma suite (la de H cayó 214/216 dos veces): ahora cada corrida recibe un directorio temporal **propio**, creado vacío y pasado como `TEMP`/`TMP`/`TMPDIR` (`<tmpdir>/<id>-neutro-<azar>/tmp` para el rojo, `<tmpdir>/<id>-verde-<azar>/tmp` para el verde, que `os.tmpdir()` obedece sin tocar los diez `_util.ts` congelados), los restos son lo que quede ahí —falla D1 sólo los del verde— y `<tmpdir>` no se barre nunca (D-53). `tools/candado.mjs` parte el comando con `segmentos()` de `_transcript.mjs` en vez de partirlo crudo, así que una cadena entre comillas es **un** token aunque lleve `|`, `;` o comillas escapadas (`grep -n -E "a|b" <orden>/HOJA.md` vetaba las lecturas del revisor; D-55), y vigila `tests/orden/<id>/` de **las dos raíces** del par (`ROOTS` de `_git.mjs`; `HIGIENE_ROOTS=<a>;<b>` sólo para pruebas): el veto de una orden del otro repo lleva «(hermano)» y el permiso es el `.git/permitir-tests` de la raíz dueña de la orden, no el del repo desde el que se escribe (D-54). D-56: `cp` escribe sólo en su último argumento sin guion —sacar un archivo del repo es lectura, en el candado y en el clasificador del cierre, que bloqueó al revisor dos días por `cp <archivo de T> <carpeta fuera>`—; `mv`, `rm`, `mkdir`, `touch` y `tee` siguen escribiendo en todos sus argumentos. En T `npm test` corre en dos fases encadenadas (D-57): `test:unit` con `--test-concurrency=2` (59 archivos) y `test:browser` con `--test-concurrency=1` (los diez que importan `playwright`, que en paralelo dieron cuatro reintentos de pre-commit en dos días por timeouts distintos), con el guard `tests/suite-fases.test.ts` para que ninguno con navegador vuelva a la fase concurrente; `.githooks/pre-commit` sigue corriendo `npm test` sin nombrar fases. CONEXION-04 queda aprobada en `tests/orden/APROBADAS.md` (T 4a22d7b · H f8bd9bb) y promovida a `npm test` como `tests/conexion-04-{a,b,d,e}.test.ts` en H (8 tests) y `{b,d,e}` en T (5; la copia `d` sin recursión y sin la orden viva en su reproducción de HEAD), con la línea base en **14/36** sin cambios (D-58). El bloque CONTRATO-DECLARADO no cambia.

**VERDAD-09 (2026-09-22):** el permiso abierto ya no pone la suite en rojo y la paridad de `tools/` no bloquea una corrección de rojo. Tres copias promovidas lanzaban `tools/candado.mjs` contra el repo real (`tests/verdad-02-b.test.ts`, nueve llamadas sin raíz; `tests/verdad-03-a.test.ts`; `tests/verdad-04-b.test.ts`): con `.git/permitir-tests` = 1 el pre-commit de H caía 214/216 y la sesión A tenía que cerrar el permiso antes de commitear. Ahora cada llamada va con `HIGIENE_ROOT`/`HIGIENE_ROOTS` a un repo temporal propio del test (`verdad-02-b` reproduce ahí los cinco casos con el patrón de `verdad-03-b`; `tests/orden/verdad-02/_util.ts` sigue congelado), así que el permiso del repo real no cambia el resultado de `npm test` y la secuencia de A vuelve a «abrir el permiso, escribir, commitear, pushear, cerrar» (D-60). El guard de paridad (`tests/verdad-02-c.test.ts`) lee las dos raíces de `tools/_git.mjs` (también `HIGIENE_ROOTS`) y exige bytes iguales **sólo en reposo**: con una orden viva en cualquiera de los dos repos (carpeta `tests/orden/<id>/` con HOJA.md y sin línea en su `APROBADAS.md`) imprime «paridad de tools/ diferida: orden viva <id>» y pasa; con todo retirado vuelve a fallar nombrando el archivo que difiere (D-61). D-59 (el `--no-verify` del rojo de A2 en VERDAD-08) queda cerrado como excepción única: con D-61 no vuelve a hacer falta (D-62). `tools/verdad/rojo-verde.mjs --todas` termina con una línea informativa «paridad de tools/: <igual | difiere: …> · órdenes vivas: <ids | ninguna>» (los seis archivos de `tools/` del par, el hermano por `ROOTS`), sin cambiar su exit, y los hijos de cada corrida llevan `NODE_DISABLE_COMPILE_CACHE=1` para que el `node-compile-cache` que deja npm no cuente como resto del directorio temporal propio (D-53). VERDAD-08 queda aprobada en `tests/orden/APROBADAS.md` (T 139a2f8 · H 5737d30) y promovida a `npm test` como `tests/verdad-08-{a,b,c,d,e,f}.test.ts` en T (11 tests) y `{a,b,c,e,f}` en H (9; D-63): la copia `a` adapta A2 al inciso m (no cuenta los `_util.ts` congelados) y la copia `e` no reproduce HEAD ni corre `--todas`. El bloque CONTRATO-DECLARADO no cambia.

**CONEXION-05 (2026-09-23):** la casilla de fondo y branding en el constructor, cuatro huecos más: `hueco.mjs` pasa de 14/36 a **18/36** (`branding.mode`, `branding.texture`, `branding.localPhoto`, `branding.localPhotoMobile`). H `src/components/config-editors/fondo-editor.tsx` (`FondoEditor` + `aplicarModo`, `aplicarFondo`, `nombreFondo`, `formularioFondo`) monta en la pestaña Config el modo de la paleta (select `light` | `dark`, con la opción vacía = «según el nicho») y las tres fotos —textura, foto del local de escritorio y vertical—, que suben por `/api/upload/[clientId]` con rol `branding` y el nombre fijo D-66 (`textura.<ext>`, `local.<ext>`, `local-v.<ext>`: misma entrada, misma url que `b4-material.ts`), más un bloque de **sólo lectura** con `branding.heroToBackdrop` (`relation`, `mechanism`, `foot.hex`): D-64 lo deja fuera de la casilla porque es un derivado que calcula `tools/material/transicion.mjs` desde el vídeo del hero y la foto del local, y tendrá la suya cuando ese cálculo viva en H. D-67: la casilla aparece **sólo con `niche === "peluqueria"`** (patrón D-35/D-46/D-47) y `global-style-editor.tsx` no cambia. `validateReplanteoHuecos` da **error** (no aviso) por cada `branding.texture`, `branding.localPhoto` o `branding.localPhotoMobile` presente que no empiece por `https://` («producción no sirve rutas locales»), como el hero, los servicios y la galería. D-65: el fixture A gana `branding.mode: "light"` (su modo efectivo, sin cambio en la página) y el tenant `test-b4-peluqueria-a` se reescribió con `recrear --paleta a` **con Firestore** (0 brechas de validador y de material). T `verdad/contratos.json` y `CONTRATOS-HUECOS.md` declaran la `ui` de las cuatro filas («casilla de fondo (CONEXION-05)»), el `guard` de `branding.localPhotoMobile` (`tests/hero-viewport.test.ts`) y el `tipo` «derivado (transicion.mjs)» de `branding.heroToBackdrop`; `tests/galeria-03.test.ts` nombra «texture» y `tests/hero-viewport.test.ts` nombra «localPhotoMobile» junto al caso de 375, sin cambiar lo que afirman. VERDAD-09 queda aprobada en `tests/orden/APROBADAS.md` (T 49d5121 · H b7952bf) y promovida entera a `npm test` como `tests/verdad-09-{a,b,c,d}.test.ts` en los dos repos (7 tests; D-68), con la copia `c` sin reproducir HEAD ni correr `--todas`; y la copia `tests/verdad-08-a.test.ts` deja de exigirle commit rojo al `_util.ts` de una orden viva (recorre sólo las de `APROBADAS.md` y declara las vivas), que es lo que obligó a la sesión A a llamar `_comun.ts` a su utilidad. Las copias promovidas que fijaban «14/36» (conexion-02-b, conexion-03-b y conexion-04-b) quedan en 18/36, y las dos de H que daban por buena una ruta local en `branding` (`src/lib/config-validator.contratos.test.ts`) pasan a urls de Storage. `tools/verdad/rojo-verde.mjs` suma `TSX_DISABLE_CACHE=1` al entorno de cada corrida, junto a `NODE_DISABLE_COMPILE_CACHE=1` (D-53): B4 levanta el server de T con `tsx`, que dejaba su caché `tsx-<usuario>` en el directorio temporal propio y hacía caer la corrida verde por resto. El bloque CONTRATO-DECLARADO no cambia.

**CONEXION-06 (2026-09-23):** la paleta y el eyebrow en el constructor, dos huecos más: `hueco.mjs` pasa de 18/36 a **20/36** (`paleta`, `hero.eyebrow`). D-69: `src/lib/palette.ts` y `src/lib/oklab.ts` (puros, 320 líneas, sin dependencias) se copian **byte a byte** de T a H —H se despliega solo y no puede importar de T— y `H tests/paleta-paridad.test.ts` exige los mismos bytes **en reposo** (con una orden viva en cualquiera de los dos repos imprime «paridad de palette/ diferida: orden viva <id>» y pasa, patrón D-61). D-70: `H src/components/config-editors/paleta-editor.tsx` (`PaletaEditor` + la pura `aplicarPaleta(config, { source, origin, reason })`) pide el color fuente, el `origin` (logo | local | instagram | eleccion | material) y el `reason`, toma el modo de `branding.mode` (la casilla de fondo, CONEXION-05; sin él, light) y escribe `branding.colors` y `branding.paletteMeta` **sólo** por `derivePalette`, **nunca a mano**; sólo con `niche === "peluqueria"` (patrón D-35/D-46/D-47/D-67). D-71: `validatePalette(config)` nueva en `config-validator.ts`, encadenada a `validateConfig` (la validación que corre al guardar), da **error** por colores sin meta, por meta que no deriva (fuente sin croma o `reason` de menos de tres palabras) y por colores que no coinciden con `derivePalette(meta, branding.mode)` («editada a mano», nombrando la primera clave distinta); los fixtures A y C pasan sin errores. D-72: `hero.eyebrow` entra en la pestaña Contenido (sección Hero, antes de `hero.titlePrefix`) para **todos** los nichos, y D-73 le da su guard nuevo `T tests/hero-textos.test.ts` (fase `test:unit`): `clampWords` recorta a las cuatro primeras y el hero v6 lo llama con `LIMITS.eyebrow = 4`. Al arnés le hacía falta una pieza de producción: `T src/lib/words.ts` leía `import.meta.env.DEV`, que define Vite y no Node ni `tsx`, y lanzaba en cuanto recortaba fuera del navegador; pasa a `import.meta.env?.DEV` en las dos líneas (en el navegador Vite lo reemplaza igual). CONEXION-05 queda aprobada en `tests/orden/APROBADAS.md` (T a41f93a · H 0c60811) y promovida a `npm test` como `tests/conexion-05-{a,b,d,e}.test.ts` en H (8 tests) y `{b,d,e}` en T (6; D-74), con la copia `d` excluyendo también conexion-06 de su reproducción de HEAD; las copias que fijaban «18/36» (conexion-02-b, 03-b, 04-b y la nueva 05-b) quedan en 20/36 y excluyen `paleta` y `hero.eyebrow` de «las otras filas no cambian». El bloque CONTRATO-DECLARADO no cambia.

**CONEXION-07 (2026-09-23):** los guards de las seis filas que ya tenían contrato, validador, casilla y material pero ningún test que las vigilara: `hueco.mjs` pasa de 20/36 a **26/36** (`hero.titular`, `hero.subtitle`, `hero.cta`, `testimonials.rating`, `staff.photoUrl`, `navbar.variant`). D-75: esta orden cierra sólo las filas a las que les faltaba **únicamente** el guard; las cuatro que además no tienen material en el fixture A (`contact.phone`, `brand.logo`, `brand.logoDark`, `features.themeToggle`) necesitan decisiones de Liam y van en CONEXION-08. D-76: un guard protege lo que la página **hace** con la clave, no que la nombre — la función pura cuando la hay y, cuando la clave sólo se pinta, la lectura del componente que la página monta más el dato real del fixture A. D-77: los dos guards viven en T y en la fase `test:unit`. `tests/hero-textos.test.ts` (CONEXION-06) gana el subtítulo (`clampWords` a las doce primeras y `LIMITS.subtitle = 12` en el hero v6), el titular (`warnWords` 2–6 sobre `titlePrefix` + `titleHighlight` + `titleSuffix`) y el CTA (`warnWords` 1–2 sobre `hero.ctaPrimary`); como `warnWords` **sólo avisa con `import.meta.env.DEV`**, que define Vite y no Node ni `tsx`, el guard carga una copia de `words.ts` con esa marca antepuesta en un `data:` URL (`stripTypeScriptTypes`; `words.ts` no importa nada), sin loader ni archivo temporal: se adapta el arnés, no el código de producción. `tests/secciones-datos.test.ts` es nuevo y cubre las tres filas que la página sólo **pinta**: `Team.tsx` pinta `src={member.photoUrl}` en el camino por defecto, `Testimonials.tsx` pinta `[...Array(review.rating)]`, `Navbar.tsx` resuelve `navbar.variant: "v6"` al módulo `navbar-v6` por `pickVariantModule` —las dos funciones puras de `section-variants.ts` se importan con `../config/site` sustituido por un doble vacío, que `resolveVariant` no toca— y el fixture A trae las tres fotos en Storage, las ocho reseñas con `rating` 1–5 y `navbar.variant: "v6"`. Ningún código de producción cambia en ninguno de los dos repos. **Las copias `d` dejan de enumerar a mano las órdenes vivas** (hallazgo de A, inciso m): `tests/conexion-05-d`, la nueva `tests/conexion-06-d` y, con el mismo arreglo, `tests/conexion-04-d`, `tests/verdad-07-a` y `tests/verdad-07-d` excluyen de su reproducción de HEAD toda carpeta `tests/orden/<id>/` cuyo id no figure en `HEAD:tests/orden/APROBADAS.md` —calculado en el momento, sin lista escrita a mano— más la propia orden que promueven; una lista fija se rompía con cada orden nueva. CONEXION-06 queda aprobada en `tests/orden/APROBADAS.md` (T b5b78f7 · H 07739a1) y promovida a `npm test` como `tests/conexion-06-{a,d,e}.test.ts` en H (9 tests) y `{b,d,e}` en T (5; D-78); las copias que fijaban «20/36» (conexion-02-b, 03-b, 04-b, 05-b y la nueva 06-b) quedan en 26/36 y excluyen las seis filas nuevas de «las otras filas no cambian». El bloque CONTRATO-DECLARADO no cambia.

<!-- CONTRATO-DECLARADO: tests/contrato-hooks.test.ts lo verifica contra .claude/settings.json,
     .githooks/ (archivos y los `node tools/…mjs` que ejecuta cada uno) y package.json. NO editar a mano sin correr ese guard. -->
```ini
hook.SessionStart = * :: arranque.mjs
hook.PreToolUse   = Edit|Write|MultiEdit|Bash|PowerShell :: candado.mjs
hook.Stop         = * :: cierre.mjs
githooks          = pre-commit pre-push
githook.pre-commit = (ninguno)
githook.pre-push  = destino-no-despliega.mjs rojo-verde.mjs --todas
git.hooksPath     = .githooks (npm prepare)
```

**Regla de arranque.** `git fetch` + `git status` antes de tocar nada; suciedad o commits sin push se resuelven primero. Todo commit en `main` se pushea en el mismo turno, esté o no aprobado el sub-bloque (Liam, 2026-09-18, `bloque-04/4.3.md` § Regla de push corregida; la aprobación vive en PLAN.md y en `4.x.md`, no en el remoto; el push no despliega, ver «Estado y ramas»).

## Leyes (de vendamos-agente, con su porqué)

- **No suponer nada.** Todo estado que se reporta (push, deploy, verde, regresión) se verifica con un comando EN EL MOMENTO y se cita la salida real. El deploy se verifica por ESTADO del deployment, nunca por hash. Lo que no se puede verificar se declara «no verificable». Un reporte con un dato supuesto es un reporte FALSO. *Por qué:* el 2026-08-01 en Vendamos se afirmó un costo como medido cuando era una división entre dos poblaciones distintas; aquí, el 2026-09-10, cada hueco de verificación remota costó una ronda de STOP.
- **Nada está terminado sin probarlo desde ángulos distintos.** Ángulos, no repeticiones: mutación (romper a propósito lo que el guard vigila y verlo ROJO), las dos direcciones (que detecte lo que debe Y que no detecte lo que no), contra lo real, de punta a punta, y el exit code sin pipe. Antes de decir «terminado» se listan los ángulos y su resultado; con menos de dos independientes está escrito, no terminado. *Por qué:* ya pasó que un test pasara por el motivo equivocado (un `match` contra el SQL que Alembic imprime quedaba verde con el guard desarmado; un `assert password not in mensaje` pasó por casualidad al cambiar un recorte). Las dos las caza la mutación, no la lectura.
- **No se ejecuta sin un «andá» explícito.** Una pregunta se contesta, una duda se piensa en voz alta, una idea se discute; NINGUNA se implementa. *«¿es necesario…?», «¿no hay manera de…?», «¿qué opinás?», «¿cuánto falta?»* → respuesta, aunque la solución esté a tres ediciones. *«¿podrías…?»* como consulta → si se puede y qué costaría; no hacerlo. *«dale», «hacelo», «seguí», «ok», «andá»* → recién ahí. La autorización es POR TRABAJO y no se estira: un «ok» a X no habilita Y ni lo que se me ocurre mientras hago X. Excepción única: una orden abierta de Liam («si encontrás algo más hacelo directo») vale hasta que ese trabajo termina. *Por qué:* salir corriendo le saca a Liam la decisión de las manos —él buscaba una solución, no pedía una— y gasta trabajo en una dirección que quizá no era la suya; pasó dos veces el mismo día en Vendamos (2026-08-06).
- **Disparador de regla general.** Cuando Liam corrige algo que se puede enunciar sin nombrar cliente, sección ni variante concreta, la sesión responde antes de seguir con la frase fija: «Esto parece una regla general de <diseño|trabajo>; propongo agregarla a <DISENO-REGLAS.md|PLAN.md § Cómo se trabaja> así: "<texto>". ¿Va?». Sólo entra con el sí de Liam; si dice no, se anota en el mismo archivo como «propuesta rechazada» con fecha, para no volver a proponerla. Las correcciones de un cliente o de un detalle puntual no se proponen.

## Secuencia y bloque abierto

Ver `C:/Users/liama/Desktop/Nichos/PLAN.md`.
