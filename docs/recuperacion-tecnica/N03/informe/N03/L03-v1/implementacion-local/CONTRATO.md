# L03 — implementación local autorizada

Liam aceptó la preparación y autorizó exclusivamente src/components/client-config-tab.tsx: retirar los tres interruptores y añadir el texto aprobado. Conservar datos/defaults/tipos/carga/payload/adminEmail y demás campos. No backend/D05, migraciones, instalaciones, efectos externos,push/deploy. Techo actualizado90% TOTAL compartido N03, último10% reservado aN04,sin resets. N03 y etapas posteriores/condición por tenant abiertas/vigentes. No descontarL03; entregar para revisión.

P0: preimagen/fuentes, contrato render/guardado RED previo refutado y congelado. P1: reemplazar sólo los tres nodos por nota. P2: GREEN del SSR real y handler de guardado real, mutante que agrega los tres controles sin quitar nota→RED por controles; payloadPASS. P3: tipos/lint pertinentes, revisión y custodia/registros. Cuatro gates materiales, sin otra preparación.

Oracle fijado: nota exacta «Sin agente, los avisos base de contacto y reservas usan email. Con agente, usan los canales configurados.» visible al expandir; ningún interruptor con las tres etiquetas; sólo botón de apertura de sección, adminEmail y resto preservados. Cerrada conserva ocultación de contenido. SSR usa JSX y subcomponentes reales/ReactDOMServer, no búsqueda de código como sustituto de render.

29 casos:27combinaciones de flags true/false/ausencia con canales/hermanos/otros campos; notifications ausente/adminEmailvacío; sección cerrada. handleConfirmedSave real→capturaPUT con igualdad completa a datos iniciales excepto _customServicesBackup (comportamiento previo). No agregar campos ausentes. Byteigualdad de prefijo/sufijo fuera de los tres nodos garantiza tipos/defaults/carga/save y demás campos sin cambios. Backend/D05 por hashes; pruebas aceptadas reutilizadas.

RED exit1 sólo diferencias de render; fallo instrumentalexit2. GREENexit0 y payload exacto. Refutación interna Newton: sin BLOCKERS; incorporados expandida/colapsada, ausencia demapa/flags, mutante conserva nota. Alcance y texto sin cambios desde autorización. Límites: SSR de sección y guardado extraído, no navegador/hidratación/DB/entrega real. D05 no se recertifica ni se toca.
