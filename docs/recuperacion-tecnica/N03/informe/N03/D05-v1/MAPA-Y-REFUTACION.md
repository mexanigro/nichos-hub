# Mapa focal de D05 y contraste independiente
- Reserva pública: BookingWizard → POST/api/book (entrega conservada) → POST/api/notify-booking. Canales resueltos por getChannelConfig y defaults; confirmación cliente WhatsApp por defecto → notifyAgentAppointmentBooked → postToAgent. server lee appointment almacenado; API usa details enviados. Divergencia registrada, no corregida lateralmente ni usada como autorización.
- CRM agenda/cliente: AdminDashboard y CustomersTab → appointment-notify-client → POST/api/appointment/notify (booked/cancelled/rescheduled) → helpers de agente. Canales/config no distinguen base nueva de compromiso antiguo.
- Contacto: ContactHub, QuickInquiry, variantes contacto y QuoteRequestModal → POST/api/contact → inbox y notificación email; si canalnew_lead_owner whatsapp/both y destinatario, notifyAgentLeadCreated → agente.
- Asistencia opcional presente en UI base: Chatbot → /api/ai/chat y /api/ai/action; BookingWizard → aiService.getStyleConsultation → /api/ai/analyze. aiService expone además análisis CRM. /api/knowledge/upload procesa conocimiento con Gemini. Los endpoints Gemini se habilitan con GEMINI_API_KEY; el modo admin tiene guard propio y no es habilitación del servicio. No se usan mode/businessContext/liveData del visitante como permiso.
- Salidas: Gemini HTTP en ambos runtimes (geminiGenerateContent y variante tools); agente vía notify-agentkit.ts en server y copia inline API (getAgentkitConfig/postToAgent). URL+secret+clientId son configuración técnica, no un derecho contratado. Email transaccional no se confunde con consumo IA/agente.
- Autoridad candidata insuficiente: features en SiteConfig son UI; notifications.enabled/bookingAlerts/contactInquiries no son entitlements IA; notifications.channels elige canales con defaults WhatsApp. Hub PlanType conserva categorías legacy y getPlanAmount sólo fija monto; provision crea clients activo/configfeatures, no clasifica permisos. No se modifica provisioning ni se inventa interpretación de planes.
- Compromisos: decisiones-alcance-v1/DECISIONES.md líneas12–18 aplica NOT-A/email sin agente a nueva oferta, conserva existentes y no acredita reconciliación por tenant.
Revisor /root/refutar_soporte, lectura en esta sesión: BLOCKER de autoridad. No encontró registro vigente que distinga nueva base/servicios anteriores; usar claves, plan o ausencia de flags inventaría política o apagaría prestaciones. Decisión necesaria: fuente controlada por servidor y tratamiento de tenants sin clasificación. No hubo voto de aceptación ni autorización para corte.
Límites: mapa de llamadas reales y fuentes locales, sin contratos/clientes remotos verificados. Sondeo directo chat demuestra sólo camino examinado, no todos los recorridos base. Plan/contrato todavía no congelados. Reconocimiento sin implementación, no auditoría general.
