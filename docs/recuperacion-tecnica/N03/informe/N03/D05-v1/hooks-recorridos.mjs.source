export async function resolve(specifier,context,next){if(['firebase-admin/app','firebase-admin/firestore'].includes(specifier))return {url:'file:///control/fixture-recorridos.mjs',shortCircuit:true};return next(specifier,context);}
