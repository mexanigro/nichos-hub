# D05 — contrato congelado

P0 alcanzado el 2026-09-09, antes de producto. Política y autorización: mensaje de Liam reproducido en CONTINUACION-APROBADA.md. Objetivo: producto base sin consumo IA/agente por claves; permisos independientes y exclusivos del operador servidor, sólo true exacto.

Alcance final: server.ts, api/index.ts, nuevo src/lib/api/optional-services.ts, src/lib/notify-agentkit.ts, src/lib/appointment-notify-client.ts y src/components/admin/AdminDashboard.tsx. Guard IA en analyze/chat/action/upload después de auth donde existe; guard agente en resolución privada; canales email base antes del cache cuando agente no habilitado. Helper CRM transmite token existente y email disponible; cancelación y reprogramación conservan prev.customerEmail; notify-booking servidor usa serviceId persistido como último respaldo del nombre. No cambiar registro de rutas/middleware, book/support/stock/health, planes comerciales, provisioning, tenant policies ni UI de captura.

Gates congelados: P0 contrato RED refutado y freeze; P1 seis archivos con controles/conexiones mínimos y tipos; P2 recorridos efectivos ambos runtimes, positivos/adversos/observador/mutación aislada; P3 revisión claridad/conservación, paridad y registros completos. Cierre sólo 4/4 y contrato GREEN.

Acceptance C1-C6 de CONTINUACION-APROBADA.md, precisión C3: email disponible implica intento al destinatario/evento esperado; walk-in sin email conserva operación y no inventa destinatario ni promete entrega. Reserva persiste primero con handler aceptado y su notificación consume ese resultado. No se certifican todos los eventos, UI íntegra, transacciones reales o entrega remota.

Instrumentos: runner-matriz.mjs (siete configuraciones por runtime, directo/auth/origen/tenant/cuota, contacto/reserva/CRM, contadores y observador); runner-recorridos.mjs (consumidor CRM real transp compilado, llamadas HTTP al runtime completo, argumentos callers por AST, documento creado realmente por handler, email/evento/destinatario y ausencia); tipos con declaración Vite, paridad pertinente y mutación del permiso compartido sólo en copia. Red existente válido se conserva: sondeo-origen ambos runtimes; matriz inicial96PASS72FAIL; base inicial14PASS6FAIL; base refinada19PASS15FAIL. Corrida con encabezado duplicado es diagnóstica inválida y no sustenta el defecto. Refinamiento de matriz exige los códigos exactos ya medidos400/503, no admite cualquier error, y delta IA por ruta.

Refutación independiente: /root/refutar_soporte (Harvey), mensajes de esta continuación, sólo lectura. Hallazgos MATERIAL resueltos: permisos exactos independientes; canales base antes de cache; contar email por evento/destinatario; preservar walk-ins sin email; controles de protecciones en ambos estados; códigos concretos de rutas opcionales; cero intentos simulados, además de exterior bloqueado; argumentos CRM por AST. Sin BLOCKER de alcance. Revisión del integrador: no usar claves/planes como compromiso; ninguna evidencia de fixture certifica remoto. No se modifica contrato para ajustar implementación.

Condición previa al despliegue: presentar tenants/servicios a conservar, evidencia y decisión de Liam sobre excepciones. No clasificar ni cambiar entornos reales ahora. N03 sigue en curso. Presupuesto TOTAL74%, observado72%, sin resets. No remoto, instalaciones, commit/push/deploy.
