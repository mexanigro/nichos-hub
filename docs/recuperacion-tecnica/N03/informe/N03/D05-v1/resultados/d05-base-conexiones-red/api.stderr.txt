  ✗  [optional] GEMINI_API_KEY                       → AI chat & style consultation (DISABLED — add key to .env)
  ✗  [optional] STRIPE_SECRET_KEY                    → Stripe payments (DISABLED — add key to .env)
  ✗  [optional] STRIPE_WEBHOOK_SECRET                → Stripe webhook verification (DISABLED — add key to .env)
  ✗  [optional] VITE_STRIPE_PUBLISHABLE_KEY          → Stripe frontend (DISABLED — add key to .env)
  ✗  [optional] VITE_ADMIN_EMAIL                     → Admin panel access (DISABLED — add key to .env)
[Kill-switch] PROJECT_ID or CLIENT_ID missing — skipping kill-switch, defaulting active.
[Kill-switch] FIREBASE_SERVICE_ACCOUNT_EMAIL / KEY not set — kill-switch disabled (degraded mode).
[Kill-switch] FIREBASE_SERVICE_ACCOUNT_EMAIL / KEY not set — kill-switch disabled (degraded mode).
[Kill-switch] FIREBASE_SERVICE_ACCOUNT_EMAIL / KEY not set — kill-switch disabled (degraded mode).
[Kill-switch] PROJECT_ID or CLIENT_ID missing — skipping kill-switch, defaulting active.
[Kill-switch] PROJECT_ID or CLIENT_ID missing — skipping kill-switch, defaulting active.
[Kill-switch] FIREBASE_SERVICE_ACCOUNT_EMAIL / KEY not set — kill-switch disabled (degraded mode).
[Kill-switch] FIREBASE_SERVICE_ACCOUNT_EMAIL / KEY not set — kill-switch disabled (degraded mode).
[Kill-switch] PROJECT_ID or CLIENT_ID missing — skipping kill-switch, defaulting active.
[Kill-switch] FIREBASE_SERVICE_ACCOUNT_EMAIL / KEY not set — kill-switch disabled (degraded mode).
[Kill-switch] PROJECT_ID or CLIENT_ID missing — skipping kill-switch, defaulting active.
[Kill-switch] FIREBASE_SERVICE_ACCOUNT_EMAIL / KEY not set — kill-switch disabled (degraded mode).
[Kill-switch] PROJECT_ID or CLIENT_ID missing — skipping kill-switch, defaulting active.
[Kill-switch] FIREBASE_SERVICE_ACCOUNT_EMAIL / KEY not set — kill-switch disabled (degraded mode).
