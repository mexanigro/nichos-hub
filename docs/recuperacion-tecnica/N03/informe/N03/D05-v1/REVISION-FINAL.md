# D05 — revisión final y procedencia

Revisor independiente: agente /root/refutar_soporte (Harvey), misma tarea, sólo lectura. Preparación: política exacta, independencia de permisos, canales antes de cache, destinatario/evento observable y limitación de walk-ins. Segunda revisión ejecutable: códigos concretos en rutas opcionales, delta de intentos por ruta, emails por evento, argumentos CRM por AST y ausencia de intentos simulados. Incorporados antes de freeze. Revisión final de cinco preimágenes existentes y módulo nuevo: «BLOCKERS: ninguno. MATERIAL: ninguno demostrado»; GREEN condicionado a corridas, ahora completas. No se atribuye autorización comercial ni certificación remota al revisor.

Integrador, segunda pasada desde tres ángulos: deuda/alcance (C1-C6 y condición de despliegue cubiertos, N03 restante abierto); ejecución (222 controles del contrato, 21 salud, tipos y paridad); adversarial (flags inválidos, credenciales sin permiso, servicios separados, auth/origen/tenant/cuota, mutación sensible). Revisión de claridad: sólo dos exports nuevos con consumidores reales; sin código muerto agregado, flags del entorno servidor exclusivamente, helper descrito como avisos base y agente opcional. Rama habilitada mantiene canales existentes. Se documenta fallback de etiqueta serviceId y limitación sin email.

Conservación: comparación AST registra mismo orden y cuerpos de middleware/rutas no modificadas; 41 declaraciones no afectadas en server y 42 en API. Los handlers de book/support/stock y runtime-health conservan huellas. Comparación física de copia saneada contra producto:332 archivos iguales, incluidas modificaciones ajenas conservadas. HEAD no cambió. No se repiten suites heredadas cuyo código/contrato permanece válido; se ejecuta salud configurada porque los imports de entrada cambiaron.

Instrumentos y detector de bucles: primera edición se detuvo antes de modificar archivos existentes al encontrar una lectura de clave anidada adicional; continuó una vez con anclaje de línea exacto, verificando preimágenes. El primer adaptador CRM duplicó Content-Type; corrigió normalización antes del freeze, sin usar ese400 como evidencia de producto. Auditoría final inicial confundió lectura general de config por runtime con lectura de preferencias de canal: el producto también lee config en otras funciones existentes; r2 verifica el primer statement de getChannelConfig y conserva la corrida inicial. No se relajó el contrato ni se modificó producto para corregir el auditor. Cada continuación aportó evidencia o cambio material; no hubo dos ciclos consecutivos sin avance.

Límites: dependencias, certificados/tokens, DB y proveedores sintéticos dentro de Docker sin red, procesos reales de ambos runtimes. No entrega de email/WhatsApp real, persistencia/transacciones remotas, navegador íntegro ni clasificación comercial de tenants. Walk-ins sin email mantienen operación y no reciben un destinatario inventado. Estado de entrega agente false si no hubo configuración suficiente; success de operación base no se interpreta como entrega del agente.
