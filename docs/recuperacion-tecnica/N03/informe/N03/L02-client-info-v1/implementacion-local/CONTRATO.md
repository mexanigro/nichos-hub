# L02 — ejecución local autorizada P0→P3

Liam aceptó la preparación y autorizó ejecutar PROPUESTA.md exclusivamente en src/app/api/onboarding/client-info/route.ts. Aceptación de preparación no es aceptación de reparación. No descontar L02, ni preparar otra acción. N03 abierta. Techo85% TOTAL compartido, inicio82%,sin resets,instalaciones,efectos externos,push/deploy. N04/N08/N10 y condición sobre prestaciones por tenant antes del despliegue vigentes.

## Gates y cambios observables

P0: completar controles fijos junto a este expediente y ejecutar RED basal antes de producto. Inputs/VECTORES-BASALES son previos al cambio, el oráculo fijos no lee el handler para decidir valores. Captura SDK/consumidor real del arnés aceptado reutilizada. Deben funcionar29 escenarios sin error instrumental, detectar fallo de config y hub por separado, además de demostrar positivos SDK y guardias. Refutar y congelar hashes; P0 incompleto impide P1.
P1: sólo route.ts construye mapas explícitos en configUpdate/hubUpdate. Cada rama opcional se adjunta sólo con hojas; preservar condiciones/defaults/arrays/sentinelas/set+merge. No convertidor ni migración. Registrar delta y preimagen.
P2: contrato fijo GREEN con todos sus escenarios; mutantes del handler final restauran por separado los bloques literales basales de config y hub. Deben volver a RED semántico atribuible a cada colección, no a importación, guardias ni instrumento. No modificar expectativas tras ver la implementación.
P3: tipos/lint pertinentes, lectura completa y refutación final, censo SHA (único src permitido), preimágenes/resultados/registros archivados; entregar para revisión sin descontar L02.

## Oracle fijo y población

21 escenarios anteriores + contador ausente, sin cambio, sólo email, sólo calle, sólo FAQ, historial truncado110 hojas hours, hours explícito vacío, nicho otro. Total29 escenarios/39 invocaciones. Includes matriz alta/resubmit x config/hub ausentes/existentes, fallback/lookup legacy, omisiones/contact/address/arrays vacíos, guardias token/tenant/locale/status y llamada11=429 sin nuevos efectos. Sin-cambio se precarga desde valores del vector fijo (no ejecutando el handler para obtener expectativas).

Cada write config/hub: máscara exacta por segmentos, hojas decodificadas exactas y transforms protobuf exactos; estado completo esperado a partir de preimagen+vector fijo. Eso comprueba hojas/hermanos/arrays/imágenes no emitidos, coexistencia literal/anidado sin borrar literales antiguos, timestamps previos y todos los defaults. Comparación adicional del lector completo; sus campos simples se proyectan explícitamente del estado esperado y arrays se contrastan con el lector real custodiado sobre ese estado fijo. Nunca derivar expectativas de writes finales ni considerar suficiente ausencia de puntos.

Historial: contenido completo esperado calculado por diff/summarizeValue reales y custodiados sobre preimagen+estado fijo, incluyendo paths/summary/count/truncated y cero registros sin cambio. Status_history payload completo; clients estado/timestamp; emails tags/destinatarios; contador ausente→increment1 y contador3→4. El arnés add sigue modelado como set: evidencia sobre payload/secuencia, no precondición create ni persistencia.

SDK instalado real produce fields/updateMask/updateTransforms sin commit. El modelo de aplicación usa segmentos SDK sin split literal, timestamp sintético fijo y contador numérico acotado. Sin DB, red ni correos reales. No prueba reglas, identidad remota, atomicidad, concurrencia, persistencia, latencias ni entrega. Estas obligaciones permanecen en sus etapas; no expandir el cambio para certificarlas aquí.

RED = diferencias semánticas guardadas, exit1. GREEN = cero diferencias y comprobaciones auxiliares completas, exit0. Error del instrumento = exit2, nunca RED válido. Mutación válida requiere instrumento operativo y fallo en la colección deliberadamente alterada. Registro del freeze identifica versión exacta de instrumentos/vectores y RED vigente.
