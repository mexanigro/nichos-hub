# N03 — reparación exclusiva de 13 diagnósticos lint

Autorización: mensaje actual de Liam; seis archivos exactos en FUENTES.json, procedentes de reconciliacion-v1/SIGUIENTE-ACCION.md. Preservar interfaces públicas, autorización, respuestas/filtros/serialización, textos e importes. Sin reglas nuevas, supresiones ni usos artificiales. Techo TOTAL74% N03, observado73%; sin resets/remoto/install/cobros/notificaciones/commit/push/deploy. Las entregas aceptadas y condición de prestaciones por tenant antes del despliegue quedan intactas.

Plan/gates: P0 preimágenes verificadas,61 controles de conservación y tipos preparatorios, refutación/freeze. P1 cambios mínimos en seis archivos y lint sin diagnósticos. P2 tipos, conservación y mutación aislada. P3 revisión final/custodia/registros y cierre sólo de esta reparación. El resto N03 conserva el balance previo.

Aceptación: A1 seis archivos,0errores/0advertencias con configuración exacta N02/local-v4/eslint-hub-diagnostico.mjs; RED existente1error12warnings se reutiliza por huellas. A2 tipos pertinentes con tsconfig estricto heredado, next-env.d.ts y sus imports; incremental=false sólo evita emisión de caché, no relaja tipos. A3 GET real registrado y withRole/withOwner extraídos por AST con sesión/DB/respuesta controladas: autorización401/403 antes de DB, lista/filtros/pendientes, timestamps, campos extra, orden de consulta y fallo DB conservados. No certificar Firestore ni auth externa. A4 textos/versiones exactos de5 idiomas×6planes y fallback×6; precios/constantes/tier/booleanos conservados contra preimagen; firmas TypeScript públicas compatibles. A5 mutación any en archivo copiado, comprobado por mismo lint, sin tocar producto. A6 revisión de diff/AST y preimágenes antes de escritura, sin cambios fuera de seis archivos.

Cambio previsto: any→unknown en aserción GET, sin modificar lógica; retirar imports/argumentos internos sin uso; retirar PLAN_TABLE y cinco parámetros privados de contratos, manteniendo contractFns y getContract; getPaymentAmount usa sobrecarga pública boolean con implementación sin argumento vinculado. Su firma TypeScript y resultado permanecen; Function.length cambia1→0, no se afirma identidad introspectiva. El censo del revisor no encontró usos introspectivos; no se añade void, condición tautológica ni suppress. El parámetro nominal de la declaración de sobrecarga describe la interfaz, no es un uso artificial.

Pruebas antes:61PASS en resultados/antes.json y tipos exit0. Rojo lint previo íntegro en ../reconciliacion-v1/LINT-ACTUAL.json. Instrumento acotado a GET/contratos/precios; no arnés general, no suites heredadas repetidas. Primer intento de instrumento tuvo cierre de objeto incorrecto; preimagen conservada, corrección única con sintaxis válida y61PASS. No fue fallo de producto ni RED de aceptación.

Refutación de diseño: /root/refutar_soporte (Harvey), lectura completa de contracts.ts; tabla/parámetros no intervienen en textos, sobrecarga estándar sin usos artificiales. Aridad JS clasificada NON-BLOCKING y explicitada arriba. Revisión ejecutable final se integra en FREEZE.md. No se congelará con BLOCKER/MATERIAL pendiente.
