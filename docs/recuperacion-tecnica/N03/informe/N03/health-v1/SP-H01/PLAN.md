# SP-H01 — adenda autorizada del contrato health-v1
Reapertura única N03/health-v1. Consumo inicial70%, techo total74 sin resets. Producto permitido: server.ts, api/index.ts, src/lib/api/runtime-health.ts. No otros consumidores/rutas/políticas. Contrato A1–A6 anterior se conserva, precisando que readiness200/503 aplica a solicitudes permitidas; el exceso devuelve429 antes de loader/lectura.
P0: reproducir ambos runtimes, refutar y congelar adenda. P1: reinstalar limitador real antes de la sonda y fuera del bootstrap funcional/tenant. P2: exceso/mutación y regresiones afectadas reales. P3: tipos/paridad, conservación, revisión y registros.
S1: valores por defecto60/60000 y configurados3/1000; primeras solicitudes permitidas dan200/503 coherente con dependencia. Exceso429 con cuerpo original genérico, cero nuevas cargas/lecturas. Expiración configurada permite nuevo intento; separación por IP y claves de ruta vigente. No duplicar contadores ni limitar dos veces.
S2: montar señales en Router bajo /api para conservar req.path del limitador original. Usar exactamente la función rateLimit de cada runtime, sin alterar su implementación, defaults/configuración/getClientIp ni otras rutas. Prefijo /API equivalente debe compartir cuota previa; caso/trailing de la ruta conserva semántica Express existente.
S3: /api/live queda explícitamente sin cuota: respuesta constante200, sin DB y sin consumir cuota de health. Salud limitada sigue antes de bootstrap/tenant; inicialización temprana/tardía/incompleta conserva503 permitido y guard funcional503; exceso429 incluso cuando bootstrap falla.
S4: corrida de ambos runtimes completos en Docker aislado ya disponible, sin.env/credenciales/remoto, dependencias controladas. Contadores loader/read antes/después del exceso. Mutación exclusiva de copia desarma el middleware de límite: positivos pasan y exceso deja de429 y provoca lecturas detectadas. Reejecutar regresión health afectada e incomplete; conservar evidencia book/support/stock y mutación bootstrap no modificados.
S5: tipos heredados con vite-env y paridad pertinente; AST de otras rutas, función rateLimit/config/IP y huellas de handlers/consumidores previos conservadas. Revisión independiente del orden y de la pérdida de protecciones.
Omisión previa comprobada: revisión cubrió disponibilidad, exposición y cuerpos de rutas, pero no la pérdida del middleware protector al adelantar señales. El contrato no contenía presupuesto de lecturas sucesivas; single-flight sólo limitaba concurrencia. El cierre previo deja de sostener protección de health hasta resolver SP-H01. Live comparte cambio de orden pero no usa DB; política explícita anterior se conserva, sin auditoría general.
Refutación integrada: sondeo de arranque por /api/live e IP de preparación separada; HEAD salud comparte cuota GET. Router conserva alias de prefijo/API; no normaliza /HEALTH o trailing. Límites en memoria por instancia conservados. Revisor /root/refutar_soporte reconoce omisión previa y requiere estos controles. Sin BLOCKERS restantes.
