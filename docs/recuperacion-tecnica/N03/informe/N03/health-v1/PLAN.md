# N03 health-v1 — plan y contrato preparatorio
Orden: arranque/salud acotados server.ts y api/index.ts sin configuración LLM/agente, máximo total74 sin resets. Misma N03; book/support/stock conservados. Sin UI, otras funciones, políticas tenant, remoto/install/Git/deploy.
Terreno: API exige GEMINI al diagnóstico production y captura bootstrap después de rutas; ambos health constantes no verifican DB; API filtra clientId por body/header. Monitor apiCheck usa HTTP200+statusok/healthy; no consume clientId. Se conserva contrato de éxito y se usa503 para falta de disponibilidad; nueva /api/live mantiene liveness independiente.
P0: plan/arnés aislado viable, RED de arranque/salud y refutación/freeze. P1: señales comunes, bootstrap explícito y dependencia opcional fuera de required. P2: arranque EFECTIVO de ambos runtimes y aceptación/mutación. P3: tipos/paridad y regresiones pertinentes, revisión/huellas/acta/registros.
Producto permitido: server.ts, api/index.ts y nuevo src/lib/api/runtime-health.ts. Posibles tests/arnés/registros en health-v1. No cambiar loaders/selectores tenant ni módulos proveedores. Bootstraps de salud se instalan fuera/antes de registerExpressRoutes; flag listo sólo tras registro+diagnóstico y setup local completados. Un guard anterior a rutas funcionales bloquea503 tras inicialización incompleta/fallida.
A1: lanzar server.ts como entrada CLI real, e importar api/index.ts completo y atender vía export default real en HTTP local, NODE_ENV=production, entorno vacío salvo campos saneados. Sin GEMINI/Anthropic/OpenAI/agente. Ambos responden live200 y health200 con dependencia controlada disponible y tenant configurado.
A2: readiness definida exclusivamente como bootstrap completo + clientId del runtime + loaderAdmin existente resuelto + lectura clients/{clientId} resuelta con documento existente y estado activo/trial/maintenance, los estados operables del guard existente. No equivale a permisos de escritura, reservas integrales, REST ni certificación de todas funciones. Ausencia configuración/DB/doc, estado bloqueado/desconocido, rechazo/carga fallida o pendiente→503 {status:'unavailable'}. No exponer id, keys, mensajes internos en body/header. Live no lee DB y siempre200 {status:'ok'} si proceso atiende.
A3: deadline1500ms cubre loader+lectura, respuesta antes de3s; lectura pendiente no significa éxito ni bloquea live. Recuperación de fallo a disponible se observa sin caché positivo obsoleto. Timeout limita respuesta; no promete cancelar SDK. Reutilizar a lo sumo una operación pendiente por proceso para evitar multiplicación de sondas colgadas.
A4: falla de registro temprana/tardía en entorno aislado: live200,health503 y rutas funcionales503 sin dejar rutas parcialmente registradas operativas. Handler exportado carga completo; no extracción aislada como evidencia de arranque. Servermissingtenant también inicia señal de fallo.
A5: cero INTENTOS de salidas a proveedores durante arranque y consultas live/health examinadas; hooks de red auditan/bloquean y Docker networknone impide exterior. Control adverso del observador intenta una URL externa sintética y se rechaza antes de red. Sensibilidad: mutar condición real de bootstrap sólo en copia aislada y observar falso health200 detectado. No se muta producto para desarme.
A6: consumidor monitor real apiCheck ejecutado con transporte/DB propios controlados contra respuestas de runtime; statusok200 acepta,503 rechaza. Ambos health/live sin headers de tenant/credenciales ni errores detallados. Tipos con vite-env desdeinicio; paridad pertinente. Conservar otras rutas y entregas anteriores por fuente/diff, no repetir suites intactas.
Aislamiento: Docker imagen local fija16e22a..., pullnever/networknone/read-only/capdrop/no-new-privileges/UID65534/4GiB/2CPU/128pids, /tmp limitado, sin.env ni credenciales reales, fuentes saneadas RO+depsN02 RO+complemento RO+salida propia. Sólo API Firebase Admin sustituida por módulo controlado; imports y bootstrap producto restantes reales. Casos producción; Vite dev/HMR no forma parte del runtime base publicado examinado. Ninguna operación remota se certifica.
Custodia: manifiesto fuentes y preimágenes antes deescritura. Cada corrida conserva nombre/resultados; no repetir estrategia sininfo; dos ciclos sin avance→STOP. Contrato final se congela tras refutación; no cambiar objetivo para conseguirverde. Reserva cerrar antes74, consultar encheckpoints.Refutación integrada: contadores loader/read; dos health concurrentes con pendiente y live sin lecturas adicionales; trial/maintenance positivos; cuerpos exactos sin headers internos en live y bootstrap fallido; dns.promises bloqueado. Inicialización incompleta se instrumenta únicamente en copia aislada desarmando complete(), conservando registro completo: live200,health503 y guard503. RED definitivo usa captura JSON/no-JSON para no cortar población ante fallback HTML. Cero intentos se refiere a fetch, HTTP(S), socket, DNS y UDP instrumentados durante el recorrido; networknone agrega aislamiento físico. No certifica actividad fuera del recorrido.
