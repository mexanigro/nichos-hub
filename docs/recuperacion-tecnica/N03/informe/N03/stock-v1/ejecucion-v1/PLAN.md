# N03 stock-v1 — plan y aceptación previa
Orden vigente: preparar/implementar exclusivamente POST /api/stock/add y GET /api/stock/items en ambos runtimes; presupuesto69→máximo74 sin resets, consultas por checkpoints, reservar margen de cierre. El alcance local y el mismo ID conservados. Método vivo r2 sin deriva.
Terreno ya reconocido: StockTab y services/stock usan Firestore directamente; no se cambian ni se afirma reparar UI. Rutas sólo server, exceptuadas. Mantener layouts, autorización y selección tenant existentes. Helpers listStockItemsByClient/fuzzyMatchStock ya disponibles en stock-tools; inyectarlos, importando sólo tipos en módulo nuevo, sin iniciar IA ni ampliar dependencias.

P0: contrato ejecutable con RED, arnés de tipos viable y refutación integrada/freeze. Cambian sólo expediente/controles. Observación: API ausente y defecto commit/reintento reproducidos, tipos de referencia verdes.
P1: extraer dos factories a src/lib/api/stock-handlers.ts, registrarlas en server.ts/api/index.ts y retirar sólo2excepciones. POST resultado exitoso agregado después de await runTransaction, una fila por entrada. Observación: casos de ambos registros coherentes.
P2: contrato local final y mutación sensible de tenant/transacción, sin cambiar oráculo. Observación: positivos/adversos aprobados y decisión desarmada detectada.
P3: tipos/paridad pertinentes, revisión independiente de claridad/alcance, huellas/registros/acta. Observación:4/4local, N03 general aún pendiente.

Superficie producto: nuevo src/lib/api/stock-handlers.ts, server.ts, api/index.ts, tests/api-parity.test.ts. Consumidores y stock-tools intactos. Reversa: preimágenes físicas de cada escritura; comprobar concurrencia antes de modificar. Sin remoto/install/commit/push/deploy. Evidencia book/support conservada; comprobar sólo lo afectado por diff.

A1: registros reales extraídos con AST, no duplicados inventados. Factories importadas/usadas, parser16kb conservado POST. Tipos resuelven ambos runtimes y src/vite-env.d.ts. RED: API carece de2rutas.
A2 POST: auth rechazada antes de cargar DB; tenant vacío400; array inválido400 y >50 413; itemId trim, cantidad Number finita>0 (conservar string numérico/fracción), reason200; tenant del runtime ignora body. Flat propio y fallback legacy sólo si flat falta; flat ajeno207 forbidden sin writes ni fallback. No encontrado207. Válido200 con1resultado/entrada, auditoría original. DBnull503 después de auth; carga500; read/commit fallidos207 sin éxito de esa entrada. Mezcla200/fracaso→207, orden conservado. Reintento callback2veces:1resultado de ÚLTIMO intento y sólo commit final; no éxito antes de resolver transacción. Mock no certifica atomicidad/contención real.
A3 GET: auth antes de lectura, tenant vacío400, DBnull503 y lectura500. Helper real filtra tenant, ignora query tenant ajeno, no hay fallback legacy nuevo. Sin búsqueda/null/array→lista; string trim y fuzzy real none/single/multiple/acento/tokens; cap50 y total previo al cap. Sin escritura.
A4: mutación aislada del guard flat-tenant debe producir writes ajenos y fallo específico; mutación de resultado previo al commit cubierta por RED inicial. Producto disco no se muta para el desarme.
A5: sólo estas dos excepciones retiradas; imports/factories/types reales; StockTab/servicio y book/support cuerpos intactos por huellas/diff. Revisión claridad: dependencias usadas, sin ramas/garantías nuevas sin control.

Instrumento: Node local/TypeScript existente para extraer funciones, VM con auth/DB/Timestamp controlados y bloqueo de imports no previstos. Helpers de consulta/fuzzy reales extraídos sin importar módulo IA. Casos HTTP loopback con Express real sólo para registro/parser; resto invoca handler real con req/res controlados. No bootstrap, SDK real ni credenciales. Tsc noEmit incluye vite-env desde inicio. No es certificación HTTP alojada ni DB real. Ninguna función externa autorizada.
Refutación independiente: /root/refutar_soporte, lectura, consumidor y sensibilidad de reintento. Integrar hallazgos antes de freeze. NON-BLOCKING fuera de rutas queda N03; no detener por estética o sugerencias opcionales.