# clientId-v1 — STOP de instrumento en P0, RED0/4

Liam aprobó implementar únicamente C.trim→N.trim→V.trim→vacío en getAgentkitConfig de src/lib/notify-agentkit.ts y api/index.ts; README/.env.example sólo documentación de selección. La autorización y consecuencia de nuevos intentos con AGENT_ENABLED=true/configuración completa siguen vigentes. No pedir nuevamente política ni presupuesto. Techo80% TOTAL N03, API76% inicio y cierre, sin resets.

## Estado y bloqueo exacto

No se editó producto ni documentación del template, no freeze, no gates alcanzados. MEDICION.json y preimágenes siguen vigentes. Se prepararon runner HTTP y copia de333 fuentes actuales, con infraestructura D05 reutilizada y destinos simulados. El preflight de contenedor no llegó a ejecutar Node, mucho menos los runtimes.

1. preparar-http.cjs intentó copiar firebase-applet-config.json desde D05/copia, donde no existe. Falló ENOENT; tampoco se había copiado todavía ejecutar.ps1. Error propio del instrumento, no defecto del producto. Preservado FALLO-PREPARAR-r1.json y preimagenes-arnes-p0/preparar-http.cjs.
2. Única corrección: retirar referencia inexistente y verificar por huella las copias parciales antes de continuar. Se completó copia333. Al ejecutar preflight, Docker exit125/estado contenedor128,Pid0: no pudo crear /app/node_modules para montar dependencias porque /app ya es bind readonly. Falta el directorio de montaje en copia/node_modules (Test-Path=False). Evidencia completa en resultados/preflight/ESTADO.json y stderr.txt. El contenedor fue eliminado según DISPOSICION.json.

La preparación física avanzó, pero ningún criterio nuevo de aceptación quedó acreditado: la continuación única volvió a fallar en el instrumento. Se aplica STOP y no se encadena otro arreglo. Imagen local sí existe; no es descarga pendiente ni instalación requerida. No es error del selector, auth, Firestore o runtime: sus procesos no arrancaron. El wrapper PowerShell termina con salida textual exit125 aunque su propio exit sea0; usar ESTADO.json como resultado real, no el exit de la invocación superior.

## Aceptación pendiente y refutación recibida

Referencia previa: MEDICION.json,18 casos/90 selecciones y preimágenes; no repetir ese sondeo sin causa. Conservación del id navegador/servidor y casos sin cambio contra esa medición. Los cambios previstos exigen id agente igual al servidor, con permisos exactos; permiso ausente/false/inválido o URL/secret/id faltantes debe dar cero intentos. HTTP sobre rutas realmente registradas en ambos runtimes: contacto y eventos CRM booked/cancelled/rescheduled con destinos simulados; conservar base/email, auth/origen y payload salvo id. Mutación aislada que restaure omisión NEXT/trim posterior debe fallar; tipos con vite-env.d.ts y paridad pertinentes.

Revisión interna /root/refutar_soporte (Harvey), sólo lectura, en esta continuación: sin BLOCKER de política; dos MATERIAL antes del freeze. (1) En casos originalmente omitidos no existe payload para comparar: capturar baseline con mirrors válidos y mismos datos/recorrido ANTES del freeze. (2) Contar cero intentos con permisos denegados, conservar base/email; completar URL/secret/id ausentes en funciones. Estos puntos se incorporan a la aceptación pendiente. No se atribuye ejecución al revisor. Baseline HTTP todavía NO VERIFICADO; runner preparado no equivale a prueba.

Gates conservados: P0 completar aceptación/preflight/baseline/refutación y freeze; P1 dos selectores+docs; P2 conexiones HTTP y conservación; P3 mutación/tipos/paridad/revisión/custodia. Todo producto futuro sigue condicionado a P0 válido.

## Retoma única concreta

Tras orden de retomar este STOP: corregir exclusivamente preparación del punto de montaje creando el directorio vacío C:/Users/liama/Desktop/Nichos/recuperacion-tecnica/informe/N03/clientId-v1/copia/node_modules, antes de montar /app readonly. Conservar preimagen del preparador si se incorpora allí; no quitar readonly ni network=none. Comprobar sintaxis de todos los instrumentos y rutas/dependencias de montaje. Ejecutar una nueva corrida de preflight con nombre nuevo, comprobar ESTADO.json exit0 y PREFLIGHT.json. Si pasa, capturar baseline HTTP mirrors sobre preimagen actual, completar controles de funciones/permisos/configuración y refutar/freeze; continuar P1→P3 sin otra aprobación de política. Si vuelve a fallar sin avance verificable, STOP con resultado concreto. No borrar ni sobrescribir resultados anteriores.

Esta retoma no fue ejecutada. Proyecto/base/coherencia real y prestaciones por tenant antes de despliegue siguen pendientes; N03 sigue en curso. Entregas aceptadas conservadas. Sin instalaciones, operaciones remotas, .env real, commit,push,deploy ni resets. Detenido para revisión.
