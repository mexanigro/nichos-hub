# Contrato de reparación de agenda — P0

Autorización vigente: issuer Auth navegador configurado explícitamente por servidor; email verificado ligado a admin_users backend del tenant/rol. No fallback de issuer ni altas. Dos fuentes con identidad y acciones por origen. Decisión anterior resuelta. Orden integrado N03: agenda→cobro→revisión original; últimos2puntos reservados, inicio92/100.

Interfaz fija prevista: createCrmAppointmentsHandlers({clientId,loadDb,authenticate}) devuelve list/patch RequestHandlers; GET /api/crm/appointments devuelve {appointments,source:{project,database,collection:'appointments'}}; PATCH /api/crm/appointments/:id acepta sólo status o date/time. Servicio crmAppointments.subscribe(callback,onError), update(fila,patch), refresh(); fila mantiene id de vista compuesto y origin:{source:'browser'|'booking-backend',documentId}. Datos persistidos nunca contienen origin ni id compuesto. Reusar dbService, no modificarlo. Autenticación requireAdminAuth cuarto parámetro expectedProjects?:readonly string[]; verifyFirebaseIdToken segundo. [] rechaza; sin argumento conserva default existente.

P0 aceptación de instrumento: pruebas fijadas antes de implementación, RED de endpoints ausentes y auth cruzada; ejecutabilidad y refutación de controles. El RED de módulos ausentes es falta de conexión, no fallo de carga oculto: el runner debe informarlo explícitamente. No es prueba de calidad de implementación. P1/P2 exigen ejecutar las mismas expectativas sobre producto y mutantes reales, sin derivarlas del producto reparado.

Población: SDK Admin instalado y refs reales, operaciones interceptadas y almacén por project/database/path. Book real escribe una cita; endpoint real lista/muta esa misma. Browser se alimenta de suscripción vigente interceptada y devuelve histórico con mismoID. UIcallbacks reales se extraen por AST como instrumento previo, avisos interceptados; no E2E hospedado. Auth RSA real contra cert sintético. Ninguna red/DB/proveedor.

Oráculos: tenant servidor no request; mismoID no equivale a misma cita; cuerpo inválido noescribe; status/cancel/reschedule a origen, createdAt/clientId/campos ajenos intactos; manifest de origen, best-effort vigente; fallo no avisa y rollback sólo fila. Lista parcial se identifica y no oculta historial. Coincidencia física conserva browser. Request tardío de sesión antigua noentra. GET/PATCH+notify en ambos runtimes con gates explícitos; emisores ajenos rechazados sin retries. Contrato auth conserva estados/defaults y vinculación real tenant/rol en lookup.

Cobertura pendiente de concretar en instrumento: permisos de UI/render, colisión-ID, errores porfuente, cleanup/refresh, lookups y registro de ambos runtimes, avisos, mutación de origen y tenant. P0 NO completo hasta que esos controles existan y sean revisados. No implementar antes.
