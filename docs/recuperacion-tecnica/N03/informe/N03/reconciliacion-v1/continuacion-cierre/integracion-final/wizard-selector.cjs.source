const fs=require('fs'),{createRequire}=require('module'),crypto=require('crypto');const p='C:/Users/liama/Desktop/Nichos/Barber-shop-template-main/src/components/booking/BookingWizard.tsx',ts=createRequire('C:/Users/liama/Desktop/Nichos-hub/package.json')('typescript'),source=fs.readFileSync(p,'utf8'),a=ts.createSourceFile(p,source,99,true,ts.ScriptKind.TSX);let expression;function walk(n){if(ts.isVariableDeclaration(n)&&n.name.getText(a)==='isOnlinePaymentProvider')expression=n.initializer.getText(a);ts.forEachChild(n,walk)}walk(a);if(!expression)throw Error('missing');const f=new Function('PAYMENT_CONFIG','return '+expression),rows=[['manual',false],['none',false],['stripe',true],['cardcom',true]].map(([provider,want])=>({provider,want,actual:f({provider})}));const result={result:rows.every(r=>r.actual===r.want)?'GREEN':'RED',rows,limit:'Cardcom conserva intento bloqueado en backend; no se convierte automáticamente una reserva pendiente en confirmada.'};fs.writeFileSync(process.argv[2],JSON.stringify(result,null,2));console.log(result.result);process.exitCode=result.result==='GREEN'?0:1;
